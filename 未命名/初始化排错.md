几个表
	sPathDestGateTable//; 子通道目的钞口列表
	sPathMotorConfigTable//子通道电机配置表
	sTrackClearMotorConfigTable_Error//通道电机堵转后的排错配置, 将通道电机配置成跟原交易流程反向, 以便拉松塞死的纸币
	sPathStaticSelConfigTable//静态拨叉 (包括出入口拨叉)配置
	sPathDynamicSelConfigTable//动态拨叉配置
	sPathNodeTable//通道传感器节点配置列表
![[Pasted image 20241010094456.png]]


![[Pasted image 20241010104916.png]]
\# define TC_FLOW_CFG_INIT
	. **mPathList** = TC_PATH_##flow##\_##dest_gate,
	
\#define TC_PATH_FLOW_DEPOSIT_GATE_OUTLET
	TC_PATH_ENTRY (**TC_PATH_UMS04_UMS02_UMS01**)
	
TRACK_CLEAR_FLOW_CFG sTrackClearConfigTable[]
	TC_FLOW_CFG_INIT

\#define TC_PATH_ENTRY
	.**mPathIndex**     = index

TrackClear_ByFlow
	TrackClearInit
		//先检测一次通道内任意传感器位置是否有纸
		AppTrackNodeHasCash
		//通道清理配置编号获取
		TrackClearGetCfg
	//钞口/钞箱准备时会对钞口进行回吸动作,因此钞口准备完毕后需再次检测通道内是否还有纸币残留
	TrackPaperRemainCheckAfterGatePrepare
	i= 0
	for ()
	{
		\*PathConfig = &sTrackClearCtrl.mTrackClearFlowCfg->**mPathList**[i];
		**ErrCode** = TrackClearCtrl_Fun (**PathConfig**)
		++i
	}




![[Pasted image 20241010105730.png]]
TrackClearCtrl_Fun（PathConfig）
	PathIndex = PathConfig->mPathIndex
	NodeList
	//生成通道传感器顺序列表
	PathNodeListInit（PathIndex，NodeList）
	//静态拨叉控制
	TrackClearSelectorCtrl (PathIndex) 
	//启动电机 
	TrackClearPrepareAndStart（NodeList）
	for ()
		//动态拨叉控制
		//各种塞纸检测
		PathQuitFlag = 
		//通道纸币走到指定位置退出检测
		//电机停止检测
		print（TrackClearCtrlend，Quit：PathQuitFlag）



PathNodeListInit（PathIndex，TargetTrackNodeList） 
	 \*NodeList = **sPathNodeTable**[PathIndex]
	for (0, TRACK_NODE_NUM_MAX)
		TargetTrackNodeList->mNodeArray[i] = NodeId
		TargetTrackNodeList->mNodeDisMM
		TargetTrackNodeList->mNodeDisStep





![[Pasted image 20241010113423.png]]
![[Pasted image 20241010113736.png]] TrackClearSelectorCtrl（PathIndex）
	SelConfigList = sPathStaticSelConfigTable[PathIndex]
		for (0, SEL_NUM)
			\*SelConfig = SelConfigList[i]
			AppSelectorCtrl (SelConfig)