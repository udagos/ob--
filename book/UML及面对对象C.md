---
same:
  - "[[../note/2025-10|2025-10]]"
prev:
  - "[[book对象及设计模式]]"
tags:
---
# 1 介绍面向对象的观念及OOPC
# 2 认识对象
## 2.1 Lw_oopc 代码
![[Pasted image 20240627111021.png]]

## 2.2 对象与类 
类是群体 (或集合), 而对象是类中的一份子。人们常用是一个 (is a) 来表达对象与类之间的关系。例如: 月亮是一个星球。
类统一说明了对象应含哪些“数据”(Data) 和哪些“函数”(Function)
### 2.2.1 类的用途
由抽象数据类型（类）所声明的变量，称为对象。

例如, 定义类如下:
![[Pasted image 20240625201017.png]]
**Rose** 就是我们新创的数据类型，将**用来生成对象**
于是可声明对象如下：
![[Pasted image 20240625201042.png]]


Rose 的重要行为是:  说出它的颜色
则 Rose 类应增加一个函数 say ()
![[Pasted image 20240625201613.png]]
![[Pasted image 20240625201621.png]]
## 2.3 对象指针
指针也可以指向对象
![[Pasted image 20240625201700.png]]
![[Pasted image 20240625201712.png]]
## 2.4 构造器
设计类是一件重要工作，其目的是借之产生对象。幕后工作者就是构造器 
在 lw_oopc. h 头文件里已经定义了构造器宏，可以直接使用
![[Pasted image 20240625201848.png]]
CTOR 就是 Constructor 的简写，这个宏里定义了 RoseNew () 构造器，它会生成 Rose 的对象，并返回该对象的指针值，类型为 void\*。将之转为 Rose\*类型后存入 pr 指针变量里。

# 3 对象沟通方法
函数常借互相调用 (数据流动) 而堆成传统 C 程序。
**对象常借互传信息 (信息传递) 而堆成面向对象的 C 程序。**

其实对象交换信息，还是靠函数调用来实现的
**A 对象传送信息给 B 对象时**，意味着 **A 对象里的某一个函数调用 B 对象里的某一个或多个函数**
只是到底**调用 B 的哪些函数呢？这是由 B 对象内部判断决定**的，如此提升了 B 对象的重用性（Reusability）
信息传递（Message-Passing）本质上也是函数调用，只是函数被对象封装起来，就**对象用户而言，只能看到对象的信息沟通，而不知对象内会由哪一个函数来响应该信息**
![[Pasted image 20240627110756.png]]
## 3.1 信息传递示例说明
### 3.1.1 分析与设计
3 个软件对象 (Object) 来组成一个 Light Controller
![[Pasted image 20240627112843.png]]
### 3.1.2 设计 OOPC 类
WallSwitch 和 DoorSwitch 对象的特性和行为是一样的，所以可归为同一个类，取名为 Switch 类。而 Light 对象可归到另一个类，取名为 Light 类。如图12-3所示。
![[Pasted image 20240627112914.png]]
### 3.1.3 生成 OOPC 对象
刚才已经设计出类了，就拿类来生成对象
![[Pasted image 20240627112952.png]]
### 3.1.4 以 OOPC 实现
![[Pasted image 20240627113413.png]]
![[Pasted image 20240627113422.png]]

![[Pasted image 20240627133023.png]]

![[Pasted image 20240627113439.png]]
![[Pasted image 20240627113450.png]]
> **类是关联关系**
![[Pasted image 20240627133310.png]]

![[Pasted image 20240627134028.png]]
![[Pasted image 20240627113530.png]]

# 4 对象沟通实例
## 4.1 以向量类封装 1 维数组
![[Pasted image 20240627140103.png]]
## 4.2 运用 malloc 库函数
它能用 malloc () 库函数来取代, 而且会具有更大的弹性
![[Pasted image 20240627140324.png]]

## 4.3 运用 define 语句
上述 Vector 对象内只能存入整数向量值
![[Pasted image 20240627140416.png]]
## 4.4 运用 void\*指针
上述例子里, Vector 对象都含有向量元素的值。运用 void\*指针可以让 Vector 对象内含有向量元素的指针值:
![[Pasted image 20240627140705.png]]
![[Pasted image 20240627140721.png]]
![[Pasted image 20240627140737.png]]
pv[]为**对象指针的数组**，内含 3 个对象指针，分别**指向 3 个 Rectangle 对象**
![[Pasted image 20240627140800.png]]
## 4.5 以矩阵封装二维数组
### 4.5.1 定义 Matrix 类
![[Pasted image 20240627141608.png]]
![[Pasted image 20240627141638.png]]
### 4.5.2 Matrix 对象包含 Vector 对象
![[Pasted image 20240627142235.png]]
![[Pasted image 20240627142243.png]]

![[Pasted image 20240627142259.png]]
![[Pasted image 20240627142314.png]]


![[Pasted image 20240627142335.png]]
![[Pasted image 20240627142350.png]]
![[Pasted image 20240627142537.png]]
![[Pasted image 20240627142600.png]]


![[Pasted image 20240627142616.png]]
![[Pasted image 20240627142624.png]]
# 5 认识接口 
## 5.1 定义接口 
对象用来划分出稳定与善变的界线，这个界线就通称为接口‘

欲得到单纯的接口，最常见的方法是让对象所包装的细节部分具有高度紧密的相关性，也就是所谓的凝聚性（Cohesion）；并且让内部细节与外部的相关程度愈低愈好，因此，创造强凝聚性的途径称为面向对象  (Object-Oriented) 方法

对象接口的用途是：让别的对象知道它们能调用此对象的哪些函数。’
![[Pasted image 20240627074112.png]]

## 5.2 多个类实现同一接口
## 5.3 以接口实现多态性
具有相同接口的一群对象，称之为多态对象
![[Pasted image 20240627074356.png]]
这两个类实现 ICoin 接口，都有 value () 函数，而且各类实现的方式（即代码内容）都不相同，因而产生不同的行为。像 value () 这种函数，称为多态函数

**但各调用不同的 value () 实现代码。多态对象常存于一个数组（Array）中**，如图 14-3 所示。**或者存于链表（List）里，如图 14-4 所示。**
![[Pasted image 20240627074723.png]]
![[Pasted image 20240627074734.png]]
![[Pasted image 20240627082738.png]]
因 pi 指向 one_dollar 类的对象，计算机自动调用 one_dollar 类内的 value () 函数。当 pi 转而指向 five_dollar 类的对象时，计算机自动调用 five_dollar 类内的 value () 函数

设计一个函数，让售票机能接受及分辨硬币体系的对象
![[Pasted image 20240627082922.png]]
feedCoin () 为售票机类的函数，能接受硬币对象的指针，然后根据对象的类而自动寻找适当的函数

![[Pasted image 20240627083043.png]]

![[Pasted image 20240627083051.png]]
![[Pasted image 20240627083109.png]]
**以上三个类都支持 ICoin 接口。现在准备将这三种不同的硬币对象投入售票机里，此时一般程序员会想到：售票机类里应该会用一些 if 指令来判断对象的类，才能正确计算出所投入的总金额。其实不然，这是多态性带来的好处**
![[Pasted image 20240627084341.png]]
![[Pasted image 20240627084420.png]]
![[Pasted image 20240627084439.png]]
## 5.4 一个类实现多个接口 
![[Pasted image 20240627145815.png]]
![[Pasted image 20240627145823.png]]
一个类可以支持上述 IB 和 IC 两个接口。为了让 IB 与 IC 两个接口互换，将定义一个 IALL 接口，它包含 IB 和 IC
![[Pasted image 20240627145841.png]]
编写 Rect 类来支持 IB 和 IC 接口了。
![[Pasted image 20240627150045.png]]
![[Pasted image 20240627150057.png]]

这里 Rect 类针对 IB 和 IC 接口去实现它
![[Pasted image 20240627150138.png]]
此 pb 指向该对象的 IB 接口, 如果像要 pc 也指同一对象, 但不同的接口
![[Pasted image 20240627151025.png]]
# 6 接口应用实例
每块电池有正负两个接口
![[Pasted image 20240627151621.png]]
在本章里将不讨论灯泡（或灯头）部分，而把焦点放在电池接口的设计上，并说明如何基于接口而顺利将电池串联起来。
从图 15-2 中可看出有两种主要对象：手电筒内壳（FlashLightBody，简称 FlashLight）类和电池（BatteryCell，简称 Cell）类。如图 15-3 所示。

FlashLightBody 将对应到 FlashLight 类的对象, 而 Cell 将对应到 PanasonicCell 或 CatCell 类的对象
ICell 就是 PanasonicCell 或 CatCell 类必须支持的接口。而 **FlashLight 对象则负责将不同类的 Cell 对象以串联方式组合起来**

15-3 中的手电筒将对应到 OOPC 程序里的 main () 主程序, 它负责生成 FlashLight 及 Cell 对象, 并且将 Cell 对象放入 FlashLight 对象里面
![[Pasted image 20240627151819.png]]

藉由精致的接口设计, 就能随时组合出串联式手电筒了

在此例子中, 除了设计 ICell 接口之外, 还会设计一个 ILight 接口, 如图 15-6 所示。
![[Pasted image 20240627153640.png]]
由于 CatCell 和 PanasonicCell 两个类都支持 ICell 接口, 所以其对象具有多态性。这会让 FlashLight 的设计简单许多。
## 6.1 以 oopc 进行接口设计
![[Pasted image 20240627153744.png]]
![[Pasted image 20240627154204.png]]

![[Pasted image 20240627154214.png]]
![[Pasted image 20240627154222.png]]

![[Pasted image 20240627154244.png]]
![[Pasted image 20240627154252.png]]

![[Pasted image 20240627154303.png]]
# 7 集合对象链表
## 7.1 认识集合对象
集合能容纳多态化的意见, 且容量无限。
反观计算机软件中常见的数组 (Array), 包含若干有序的变量。只是数组内的变量, 其**类型必须一致**, 是一元化的集合体。
![[Pasted image 20240627092728.png]]

请看看**结构 (Struct) 吧! 它比数组更具多态性, 能包含多种类型的数据**, 例如:
![[Pasted image 20240627092806.png]]
,account 结构无法容纳其他类型的数据; 而且肚量有限, **不能延伸扩充**。为了支持 OOP 的重要观念——多态性 (Polymorphism), 需要具备多态性且可伸展的集合对象 (Containerobject)。

集合对象如菜篮子般, 能装各种蔬菜水果。简而言之,**集合对象是多态性的数据结构** (Polymorphicdatastructure)


是具有相同接口的对象皆为多态性对象; 这群对象最适合放入集合对象里。在各种数据结构中, 数组 (Array) 是最简单的。**除了数组外, 还有向量 (Vector)、链表** (Linkedlist)、树 (Tree), 等等。其用途是将相关的对象集合起来, 表达对象间的关系，使得计算机软件易于掌握对象间的复杂关系

例如: 一支棒球队, 含各种成员, 且各有不同的角色, 像经理、教练及球员等各有各的职责。由于他们属于同一支球队, 各自拥有重要角色
现在请看如何用一般数组表达上述棒球队的组织吧!
![[Pasted image 20240627093058.png]]
![[Pasted image 20240627093121.png]]
![[Pasted image 20240627093131.png]]
players[]含 20 个指针可指向字符串, 但它至多可指向 20 个球员
缺点为:
必须先确定球员人数
必须费神处理变量 i，若当某支球队聘请多位经理及多位教练时, 岂非要许多个像 i 这种下标呢?
## 7.2 以 OOPC 实现 LList 集合类
LList 集合类, 代替上述 players[]数组
**LList 对象内含多态性链表 (LinkedList**), 能存不同类的对象, 如果这些对象又具有多态性, 就更能发挥巨大效益
![[Pasted image 20240627093758.png]]
![[Pasted image 20240627095211.png]]
![[Pasted image 20240627093850.png]]

![[Pasted image 20240627095256.png]]
![[Pasted image 20240627094241.png]]

## 7.3 vector 集合类
![[Pasted image 20240627095937.png]]


![[Pasted image 20240627095946.png]]
![[Pasted image 20240627095956.png]]
Vector 类与前面 LList 的区别在于: **LList 可随内部对象个数增加而不断扩大容量。但是 Vector 在生成时必须设定其容量**, 然而, Vector 的 Add () 执行效率较佳。各有长处, 视需要而决定采用哪个。
> 和数组区别在调用时定义大小，不是单一类型



![[Pasted image 20240627101858.png]]
![[Pasted image 20240627102632.png]]
![[Pasted image 20240627102647.png]]


![[Pasted image 20240627102701.png]]

![[Pasted image 20240627102710.png]]
![[Pasted image 20240627102720.png]]


![[Pasted image 20240627102735.png]]
![[Pasted image 20240627102743.png]]


![[Pasted image 20240627102753.png]]
本程序表示先装入两块松下牌 (PanasonicCell) 电池, 再装入一块黑猫牌 (CatCell) 电池, 手电筒计算出总电量为 7, 并显示出来。当你继续装入电池时, 手电筒会累积而计算出总电量。





# 8 介绍 UML
# 9 uml 图示
![[Pasted image 20240625202146.png]]
# 10 uml 类图
## 10.1 为什么要描述类间的关系 
在开发软件时，不仅需要描述对象的属性（Data）和行为（Behavior），而且必须描述对象之间的沟通及其结构面的连接关系
在上一节里已经说明了，在 OOPC 程序里，必须定义类来叙述对象，所以在类定义里，不仅需要定义类的内部属性及函数，还得定义类间的连接关系。这种连接关系包括组合（Composition）关系及结合（Association）关系等。
## 10.2 为什么要绘制 uml 类图 
....
类定义的主要工作是要描述下述各项：
对象的属性, 这表达于类的数据成员。
对象的函数, 这表达于类的成员函数。
对象与对象间的关系, 这主要表达于类间的组合关系和结合关系。

除了程序员使用上述的对象思维之外，系统分析员及架构设计师们也是采取一样的思维。程序员通常很熟悉计算机语言，能轻易地将心中的类和对象表现为 OOPC 的类和对象
**系统分析员和架构设计师们，通常并不熟悉 OOPC 等计算机语言，那么又该如何表达其心中的类和对象呢**。以图形方式呈现出来，并且传达给程序员，再编写成为 OOPC 程序代码。

我家的电冰箱里有个小灯，冰箱门打开时，它就亮起来；一旦关上门，它就熄灭了
寻找到冰箱和小灯两个类。当冰箱门每次被打开时，冰箱就传送 turnOn () 信息给小灯。小灯就亮起来了
有人认为冰箱与小灯两个类应该是组合关系，另有些人会认为两者应该是结合关系。这两个观点没有对与错之分，如果认为是结合关系，就使用 UML 表达出来；若认为是组合关系，也可以使用 UML 表达出来
**这就是为什么要绘制 UML 类图的基本理由：取得共识。**
![[Pasted image 20240702133634.png]]
如果程序员也认同图 19-4，就能基于此类图而编写为 OOPC 的类代码了。

## 10.3 如何得到类 
### 10.3.1 从对象归类而得到类
对象那么多, 哪个是跟此系统或程序有关的呢? 
例如: 在设计一个销售系统时, 用户是一重要对象, 产品及订单也是重要对象; 至于原料的产地及原料的供货商虽然是明显的对象, 但不见得与销售系统有关。
反之, 若您所设计的是采购或生产系统时, 原料、产地及其供货商就成为重要对象了

现在就来介绍较实用的找对象方法。其中最常见的是，从有关文档着手。在文档里会发现下述线索，再进而找出对象。
(1) 人 (People)——人是任何系统的重要角色, 通常是最容易找到的对象。例如: 公司有 5 位销售员, 各负责一个地区的任务, 并与该区的用户联络。从这段叙述中, 就可发现两种对象——销售员及用户, **每一位销售员皆是对象; 同样地, 每一个用户皆是对象。**
(2) 地方 (Sites)——地点是很容易发现的对象。例如: 您从订单上看到产品将送达的目的地、用户的所在地。再拿旅行社行程来当例子, 各旅行团将在不同的观光地区停留, 各观光地点皆是对象。
(3) 事物 (Things)——在可摸到或看到的实物中, 也容易找到与系统有关的对象, 例如: 产品是销售系统及生产系统的明显对象, 原料细项是生产及库存系统的重要对象。就饭店管理系统而言, 房间是重要对象。书本及杂志为图书馆或书店管理系统的明显对象。
(4) 事件 (Events)——在企业界最常见的事件是交易, 当事件发生时, 我们会去记录它发生的时刻、有关金额等。这种我们所关心的事件也常是重要的对象。值得注意的是, 这些事件是已发生的, 为一项行为或动作。所以在文档中, 常是一个句子的动词。例如: 今天共有 3 种原料已降至安全存量之下, 所以共订购 3 种原料。这每一订购 (Ordering) 事件皆为对象。就飞机场的控制系统而言, 每次飞机起飞或降落皆是重要对象。而提款及转账皆为银行系统的重要对象。挂号及就诊为医疗系统的对象。
(5) 构想 (Ideas)——与企业营运或机构管理有关的构想或各种计划或其他概念; 这些无形的, 但决定企业活动的构想, 常是重要对象。例如: 公司正拟定 3 种广告策略, 其中每一种策略就是企业营销系统的重要对象。这家公司正透过 2 种渠道与小区居民沟通, 这里的渠道也是概念性的对象。
(6) 外部系统或设备 (ExternalSystemsorDevices)——软件系统常会与其他系统沟通, 互相交换信息。有时也由外部设备取得交易数据或把处理结果送往外部设备。这些外部系统或设备也是对象。例如: 库存系统与采购系统会互相沟通, 对库存系统而言, 采购系统是对象; 反之, 对采购系统而言, 库存系统则为对象。如果收款机直接把交易数据传送给销售系统, 则收款机是此销售系统的对象。如果股票系统直接把数据传送到交易市场的电视显示屏上, 则对股票系统而言, 电视显示屏是对象。
(7) 组织单位 (OrganizationUnits)——企业机构的部门或单位。例如, 在学校管理系统中, 教务处及训导处等单位皆是对象。 
(8) 结构 (Structures)——有些对象会包含其他对象。所以在对象中常能找到其他对象。例如, 在学校的组织单位——教务处, 含有小对象如注册组及学籍组等。在汽车对象中可找到引擎、轮胎及座椅等对象。在房屋对象中, 会发现到厨房、客厅、沙发等对象。
### 10.3.2 从领域概念找到类  
领域知识的组成要素是领域概念 (DomainConcepts)。概念有它的属性 (Attribute), 概念之间有其关系 (Relationship)。系统分析员就将分析到的概念对应到面向对象语言 (如 OOPC、C++、Java 等) 里的类 (Class), 如此就分析出类了。
简而言之, 面向对象分析 (OOA) 就是要分析**领域知识里的概念**, 并以计算机语言的类机制来表示, 然后将其存入计算机中, 让计算机拥有专业知识, 提供专业的服务


找出领域知识里的概念，就是找出软件系统的对象和类
....
# 11 用例图
## 11.1 为什么需要用例图
用例图 (UseCaseDiagram) 表达了**系统提供用户哪些服务**
由于是用户付钱去买的系统服务, 是系统需求的核心部分, 所以它是 UML 想要表达的重点所在。

为什么要特别看待这些功能呢？因为这些是用户认为有价值的服务
它擅长表现用户为什么（Why）要使用（Use）系统，以及如何（How）使用系统。

用例在系统的众多功能当中, 属于直接提供用户服务的部分。例如麦当劳餐厅可视为一个系统, 它具有许多功能, 像卖汉堡、采购蔬菜
其中有一小部分是用户能看得见、感觉得到又觉得有意义的服务, 就是用例; 而系统的其他功能就不称为用例, 而只是一般的系统功能罢了。
## 11.2 用例的内涵 
**一个用例就表达一项系统服务**
当系统在提供完整的服务或产品的过程中，会执行一连串的小活动
例如，人们去麦当劳餐厅买汉堡时，柜台人员会向用户询问是外带还是内用，而决定其包装程序
因此，每个人去买汉堡时，其使用麦当劳服务系统的途径会有些相同，也可能有些不同。

**每个人使用系统的途径就是个实例**（Instance），特别**称为场景**（scenario）或实景。虽然每个人使用系统的场景会有些差异，但是若用户的**目标（Goal）是相同的，则其场景也会极为类似。那么这些类似的场景的集合就是个类（Class），这种类特别称为 UseCase**
**这种类 UseCase 其实例（Instance）就是场景（Scenario）。至于一般的类, 其实例是对象 (object)**

由上所述，人们是**依用户的目的（Goal）来将场景分门别类（Classify），由此得到不同的用例**
同类（UseCase）内场景大同小异，所以就可把其共同而重要的特点（如活动和顺序）以文字描述出来，根据 Wirfs-Brock 的建议，可只描述用户与系统之间的对话
![[Pasted image 20240627182402.png]]

**用例的主要内涵及其表达为：**
- 描述该服务的名称及返回的信息类型, 它描述 **What**。
- 描述服务过程中,**用户与系统之间有哪些交互 (即两者的对话流程**), 其描述 **How 和 When**。但是请留意, 这个 How 不是员工做汉堡的 How, 而是用户如何与系统最好交互的 How。请你务必区别开它们。
- 描述用户 (又称为 UseCase 的 Actor) 的角色, 它描述 **Who**。
### 11.2.1 用例图表达 What 及 Who
一个餐厅的用例图如图 20-2 所示。
![[Pasted image 20240627181937.png]]
图 20-2 表达了 What 和 Who。在 What 方面，**一项用例表达了用户对系统的一项需求**，也就是系统的一项责任（Responsibility）或功能（Function）。

在 Who 方面，一个系统常含有许多的用例，而环境里的人或物（即系统的用户）在不同时刻，可能扮演**不同角色来使用系统中的不同用例，以取得系统的不同服务**。角色（role）代表一群对系统怀有相同兴趣或目标的人或物，在 UML 里称为 Actor。

当您扮演 actor 2 时 (怀有一个目的), 就可使用系统, 此**时系统就执行 UC**: 预订晚餐来服务你。同样地, 当您扮演 actor 1 时, 就可使用 UC: 吃晚餐或 UC: 喝下午茶来服务你。
### 11.2.2 用例描述表达 How 及 When
图 20-2 比较偏重于用例的功能面，主要在行为方面，说明系统如何配合 Actor 使用系统的过程，才能顺利达成 Actor 的目的。也就是说用例的行为决定于 Actor 的使用过程。
因此，在用例描述里应详细描述 Actor 使用系统的途径，以及系统应该配合的行为。**例如上图里的 UC：吃晚餐，可写个 UseCase 描述来描述它们，如图 20-3所示。**
![[Pasted image 20240627182710.png]]
**也就是 Use Case  描述**

在 U**se Case 图中, 只表示出用例的外观行为**, 而把用例内的行为封装起来。到了系统设计阶段, 就可使用下一章将介绍的 **UML 序列图来表达用例内部的行为**
## 11.3 用例与对象的密切关系
仔细分辨**用例（UseCase）、对象（Object）与系统（System）的不同**，以及它们的相关性

用例是用来描述系统的服务细项。用例是系统的一个观点（View），是系统用户（User）内心对系统的期待，也就是用户对系统的需求（Requirements）

在用户眼中的**系统就像青蛙眼中的池塘，用例就像青蛙眼中的荷叶**，青蛙眼中的各片荷叶之间并没有相连，也不关心各片荷叶在水面下是如何连接在一起的。至于架构师（Architect）或设计师（Designer）则关心水面下的根茎应如何支撑水面上的荷叶，水面下有许多子系统（Subsystem）或对象，其支持着水面上的各片荷叶。

不同的用例可要求同一个对象来提供服务，即一个对象可同时参与许多个用例，如图 20-5 所示。
![[Pasted image 20240627183208.png]]
## 11.4 用例的经济意义 
一个对象可参与许多个不同的用例，让对象有更多复用（Reuse）的机会
## 11.5 用例间的关系 
各用例在其幕后都是共享同一群对象。也就是用例之间自然就具有共享模块的关系
于 UML 用例图只呈现外观，所以在用例图里看不到这种关系。

**在外观方面，UML 定义了两种关系**，分别是包含（Include）和扩充（Extend）关系
- **包含关系**
用以表示某一个用例的对话流程中，包含着另一个用例的对话流程。

**当出现几个用例有相同部分的对话流程时**, 将相同的流程记录在另一个用例中, 前者称为基础用例 (BaseUseCase), 后者称为包含用例 (InclusionUseCase)。这样一来, 这些基础用例就可以共享包含用例, 而且未来其他的用例只要创建包含关系, 就可以立即享用已经在其他用例定义好的相同对话流程了。如图 20-10 所示。

![[Pasted image 20240627183551.png]]
这是 UC: 结账的用例图和 UCD。**经过一些时日之后, 系统新增功能了, 多了一个新的用例**如图 20-11 所示。
![[Pasted image 20240627183630.png]]
**此时, UC: 买汉堡套餐的 UCD 就不必重复编写一次结账部分了。可单独编写 UCD 如上。**


- **扩充关系**
两个用例之间可以有扩充 (Extend) 关系, 用以表示某一个用例的对话流程, 可能会依条件临时插入另一个用例的对话流程中

有了扩充关系后, 便可以**将特定条件下才会引发的流程记录于扩充用例中**。在执行基础用例期间, 可以只是单纯地执行基础用例所记载的流程; 但是在特定条件发生时, 则会额外插入并执行扩充用例所记载的流程。如图 20-12 所示。
![[Pasted image 20240627184106.png]]
可写 UCD 如下
### 11.5.1 UC 描述: 结账
店员告诉用户总共多少钱; 
用户付钱;  
店员找钱;  
店员将餐点和发票交给用户。
### 11.5.2 UC 描述: 买汉堡套餐
用户告诉店员购买汉堡套餐;  
店员准备并包好餐点;  
进行“UC: 结账”; 
**如果是儿童来买**, 进行“UC: 赠送玩具”。
### 11.5.3 UC 描述: 赠送玩具
店员问儿童想要什么玩具;  
儿童选取玩具。
## 11.6 企业用例与系统用例 
### 11.6.1 上下层的用例 
企业用例是指高层级的用例, 而系统用例则是低层级的用例, 但这只是相对关系而已。
### 11.6.2 上下层用例的关联 
（以面向对象的企业工程所产出的模型为基础，来导出信息系统的模型，是个极直截了当的途径。）
导出的步骤，如图 20-14 所示。其详细步骤如下：
	使用到系统的 Worker，就成为系统的 Actor。
	企业 Actor 会用到系统，它们也成为系统的 Actor
	针对每一个**企业用例, 查看该用例的 Actor 及各 Worker**, 若它们会成为系统 Actor, 就为它们个别创建一个系统用例
	企业里的 Entity 对象, 代表着 Worker 必须用到的重要事物, 这些重要事物必须长期记录与保管, 所以它们也成为系统的 Entity 对象。

![[Pasted image 20240627185606.png]]
从图 20-14 中，你可以看到**企业用例的幕后有一群 Worker 和 Entity 对象互相合作来提供企业用例所追求的目标**，以满足用户的期望。图 20-14 也相当于图 20-15所示。
![[Pasted image 20240627185631.png]]

### 11.6.3 从上层用例导出下层用例的步骤
善用此关联性，就能轻易地藉由上层用例的引导而找出优质的下层用例了
为了让你更熟悉导出的程序，下面以 **RRR 汉堡快餐店**为例详细说明其推导过程，其步骤如下。
step 1：以**企业为核心**, 看看外界有哪些**人或单位会跟 RRR 企业来往**
![[Pasted image 20240627185719.png]]
Step-2 确定我们要**创建企业用例图的对象** (即 RRR 汉堡快餐店）
![[Pasted image 20240627185800.png]]
Step-3 把焦点针对用户的目的 (Goal)。就 RRR 企业而言, 图 20-17 的用户和玩具商皆是它的 Actor。**针对各 Actor, 确定它与 RRR 企业打交道的愿望** (Expectation) 或称为目的
![[Pasted image 20240627185914.png]]
Step-4 把焦点针对**企业内部的 Worker。例如, 企业里有柜员与信息系统 (IS) 两个主要  Worker**。**针对各个用例, 厘清 Worker 之间的沟通与合作关系**。
例如**针对“UC: a”  而厘清 worker 之间的合作关系**
![[Pasted image 20240627185922.png]]
Step-5 确定我们要设计**系统用例图的对象**, 如果是 IS 系统
![[Pasted image 20240627185944.png]]
Step-6 针对会**与 IS 打交道的 Worker, 逐一厘清这些 Worker 的愿望或目的**
![[Pasted image 20240627190009.png]]
接下来, **针对另一个 UC: b, 厘清 Worker 之间的沟通与合作关系**
![[Pasted image 20240627190103.png]]
Step-8 在这个新的“UC: b”中, 用户通过 Web 直接使用 IS 系统, 此时用户也成为 IS 模块的 Actor 了。例如用户跟 IS 打交道的目的只有一个:z
![[Pasted image 20240627190336.png]]
Step-9 就找出优质的系统用例了
![[Pasted image 20240627190412.png]]
# 12 序列图
## 12.1 UML 序列图的意义
用例（UseCase）是收益与成本的衔接点
收益方面，用例引导我们用心探索围绕使用的一连串为什么的问题，从而**厘清了**
 系统提供各种功能的目的;
 用户跟系统的交互情形。
用例让软件人员暂时不考虑软件系统内部的行为和结构，而是专注于厘清用户为什么去使用这个系统

因此**用例也引导我们基于“对象合作的角度”去进行系统的构建**。例如,“开前车灯”的 UCD: 当司机需要开前车灯时, 就按下前灯开关, 摩托车就从电池送电流给前车灯, 前车灯就亮了。接着司机可能会调整远近灯, 就按下前灯开关, 摩托车就变换前车灯 (远近灯 ), 替代的灯就亮了。

==从系统内部对象的合作角度看，一个用例就是系统内部的一群对象为了共同完成一项用例，而展开的对象交互过程==。UML 序列图是用来表达这一交互的情境
图 21-1 就是开前车灯用例的序列图。
![[Pasted image 20240627190703.png]]
**这张序列图准确地表达出用例幕后的对象合作情形。一个用例通常会对应一个序列图**
## 12.2 UML 序列图的语法 
一个用例通常会对应一个序列图。其表达出一群对象经由信息传递、互助合作来达成用户的某项期望
如图 21-2 所示，它表达出银行托收业务的收件用例的对象间交互情形‘
![[Pasted image 20240627191008.png]] ’即使图 21-2 是正确的，也不能表示这 3 个对象的角色已经是最佳的了。因为这些对象可能还会参与别的用例
一个好的系统必须每个对象都能和谐交互地达成每一个用例代表的服务。


UML 序列图的语法结构，**序列图的上方放置对象**，
对象下方的虚线代表对象的生命期，此虚线称为对象的生命线
带箭头的实线代表信息与传送方向。**对象接收到信息后，会执行某项工作。对象执行工作的期间，称为活动期**。

对象接收了两个信息，就必须执行两项任务。在执行此两项任务期间，对象发出三个信息
![[Pasted image 20240627191233.png]]
## 12.3 从序列图落实到 OOPC 类 
序列图表达了对象的沟通合作情形，这些对象都是由类所定义的，所以从**序列图的对象可对应到类图里的类**
而类图里的类会对应到软件里的 OOPC 类，所以 UML 序列图是 OOPC 程序员编写代码时非常有用的指引
说明如何从序列图对应到 OOPC 代码类。
### 12.3.1 共享类的考虑
虽然在已有的用例里，电池对象是很棒的，但面**对新需求下的新用例，经由新用例的序列图检验出此电池对象并无法支持新的用例时**
该电池对象已经不合时宜了，有必要修改或是整个换新
事实上，新的用例会促使某些对象变得老旧，

使用 EA 等开发工具的好处之一就是能快速修改用例图、对象类图及序列图等
在 EA 环境里能迅速更改序列图，如图 21-9 所示。
![[Pasted image 20240629091336.png]]
对象是许多用例共享的, **一个对象可能参与数个用例**, 所以在**设计某一序列图时, 其参与对象的类定义可能早已存在了, 而且参与了几个用例。所以您不是从某一个用例 (或序列图) 而生成新对象类定义, 而是从新的用例 (或序列图) 来更新对象的类定义**

您必须知道如何从序列图对应到对象的类定义, 才能知道已有的类定义是否足够支持新的用例的需要。如果发现已足够了, 就直接使用 (Reuse) 原来的类定义即可; 如果发现不够, 就须对类定义进行修改
### 12.3.2 对应到类
### 12.3.3 对应到函数 
图 21-10 上面的对象, 每个对象都必须对应到其类定义, 包括逻辑上对应到 UML 类图 (ClassDiagram) 里的类, 以及程序实现上对应到编程语言 (如 OOPC) 的类定义。
**图 21-10 表明序列图里的对象对应到 OOPC 代码中的类声明。**
![[Pasted image 20240629091923.png]]


信息的箭头指向某个对象的活动期符号（矩形），意味着**对象接到一个信息时**，该对象就会去进行一项运算服务，也就是会去**执行类里的一个函数**（Function）
如图 21-11 所示
![[Pasted image 20240629092028.png]] 

此**信息 MakePurchase 是传递给 CreditCard 对象的**，信息箭头指向一个矩形，**代表此对象会执行类里的 MakePurchase () 函数**

如果对象的生命线上有**两个矩形**，而且信息名称不相同（即接受两个不同的信息），就必须使两个矩形各对应到一个函数，如图 21-12 所示
![[Pasted image 20240629092244.png]]

以上是**针对进入（Incoming）的信息，当对象接到一个信息时，会执行矩形所对应的函数**

**在执行此函数的过程中，会发出信息给其他对象（即调用其他类的函数），就产生了外传（Outgoing）信息了。**
### 12.3.4 对方对象→信息名称 (参数)
在序列图里, 矩形的一个外传信息会对应到函数里的一个信息传递指令。**如图 21-14 里的 MakePurchase 信息箭头所指向的矩形有三个外传信息。**
![[Pasted image 20240629092637.png]] 
....
## 12.4 UML 序列图示例说明
### 12.4.1 绘制用例图 
编写过 **Toggle Light Controller 软件程序, 它设计了两项功能来直接服务用户**, 如图 21-16 所示。
![[Pasted image 20240629093832.png]]
**从 User 的角度来看, 整个 Toggle Light 系统需要提供两个用例:**
	 使用 Wall Switch 开灯或关灯; 
	  使用 Door Switch 开灯或关灯。
	![[Pasted image 20240629093845.png]]

现**设计三个软件对象** (Object) 来组成一个 LightController。其中, WallSwitch 对象控制 WallSwitchSet 硬件设备; DoorSwitch 对象控制 DoorSwitchSet 硬件设备; 而 Light 对象控制多个 LightBulbSet 电灯。
### 12.4.2 绘制类图 
为什么是一个 ToggleSwitch 类产生两个对象呢? 而不是设计两个类 WallSwitch 和 DoorSwitch 各生成一个 Switch 对象呢? 这完全是类设计者的专业判断了。一般而言, 如果 WallSwitch 和 DoorSwitch 两个对象的数据属性 (Attribute) 和行为 (Operations) 都一样, 只需要一个模子就够了
![[Pasted image 20240629094109.png]]
### 12.4.3 绘制序列图 
**运用 UML 序列图表达用例背后的对象合作情形。**
	**针对 UC-1:** turn light on or off with **Wall** Switch 其序列图如图 21-19 所示。
	![[Pasted image 20240629094250.png]]
	**针对 UC-2:** turn light on or off with **Door** Switch 其序列图如图 21-20 所示。
	![[Pasted image 20240629094259.png]]
### 12.4.4 对应到 OOPC 类
**序列图里的对象必须对应到类图里的类**，如图 21-21 所示。
![[Pasted image 20240629094512.png]]
**序列图的对象也会对应到 OOPC 代码类**，例如在第 12 章里就编写了如下的 OOPC 代码：
![[Pasted image 20240629094646.png]]

**由于 UML 与 OOPC 都基于相同的 OOP 思维, 使得 UML 序列图与 OOPC 代码能有完美的对应关系**
接着, OOPC 程序员又依循图 21-22 所示的对应关系。
![[Pasted image 20240629100742.png]]
在第 12 章里, OOPC 程序员就编写了如下的代码:
![[Pasted image 20240629100916.png]]
![[Pasted image 20240629100925.png]]
这种完美的对应，其实来之不易，需要系统分析员、架构设计师和程序员之间都有一致的面向对象思维才行，希望你的团队能拥有这样的默契
# 13 状态图
## 13.1 why 状态图
状态机能把系统分为多个状态 (State), 藉由状态将事件 (Event) 分组, 让人们对复杂的事件分而治之。如果你想构建的系统属于控制系统，UML 状态图就可以帮你许多忙。

状态图的主要用途是明确地说明在什么状态下，哪些事件是有效的，哪些是无效的，这统称为事件分组（Partition）。

## 13.2 简介 uml 状态图
### 13.2.1 状态、事件和转移
状态图 (statechart) 可以让我们**==聚焦在单一种类的对象本身==, 描述对象一生中可能出现的状态 (state) 变化。**
以 Skype 这类的网络通话系统为例, 我们**设计了用户对象来对应真实世界的用户**。针对通话服务, 如果我们想要看到**用户对象**从生成到消失之间全时期的变化状况, 这时就可使用状态图来描述用户对象全时期的生命周期, 如图22-1所示。 
![[Pasted image 20240629102246.png]]

除了事件之外，转移线旁还可以标示出参数（Parameter）、警戒条件（Guard）及活动（activity），如图 22-3 所示
![[Pasted image 20240629102808.png]]
当事件发生时, 除了会带进参数外, **还必须同时符合转移线旁的警戒条件, 才能够转移状态并执行转移线旁的活动**。以 Skype 为例, 如果我封锁了某位联络人, 将无法与他通话。换言之, 并不是拨打事件发生就一定会转移状态, 如图 22-4 所示。
![[Pasted image 20240629102908.png]]
### 13.2.2 活动 
除了**转移瞬间可以执行活动外, 对象处于某个状态期间时, 也会执行活动**。状态内部的活动有**两类, 一类是预设好、一定会执行的内部活动; 另一类是因事件而引发的活动。**
在状态图示内部可分为三个间隔, 分别放置状态名称、一定会执行的活动, 以及由事件引发的内部转移, 如图 22-7 所示。

图 22-7 中含有**三个预设的内部活动**, 分别为 entry、exit 和 do
	entry 意味着对象进入此状态时，必须立即执行且不可被中断的行动
	当对象接到事件并打算转移到下一个状态之际，必须在离开前执行 exit 行动
	do 活动则介于 entry 与 exit 之间，对象在执行完 entry 行动之后，就会开始执行 do 活动。对象可能持续执行 do 活动直到发生外部事件而中断，接着执行 exit 行动，而后转移到下一个状态。
	![[Pasted image 20240629103811.png]]
以网络通话为例，用户对象一进入通话状态，就立即执行记录通话起始时间的行动，接着持续执行传送数据包的活动，直到挂断事件发生了，才会结束传送数据包的活动，并且立即执行记录通话结束时间的行动，如图 22-8 所示。  
![[Pasted image 20240629103844.png]]
### 13.2.3 复合状态 
当一状态内部含有其他的子状态（Substate）时，我们就称这样的状态为复合状态
在图 22-9 中, 执行中复合状态, 将联机及通话状态包在里面, 成为它的子状态

**最后，用户对象会停留在通话子状态中，直到发生挂断或注销事件，才会离开复合状态**
![[Pasted image 20240629104031.png]]
![](../fujian/Pasted%20image%2020241220100817.png)
### 13.2.4 子机状态
另一种很好用的子机状态 (SubmachineState)。当我们想要复用某个状态机设计时, 可在状态图内放置子机状态, 表示复用某个状态机设计
...
### 13.2.5 历史状态 
历史状态（HistoryState）可以记住并重新进入**对象在离开复合状态时的最后一个待过的子状态**
**对象会因特殊事件发生而中断正常状态**，随后又复原到中断时的状态并继续执行。历史状态在这样的设计中特别好用
![[Pasted image 20240629104520.png]]
历史状态放置于复合状态内部
这个设计意味着, 对象在在线复合状态的任一子状态期间, 可能会突然遭遇网络不正常而断线, 因而离开复合状态, 并进入断线状态。随后, 一旦网络复原, 对象会从断线状态再度进入在线复合状态。由于断线状态连接到历史状态, 所以对象会获得历史状态的记忆, 直接进入先前离开时的子状态中


对象进入历史状态，就相当于转移进入最近一次的子状态，所有该执行的entry行动，一个都不能少
例如，对象是在**通话子状态期间突然遭遇网络不正常断线，因此对象会先执行通话子状态里的 exit 行动**，记录通话结束时间后，才离开通话子状态。**紧接着，对象会开始执行在线复合状态里的 exit 行动，记录在线状态离开时间**，最后离开在线复合状态，**转移到断线状态**。对象从通话子状态转移到断线状态期间，必须依序执行图22-13中1号及2号标示的 exit 行动。
![[Pasted image 20240629105525.png]]

**网络恢复之后**, 对象将**从断线状态转入当前离开的通话子状态**。首先, 对象会先进入在线复合状态, 并且执行它的 entry 行动, 记录在线状态进入时间。接着, 对象才会进入通话子状态, 并依序执行它的 entry 行动, 记录通话起始时间, 以及它的 do 活动, 传送数据包。对象从断线状态转移到通话子状态期间, **必须依序执行图 22-13 中\*3 号、\*4 和\*5 号标示的 entry 行动。**

### 13.2.6 决策 
决策 (Decision) 比较简单, 我们常常会依据不同条件而决定状态转移的途径
![[Pasted image 20240629155901.png]]
### 13.2.7 汇合
刚才的决策是一种分岔路, 视条件而**转移到不同的状态之后, 可能汇合 (junction) 进入同一个状态**
![[Pasted image 20240629155941.png]]
### 13.2.8 并行 
嵌入式或实时系统的主要特点是, **经常同时执行许多个活动**
系统里会规划许多可**并行的线程** (thread) 来执行这些活动。在 **UML 状态图里, 可以运用它的并行** (concurrent) 状态来表示
如将**图 22-15 的含义改用下述的并行状态图表示, 如图 22-16 所示**。

一开始进入通话状态时, 就立即进入两个并行的子状态机, 各子状态机都会处于单一的状态。一旦要离开通话状态, 会执行累计通话点数活动, 此时会根据各状态机的状态而决定如何执行累计通话点数活动。

![[Pasted image 20240629160200.png]]
### 13.2.9 同步 
在**两个或多个状态并行状态之间, 它们有时候会互相协调**
通话与录音两个并行状态必须同步才能正确地录下通话的内容, 而且两者都必须完成了才结束 Skype 的通话联机。
![[Pasted image 20240629160346.png]]
## 13.3 如何使用 oopc 实现 UML 状态图
### 13.3.1 举例: 以小灯状态为例
以电冰箱为例，冰箱里有个小灯，当冰箱门打开时，它就亮起来；当关上门时，就熄灭了
将冰箱与小灯拟人化（Anthropomorphize），当冰箱知道门被打开时，冰箱的状态就改变，发出 turnOn 信息给小灯。就小灯而言，turnOn 是个事件（Event），这个事件令小灯改变状态（亮

UML 状态图 (State Diagram) 可表现小灯状态的转移
![[Pasted image 20240629160631.png]]
**小灯是一个对象**，它有图 22-26 所示的**两个状态**。于是编写 **Light 类及其接口**的代码如下。
![[Pasted image 20240629162306.png]]
![[Pasted image 20240629160811.png]]
![[Pasted image 20240629162329.png]]

![[Pasted image 20240629160904.png]]
### 13.3.2 举例: 以冰箱的状态为例
**另一个对象冰箱**的状态转移图吧！冰箱有两个状态开着门（DoorOpened）与关着门（DoorClosed）。打开冰箱门时，事件发生了，冰箱由 DoorClosed 状态转移至 DoorOpened 状态

打开门 (openTheDoor) 时, 冰箱转移至 DoorOpened 状态。此种**状态变化促使冰箱对象产生一项行动——送出 turnOn () 信息给小灯, 于是小灯转移至 On 状态**, 灯就亮了。关起门 (closeTheDoor) 时, 冰箱转移到 DoorClosed 状态, 此时冰箱产生一项行动——送出 turnOff () 信息给小灯, 于是小灯转移至 Off 状态, 灯熄灭了

**冰箱的状态转移图** ![[Pasted image 20240629161057.png]]
**冰箱与小灯都是对象，也各有图 22-28 所示的两个状态**。
**上一节已经编写了 Light 类及其 IL 接口的代码，现在新增一个冰箱（Refri）类的步骤如下**
![[Pasted image 20240629162354.png]]
![[Pasted image 20240629162430.png]]
![[Pasted image 20240629162416.png]]

![[Pasted image 20240629161301.png]]

### 13.3.3 举例: 以银行账户 (Account) 的状态为例
当个体状态改变时, 所引发的行为可分为两大类:
#### 13.3.3.1 立即行为
银行账户个体处于透支状态时, 立即输出信息提醒用户——您已向银行贷款了。如图 22-30 所示。  
![[Pasted image 20240629162759.png]]
用户**提款时**, 如果 acc 个体**处于透支状态, 就立即调用 message () 函数显示出信息, 告知用户共欠银行多少钱**。如果处于正常状态, 就不会输出信息。必要时, 可明确记载 Account  的个体状态。
请你看看如何以 OOPC 来实现这个状态图:


![[Pasted image 20240629164608.png]]
![[Pasted image 20240629165141.png]]
**state 变量记载着个体处于哪个状态**
每回提款时, 若 acc 个体处于透支状态, 就会有特殊动作——调用 message () 函数。所以 message () 是透支状态的行动


**另一种常见情形是: 只有当 acc 个体从正常状态转移到透支状态时, 才进行 message () 动作**, 如图 22-31所示
![[Pasted image 20240629163117.png]]
原来已处于透支状态, 取款后仍处于透支状态, 但并不调用 message () 函数。
![[Pasted image 20240629165159.png]]

此程序输出 Over (-50)。

 **一般银行账户大多不允许透支, 此时 acc 个体短暂处于透支状态, 送出信息后, 由于未改变金额, 所以立即返回正常状态**。此情形, 用 OOPC 实现如下:
....

**提款时, 当欲取金额大于余款时, acc 个体送出信息, 但未改变金额。**
#### 13.3.3.2 后续行为
在上节中银行账户的状态随金额大小而改变, 一旦转移到透支状态, 立即产生特殊行为——执行 message ()
另一种常见情形是: 状态转移时并不立即产生特殊行为, **但却影响后续的行为。例如, 到银行取款时, 常须输入密码, 如果密码正确, 就打开账户**
因此, 账户可分为两个状态
![[Pasted image 20240629170139.png]]
当账户处于 Open 状态, 且接到您的信息——取 200 元时, 账户就给您 200 元钞票

![[Pasted image 20240701093123.png]]
![[Pasted image 20240701093149.png]]

**信息 open (), 其行为会影响 acc 个体的 withdraw () 行为。**


# 14 UML +oopc 的实际示例
# 15 UML+OOPC 实用示例之一
## 15.1 涂鸦程序示例说明
为了让代码精简易懂, 对此示例做了一些需求限制
只画线条, 提供黑、红、蓝、绿四种颜色给涂鸦者挑选;
不配底图, 也不配声音;
一边涂鸦, 一边录制涂鸦的过程;
可以重复播放所录制的涂鸦情境;
可将录制的内容存入 (save) 涂鸦文件里;
可从涂鸦文件内容加载 (load) 到计算机, 并重复播放。

在画线条的过程中, 线条的端点和时间会自动存放到内存里, 一直到按下休止符按钮为止, 你能随时重新播放 (按三角图标按钮
此外, 还可以将所绘的图案存入文件中 (
之后可随时从文件中读取 (按\<Load\>选项) 绘图内容, 并回放 (按三角图标按钮) 出来。

## 15.2 涂鸦系统分析与设计
### 15.2.1 用例图 
开发者经常利用|\<<include\>>或\<<extend\>>关系来表达系统特色, 创造对用户的吸引力。如图 23-6 所示, 将涂鸦程序的特色都凸显出来了。
![[Pasted image 20240702084533.png]]


**编写主要的用例叙述 (UCD) 如下**
UC: 涂鸦的 UCD
![[Pasted image 20240702084708.png]]

UC: 播放的 UCD
![[Pasted image 20240702084725.png]]
### 15.2.2 绘制类图 
**用例叙述里, 可发现数个主要的概念或术语,** 例如: 涂鸦内容、线条端点等, 以及 **Scribble 和 dwPoint 两个主要类**
其中 Scribble 对象代表一张涂鸦, dwPoint 对象代表轨迹 (由一连串的小线段所构成) 上小线段的端点。**再加上负责图形显示的 Painter 类**, 就形成了如图 23-7 所示的类图。
,dwPoint 类 m_x 和 m_y 记载轨迹上小线段的端点坐标, m_type 代表提笔中 (即不画出移动轨迹) 或下笔中 (即会画出移动轨迹)。m_color 代表目前所挑选的颜色。m_timeSpan 代表从第一点到此点的时间差距
Scribble 类会定义一个 poList 串行来储存一系列的端点。
![[Pasted image 20240702085147.png]]
### 15.2.3 绘制 Scribble 状态图 
Scribble 类也扮演着控制的角色, **如果觉得 Scribble 的行为有些复杂, 可考虑绘制它的 SysML 的状态图**
![[Pasted image 20240702085714.png]]
**当进入状态 1, 接到 EV_begin 事件时, 就会清除掉 (clear) 原有的涂鸦动作, 并进入状态1;**
在状态 1 若**接到 EV_re_draw 事件, 就执行回放  (re_draw) 的活动**, 进入状态 2;
在状态 2 若接到 EV_begin 事件, 就会清除掉 (clear) 原有的涂鸦动作, 并返回状态1
### 15.2.4 绘制序列图  
**序列图是用例图与类图相遇的地方, 其表达如何将 Scribble、dwPoint 等对象组合出各用例图里的各项服务。**
用例图里, 可看到**两个主要用例涂鸦和播放。现在就来看看如何用序列图表达这两个用例。**
### 15.2.5 用例: 涂鸦
由于页面宽度的限制, 可将之切分为两个序列图 



![[Pasted image 20240702091355.png]]
从图 23-9 中可以看出, Scribble 自己不做画图的动作, 而委托 Painter 来做。Scribble 将 poList 串行参数传给 Painter 并且请 Painter 将串行里的每一个 dwPoint 绘至屏幕画面上
从图 23-10 中可以看出, Scribble 亲自与 FileSystem 沟通以保存 (save) 及载入 (load
![[Pasted image 20240702091432.png]]

### 15.2.6 用例 : 播放
只是**复用前面序列图 (即图 23-7 和图 23-8) 已经描述过的 load ()、re_draw () 等功能而已。**
![[Pasted image 20240702091609.png]]
## 15.3 实现 
这里展现的是在上一节里, 类图 (即图 23-7) 包含的三个主要类: Scribble、dwPoint 和 Painter, 其分别对应到 scribble. cpp、dwPoint. cpp 和 painter. cpp 代码文件。
### 15.3.1 定义 dwPoint. h 头文件
### 15.3.2 “定义 painter. h 头文件”
### 15.3.3 定义 llist. h 头文件 
### 15.3.4 定义 scribble. h 头文件  

# 16 UML+OOPC 实用示例之二
## 16.1 认识“录音”概念和技术
### 16.1.1 认识 PCM 规格
本章是假设你想自己写个录音程序, 通过麦克风来录制自己的声音, 并且存入. wav 类型的声音文件

在编写录音程序方面, Windows 提供了完整的 API 函数可供你去调用

通常以 8、11、16、22 kHz 取样后再依据 PCM (PulseCodeModulation) 音轨来进行编码、数字化之后就成为. wav 格式的音频文件
音波本质上可以分解成为数个正弦波形及其频率的整数倍数, 所以 PCM 能依固定的周期而进行取样

例如, 若采取 11.025 kHz, stereo, 8-bit 模型, 并且使用双声道来录音的话, 则理清下述的数据关系, 对于设计及编写录音程序会有很大帮助。
每次取样8bit=1Byte;
每秒钟取样11025 (即11.025×1000) 次;
录音声道数=2;
可算出每秒钟将录制 (11025×1×2) Bytes 的音频数据;
若录制 N 秒,共需要 (11025×2×N) Bytes 的内存空间来存放。
### 16.1.2 设置录音格式 
在你的 VB. NET 录音程序里, 只需要**将上述的参数赋值 (assign) 给如下的 format 结构**里, 然后调用 Win 32 API 就可以了

![[Pasted image 20240702093653.png]]
Windows 依据 format 里的信息进行音频的采样, 其过程中会**不断地把采集到的音频数据存入缓冲区 (buffer) 里,** 
**而你的程序必须及时给予足够的缓冲区, 或者即刻从缓冲区取出数据。**
### 16.1.3 设置缓冲区格式 
**你的程序必须安排缓冲区, 并决定其大小, 其定义的格式如下。**
缓冲区格式的内容:
![[Pasted image 20240702093816.png]]
**设定好缓冲区之后, 你的程序就可以从缓冲区不断取出音频数据。**
**取得数据后, 可以将它存入文件里**。因此, 你必须知道. wav 文件的格式, 才能准确地保存好文件。
### 16.1.4 将音频数据写入. wav 声音文件 
在. wav 的音频文件里, 开头 44 Bytes 必须记载我们采取的是 8 bits 还是 16 bits, 也必须记载我们选择几声道方式录音
这 44 Bytes 长的区域通称为. wav 音频文件的文件头 (Header), 继文件头之后 (即从 45 Bytes 起) 到文件尾才储存所录的音频数据
![[Pasted image 20240702094307.png]]
### 16.1.5 使用 Win 32 所提供的录音 API
在录音方面, Win 32 提供如下的常用 API 函数:
![[Pasted image 20240702094326.png]]
## 16.2 单纯“录音”的示例分析
单纯录音 10 秒钟的例子
### 16.2.1 绘制系统用例图 
,因为**操作需求简单, 只是系统执行过程较复杂**, 所以系统用例图并不复杂,


图 24-1 显示出用户需求是简单的: 只是想要录音 10 秒钟, 然后存入. wav 格式的文件。
![[Pasted image 20240702094526.png]]
### 16.2.2 绘制类图 
**此为典型的软硬件集成设计的系统, 其中包括三种不同类型的模块。**
硬件设备例如麦克风、硬盘等。
驱动软件 (driver) 或服务提供者 (serviceprovider)
例如录音的 MMSYSTEM (即 winmm. lib) 程序。
主控软件 (或称为应用软件)   例如声音录制的 OOPC 程序。

**为了明确表达上述三者之间的密切关系, 先绘制一个软硬集成设计的类图**
![[Pasted image 20240702094848.png]]
接着,**把焦点放在软件 (包括驱动程序和应用程序) 部分, 并且将驱动程序视为应用程序与硬件设备之间的接口, 就能绘制以软件为主的类图**
![[Pasted image 20240702094944.png]]
### 16.2.3 绘制序列图 
藉由序列图来表达上述 Use Case 幕后的系统流程
![[Pasted image 20240702095530.png]]


# 17 定义类 
## 17.1 类设计之实例说明
### 17.1.1 分析和设计 
街道上的红绿灯有 3 种状态: 亮红灯、亮绿灯和亮黄灯。

图 29-1 说明, 可以**设计一个 TLight 软件对象来控制 (通过 P 0 端口) 红绿灯的硬件组件**。**为了产生 TLight 对象, 就得先定义一个 TLight 类,** 
**但是这个类该包含哪些数据和函数呢?** 由于红绿灯对象的状态很明确, 我们就可以从**状态分析开始**
![[Pasted image 20240701133724.png]]
当处于 RED 状态时，会做两件事情：
1. TurnOnRedLight: 改变 P 0 端口的 P 0^0、P 0^1 和 P 0^2 针脚的值, 使红灯亮 (也关闭绿灯和黄灯)。
2. 2. Wait: 让红灯亮着持续一段时间。

于是，**得知 TLight 对象的行为了，将它们对应到类的函数，就可绘出 UML 类图**
![[Pasted image 20240701133943.png]]
### 17.1.2 以 Keil C 实现红绿灯控制程序
编写 TLight 类了
![[Pasted image 20240701134048.png]]
类定义好了，就开始编写函数的实现内容：
![[Pasted image 20240701134105.png]]
![[Pasted image 20240701134113.png]]
这个 FUNCTION_SETTING (run, run) 宏的用意是：让类定义（. h 文件）的函数名称能够与实现的函数名称不同
这是创造. c 文件自由替换的空间，是实现接口的重要基础

如何编写主程序：
![[Pasted image 20240701134205.png]]

### 17.1.3 红绿灯类的另一种写法
如果对象可以明显地区分出它的不同状态，就能有效降低其复杂性
现将上一小节的 TLight 类改写如下:
![[Pasted image 20240701134326.png]]
一般而言，如果像 goto_R ()、goto_Y () 和 goto_G () 等**内容并不复杂的函数，可以将它们合并为一个函数（如 perform () 函数）。**
![[Pasted image 20240701134530.png]]
![[Pasted image 20240701134541.png]]
![[Pasted image 20240701134557.png]]

一般而言前一种方法比较流行。

# 18 应用范例一
## 18.1 以 Toggle Light 电灯为例
## 18.2 以红绿灯控制系统为例
### 18.2.1 分析与设计  
TLight 对象来控制一个红绿灯组件的状态变化
一般的十字路口上, 通常需要同步控制两个红绿灯, 以便同时协调两边的车辆
同步控制一组 (含有两个或4个红绿灯对象) 组件 
![[Pasted image 20240701135600.png]]
这两个红绿灯组件的状态变化必须互相协调, 例如一个亮绿灯时, 另一个必须亮红灯

此时可以设计两个 TLight 对象来分别控制一个红绿灯硬件组件, 并且设计一个 **CTRL 对象来指挥这两个 TLight 对象, 以构成层级式控制的架构,** 如图 30-8所示。

![[Pasted image 20240701135650.png]]
#### 18.2.1.1 初步的类规划。
从图 30-8 可知道需要 3 个对象。那么, 总共需要几个类呢? 这要视两个 TLight 对象行为上的差异而定, 如果差别很大, 宜设计出两个类, 各产生一个对象。反之, 如果两个 TLight 对象的行为很相似, 可只设计一个 TLight 类即可

图 30-8 是一张草稿图, 并非标准的 UML 图, 但它可协助我们思考如何设计如图 30-9 的类图。
![[Pasted image 20240701135932.png]]
**知道共需要几个类了, 但内部的数据和函数都还欠缺, 我们将借助对象状态分析来寻找类的函数。**
#### 18.2.1.2 分析对象的状态变化
**要如何决定类应含有哪些函数呢? 在控制系统中, 最常见的办法是观察其状态的变化**
##### 18.2.1.2.1 针对 LightR 和 LightL 对象
LightR 和 LightL 类的对象行为是一致的, 其状态如图 30-10所示。
![[Pasted image 20240701140149.png]]
其中, ev_Y 和 ev_RG 是两个**事件, 也可以将它们视为信息**。这是 CTRL 对象发出的指挥信息, 来**驱动** LightR 和 LightL **对象的状态变化**
##### 18.2.1.2.2 针对 CTRL 对象
如果十字路口的两方车道每隔145秒钟交换一次通断状态, 而且黄灯停留时间为15秒的话, 则 CTRL 就只有两种状态了, 称之为 S 状态与 L 状态。 
![[Pasted image 20240701140540.png]]
一开始就进入 L 状态, 此刻有一组红绿灯处于 GREEN 状态, 而另一组处于 RED 状态。
停留 130 秒之后发出 ev_Y 信息, 表示 GREEN 状态维持 130秒。
从图 30-10 可看出, 另一组处于 RED 状态的红绿灯并不会接受 ev_Y 信息, 所以 RED 状态会维持145秒。
#### 18.2.1.3 绘制完整的类图
**从图 30-7 的状态行为可协助我们思考 CTRL 类该有哪些函数。**
完整的类图如图  30-12 所示。
![[Pasted image 20240701141310.png]]
图中的菱形表示 CTRL 对象包含一个 LightR 对象和一个 LightL 对象。而黑色菱形表示该两个对象是由 CTRL 对象所产生的。
#### 18.2.1.4 设计对象间的互动细节
这些类能用来产生**对象, 它们会互相沟通互动, 现以 UML 的对象合作图来表示**, 如图 30-13 所示。
![[Pasted image 20240701141506.png]]
这使用了信息传递的沟通方式。
### 18.2.2 以 LW_OOPC 实现
#### 18.2.2.1 Step-1 编写 LightR 类别
![[Pasted image 20240701141842.png]]
![[Pasted image 20240701141910.png]]
**就 LightR 而言, ev_Y 和 ev_RG 是两个外来的事件或信息。LightR 对象会调用 goto_R () 等函数来响应这些外来的事件。**

#### 18.2.2.2 编写 LightL 类。 
#### 18.2.2.3 编写 CTRL 类
![[Pasted image 20240701142050.png]]
![[Pasted image 20240701142108.png]]
**CTRL 对象与 LightR、LightL 对象的状态变化是不一样的,但是会有同步 (Synchronize) 的现象, CTRL 的状态变化会带动 LightR、LightL 对象的状态变化。**
#### 18.2.2.4 编写 main () 函数。 
![[Pasted image 20240701142140.png]]


此程序执行时, 输出如图 30-14 所示。 

![[Pasted image 20240701142200.png]]
**表示 LightR 处于 YELLOW 状态 (P0^1为1), 而 LightL 处于 RED 状态 (P1^0为1)**。
停留一段时间之后, 发出 ev_RG 信息给 LightR 和 LightL 对象, 如图30-15所示。
![[Pasted image 20240701142246.png]]
此时输出如图 30-16 所示。 
![[Pasted image 20240701142309.png]]
这表示**LightR 由 YELLOW 状态转为 RED 状态**(P0^0为1)。而 LightL 由 RED 状态转为 GREEN 状态 (P1^2为1)
# 19 应用范例 2  

## 19.1 界面用途: 从硬件的 PnP 谈起 
由于硬件结构上处处是接口, 如果能将软件对象的接口与硬件组件接口同时考虑, 对于嵌入式系统则会有极大的帮助
因为如此的话, 软、硬件不仅可以同步设计, 而且可以互相替换, 软、硬件都能达到便捷 PnP 的美好境界。
### 19.1.1 硬件端口 (Port) 就是接口 
硬件的端口 (Port) 其实就是接口
![[Pasted image 20240701153143.png]]
**有了像 P0、P1等接口, 一个8051单片机就可以与 LED 显示器分合自如, 也即俗称的 PnP (Plug and Play)。**
### 19.1.2 软件接口 
在 8051 硬件组件里含有嵌入式软件
![[Pasted image 20240701153252.png]]
**如果软件也有端口可连接硬件端口时**
![[Pasted image 20240701153258.png]]
LED 对象的 data_port 连接到8051硬件组件 P0时, 其关系就相当于硬件组件 P0连接到 LED 显示器。基于这个对应关系可得到下面的效果:
	你已经理解: 8051 硬件组件很容易将 LED 显示器 PnP 掉。
	同理可知道: LED 软件对象很容易将 8051 硬件组件 PnP 掉。

**当我们从 8051 硬件组件 PnP 掉 LED 显示器时, 意味着: 8051 硬件组件 (如同车体) 的重用 (Reuse), 而不是 LED 显示器**
许多人误解为接口设计是在追求小对象的重用, 其实是不对的。想一想, 当您的台灯灯管坏了, 把坏灯管拔掉, 换上新灯管, 结果是: 重用了整个台灯, 而不是重用灯管
## 19.2 LED 显示器控制设计 (1)
### 19.2.1 分析与设计 
**逐步实现图 31-3 的软件接口**
从图 31-4 可知 LED 类需要定义两个端口。
![[Pasted image 20240701153849.png]]
定义 LED 类如下:
![[Pasted image 20240701154002.png]]
接下来, 设计两个 channel (可呈现为对象或函数)。
![[Pasted image 20240701154010.png]]


其中, channel_0 已经连接到 P 0, channel_1 已经连接到 P1
现在就**拿 channel_0 连接到 LED 对象的 data_port**
其指令写法为:
![[Pasted image 20240701154825.png]]

**于是就将 LED 对象与 8051 硬件组件连接起来了**
![[Pasted image 20240701155003.png]]
### 19.2.2 staruml 分析
图 31-4 和图 31-5 都不是 UML 标准图示, 现以 StarUML 工具绘制 UML 的组合结构图 
![[Pasted image 20240701155120.png]]
### 19.2.3 实现 
首先编写 **LED 类的定义**:
![[Pasted image 20240701155203.png]]
定义了**data_port 和 scan_port 两个函数来实现 LED 对象对外的端口**。
接着**定义一般软件内部函数 run ()** 来处理对象内部的事务
![[Pasted image 20240701155323.png]]
![[Pasted image 20240701155532.png]]
执行 run () 函数里的指令:
![[Pasted image 20240701155633.png]]
就把 channel_0 () 和 channel_1 () 连接到 LED 对象的两个端口上。


![[Pasted image 20240701155655.png]]
LED 显示器的第 1 位数出现'0'。


![[Pasted image 20240701155720.png]]
前面图31-6是 UML 的组合结构图,善于呈现空间感,并且凸显软件内在 (SoftwareInside) 的角色。

**一旦软、硬件接口厘清了, 就可使用 UML 类图 (ClassDiagram) 来呈现详细的软件对象接口**, 如图31-7所示。
![[Pasted image 20240701155809.png]]
该图包含软件和硬件组件, 并且采用 UML 的标签\<<HW\>>和\<<SW\>>来**区别软件对象与硬件组件**。其中,
+P 0 和+main ()**的'+'号表示这是对外沟通的端口 (或函数)。**
 # data_port 的 **'#'号表示在自己 (Local) 的. C 文件里并未实现 (Implement) 这个函数。**
~channel_0 **的'~'号表示它实现在自己的. C 文件里, 它将连接到别的对象的'#'型** (如 # scan_port ) 函数, **成为该'#'型函数的实现函数。**
图中, 比较有趣的是**如何看待 P 0、P 1 等硬件的端**口。我们会采取两种观点:
	1. 外部观点 (OutsideInView): 我们把 **LED 显示器连接到 P 0 和 P 1 上,** 就如同把轮胎装到轮盘上。此时我们通过 P 0 及 P 1 来使用 8051 组件内部的实现, 所以**嵌入于 8051 的 LED 软件对象成为 P 0 及 P 1 的实现 (或称为实现)**。
	2.内部观点 (InsideOutView):**从软件往外看**, P**0和P1连接到LED显示器, 而软件LED对象通过P0和P1控制LED显示器, 此时LED显示器就成为P0和P1的实现了**。



## 19.3 LED 显示器控制设计 (2)
### 19.3.1 分析与设计
在上一节的图31-7**里, 设计了一个 LED 软件对象, 通过 P0和 P1来控制 LED 显示器**。如果**系统继续扩大, 例如 P3连接到蜂鸣器, 我们也会设计一个 Beep 软件对象来控制蜂鸣器**。
**像 LED、Beep 等对象增多了, 我们通常会额外规划一个控制对象, 担任指挥中心**。如图31-8所示。
![[Pasted image 20240701161746.png]]
CTRL (即 Controller 的简写) 担任了原来 main () 函数的工作, 因而简化了 main ()
此外, **CTRL 扮演软件主机板 (SoftwareMotherboard) 的角色**。**LED 对象和 Beep 对象就 PnP 到 CTRL 对象上**, 就如同一般 PC 计算机的 Keyboard、硬盘等连接到硬件主机板一般

于是呈现出标准的嵌入式软、硬件整合架构:
	 main () 启动 CTRL 对象开始运行。
	**CTRL** 依循复杂的逻辑规则, 有节奏 (可以使用硬件的 Timer 组件)**且井然有序地指挥  LED 和 Beep 等小对象运行**。  
	 **LED 和 Beep 等小对象通过 P0、P1 等接口而控制外部的 LED 显示器等硬件组件。**

### 19.3.2 实现范例 
![[Pasted image 20240701164105.png]]
定义了 data_port 和 scan_port **两个函数来实现 LED 对象对外的端口**
并声明一个 int 型态的数组: digits[]。如果 digits[]的内容为{3,5,7,1}, 就会在 LED 显示器出现这 4 个阿拉伯数字。
![[Pasted image 20240701181728.png]]
![[Pasted image 20240701164210.png]]
然后执行下述指令:
![[Pasted image 20240701181836.png]]
于是 LED 显示器的第 i 位数出现该阿拉伯数字。

再来编写 CTRL 类:
![[Pasted image 20240701182219.png]]
![[Pasted image 20240701182241.png]]
再通过 load_data () 函数来填入 digits[]的内容, 然后以 show_digits () 函数显示出来

编写主程序:
![[Pasted image 20240701182259.png]]
以上范例, 凸显了 CTRL 对象的重要性

## 19.4 LED 显示器控制设计 (3) 
**在上一个范例里, LED 对象的 data_port 和 scan_port 两个接口都是以函数形式出现的,**
其实它们还能**以对象指针形式**呈现。

意思是, 上一个例子, **将 channel_0 和 channel_1 定义为函数, 其实也能将它们定义为类**
![[Pasted image 20240701182919.png]]
**首先替 Channel_0 和 Channel_1 类定义其共同的接口 IPort。**
![[Pasted image 20240701183002.png]]
![[Pasted image 20240701190006.png]]
![[Pasted image 20240701190138.png]]
scan_port 和 data_port 变成为 IPort\*类型的指针，指向 Channel_0 或 Channel_1 类的对象。
![[Pasted image 20240701190613.png]]
![[Pasted image 20240701190622.png]]
然后执行下述指令:
![[Pasted image 20240701190735.png]]
于是 LED 显示器的第 i 位数出现该阿拉伯数字。 


![[Pasted image 20240701190800.png]]
![[Pasted image 20240701190813.png]]

![[Pasted image 20240701190821.png]]





此范例凸显了 LED 等软件对象接口的多样化, 可以呈现为函数, 也可呈现为对象指针

类设计的过程为:
Step-1 关注术语或词汇, 用它们来作类名称。
Step-2 把跟某一词汇有关的函数汇集起来。
Step-3 把函数共享的数据项汇集起来。

当你找到 LED 这个重要词汇, 就找到重要类了。这个类将产生对象来控制一个 4 位数 7 节 LED 显示灯。所以 LED 对象将有两个重要的函数:
送出 data_code 到硬件的 P0或其他 Port。
送出scan_code到硬件的P1或其他Port。
此外, 可能需要一个 load_data () 函数, 由外界给予有关的数据, 经过一些处理之后, 分别送出 data_code 和 scan_code。因此从行为层面找到了 LED 类的重要函数。此时, LED 对象的 data_port 和 scan_port 两个接口都以函数形式出现, 其实它们还能以对象指针形式呈现


控制是一项任务, 也是一个重要的词汇, 于是设计一个 CTRL (即 Controller 名词的简写) 来担任这项任务。
于是 CTRL (软件) 对象指挥 LED (软件) 对象, 然后 LED 对象再送出信号到硬件的 P0、P1等硬件接口来控制像4位数7节 LED 显示灯等对象。
