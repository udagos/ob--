# 微处理器体系架构
冯诺伊曼
哈佛：指令数据分开
## ARM 微处理器体系架构

“Cortex-M4处理器架构”
![[Pasted image 20240925160750.png]]
Cortex-M 4 处理器的可编程模式主要包括核心寄存器、堆栈和异常中断所涉及的寄存器
### 可编程模式
通用寄存器 R 0~R 12
堆栈指针SP
链接寄存器 LR
程序计数器
### ==特殊寄存器==
#### 程序状态寄存器 PSR 
Program Status Register

内部又被分成 3 个子状态寄存
应用状态寄存器 PSR (Application Program Status Register, APSR)>  
中断状态寄存器 (Interrupt Program Status Register, IPSR)和
执行状态寄存器 (Execution  Program Status Register, EPSR)

只有在特权模式下, 通过 MRS 和 MSR 指令, 才允许访问这 3 个寄存器。
#### 中断屏蔽寄存器
PRIMASK、FAULTMASK 和 BASEPRI 寄存器用于控制异常的使能和禁用
特权级下, 才允许访问这 3 个寄存器。 
#### 控制寄存器
“定义处理器特权级别和用于选择堆栈指针”

## 中断机制
![[Pasted image 20240925161729.png]]
**NVIC 支持多达 240 个可屏蔽中断 (IRQ)、一个 NML 一个 SysTick 定时器中断和一些异常中断**。大多数 **IRQ 由定时器、I/O 端口和通信接口等外设产生**。NMI 通常由“看门狗”定时器和掉电检测器等外设产生。


**在使用任何中断之前, 都需要完成以下工作:** 
(1) 设置所需的中断优先级 (可选);  
(2) 使能外设中可以触发中断的中断产生控制;  
(3) 使能NVIC中的中断。

编写程序时，相应ISR名称与向量表中的名称一致，这样链接器才能将该ISR入口地址放入向量表的正确位置。
**对于异常/中断处理的过程**，包括接收异常/中断请求、进入异常/中断服务、执行异常/中断服务和异常/中断返回操作4部分。
# 总线
数据、d
地址、
控制、i
扩展总线 s

##  总线结构与协议
“**大多数总线协议是基于四周期握手协议构建的**”
“握手时使用一对专用于握手的线路：enq 与 ack,其他线路用于握手期间的数据传输。**握手的每步都由 enq 或 ack 线路上的电平转换来标识**。”
![[Pasted image 20240925162844.png]]
(1)设备 1 的 enq 信号升高发出查询信号, 告诉设备 2 做好接收数据的准备。 
(2)当设备 2 准备好接收时, 设备 2 的 ack 信号升高发出确认信号。这时, 设备 1 和设备 2 就可以发送或接收信号。
(3) 一旦数据传输完毕, 设备 2 就降低 ack 信号, 表示已经接收完数据。
(4)当设备 1 看到设备 2 的 ack 信号变为低电平时, 设备 1 也将其 enq 信号降低, 变为低电平, 此次握手结束。

**使用握手信号可以完成对数据的读写操作**, 其时序图如图 3.5 所示。
![[Pasted image 20240925162947.png]]
**猝发传输**
## dma
“DMA 控制器与 CPU **分时使用内存**的方式通常采用以下3种方法:”
(1)停止 CPU 访问内存;  (2)周期挪用;  (3) DMA 与 CPU 交替访问内存。

“个完整的 DMA 传输过程必须经过 DMA 请求、DMA 响应、DMA 传输和 DMA 结束4个步骤”

“CPU 和 DMA 控制器各自拥有访问内存地址寄存器、数据寄存器和读/写控制寄存器”

### f 4
“该系列芯片中 DMA 是一个 AMBA AHB 模块，有3个 AHB 端口，**其中一个从设备接口用于 DMA 编程，两个主设备端口用于外设和存储器访问**”

该芯片拥有两个通用的 **8 个数据流**双端口 DMA 控制器, 所以一共可以支持 16 个数据流, **每个数据流有多达 8 个可选通道** (即硬件请求)
![[Pasted image 20240925163733.png]]
(l) DMA 数据流/通道:在一个数据流中同一时间只能有一个通道 (请求)被激活, 同一外设的请求不会有多个激活的 DMA 数据流服务
(2)数据流优先级: 每个 DMA 端口都有一个仲裁器处理 DMA 数据流的优先级
(9) FIFO 模式: 每个数据流有独立的四字 FIFO, 用于临时存储传输数据, 软件可以将其阈值编程为 1/4、1/2、3/4 或满以定义 DMA 存储器端口请求时间。**如果被关闭，就是直接模式。如果被使能，则可以使用数据打包或解包、猝发模式**。
### DMA的设置
**如果要设置一个DMA数据流x (这里x为数据流号码)，需要执行下列程序**。
### DMA请求的仲裁
STM32F4芯片中的DMA控制器嵌入了一个仲裁器,这个仲裁器基于两个AHB主设备接口 (可以是存储器或者外设接口) 上的优先级来管理8个DMA数据流请求

如果有多个DMA请求, 那么DMA在模块内部执行活动的请求之间的仲裁。**如图3.25所示为由DMA数据流请求1和请求2同时触发的两个循环DMA请求的服务情况**

在下一个AHB时钟周期, DMA仲裁器检查活动的到来的请求并将访问权交给请求1数据流, 因为它具有最高的优先权。下一仲裁周期发生在请求1数据流的最后一个数据周期。在仲裁期间, 请求1被掩膜, 仲裁器只看到请求2是活动的, 从而将访问权保留给请求2, 以此类推。
![[Pasted image 20241014113412.png]]
## ARM CPU 的总线结构
### ARM 总线结构
片上总线标准AMBA
![[Pasted image 20241014110635.png]]
DMA控制器在被编程时或者初始化时是从设备，但在DMA从系统读或者传输数据时必须是主设备
总线上存在多个主设备,  则需要仲裁器来决定如何控制各种主设备对总线的访问
#### AHB 总线
“字节和字的传输。 AHB 总线连接构成的系统由主设备、从设备和基础结构3部分组成”

“**基础结构则由仲裁器、主设备到从设备的多路器**、从设备到主设备的多路器、译码器、虚拟从设备(Dummy Slave)、虚拟主设备 (Dummy Master)组成”
![[Pasted image 20240925181743.png]]
#### apb
“**APB的总线架构不像AHB可以支持多个 主设备**，在**APB上唯一的主设备就是APB桥**”
#### axi
AXI 协议读采用两个通道来实现, 具体见图 3.29
![[Pasted image 20240925181951.png]]
“写操作采用3个独立的通道实现, 见图3.30”
![[Pasted image 20240925182010.png]]
AXI 总线基于 VALID/READY 的握手机制来进行数据传输, 主设备传输源端使用 VALID 表明地址/控制信号、数据是有效的, 从设备目的端使用 READY 表明自己能够接收信息。
读写的 5 个传输通道均使用 VALID/READY 信号对传输过程的地址、数据、控制信号进行握手。
AXI 总线使用双向握手机制, 传输仅仅发生在 VALID. READY  同时有效的时候。具体又分3种情况
### “多层总线矩阵结构”
![[Pasted image 20241014111704.png]]
“这样多的主、从设备自然应采用**多层**总线矩阵结构来支持，**保证并发访问和有效操作**

# 存储系统
![[Pasted image 20240925184658.png]]
“**内存映射图中，存储空间可以划分为以下几类**:”
(1) 程序代码访问区域 (CODE)。
(2) 数据交互区域 (SRAM) 
(3) 片内外围设备 (Peripherals)  
(4) 外部存储空间 (RAM) 或片外外设 (Devices) 
(5) 处理器内部控制 (NVIC) 和调试区域 (System/Vector)。
**其地址分配如表4.3所示**。
![[Pasted image 20240925184646.png]]
“**程序存储器、数据存储器、寄存器和I/O都统一在4GB存储空间线性组织**”
### 弹性静态存储器控制器


![[Pasted image 20240925184613.png]]
### 弹性存储器控制器
![[Pasted image 20240925184604.png]]
nor flash放代码（可执行）和nand flash放数据
sdram 缓存和sram内存
# 嵌入式输入/输出设备的接口
## I/O 接口
### I/O 接口组成
“，I/O 接口一般包括**数据缓存、逻辑控制和外设连接**3个基本的功”
![[Pasted image 20240925184843.png]]
数据缓存功能模块主要对应一组寄存器, 负责 I/O 交互过程的控制和记录等。数据缓存寄存器可根据存放数据类型的不同, 划分为**数据寄存器、控制寄存器和状态寄存器**。
	其中数据寄存器用来存放交互过程中需传输的数据, 
	控制寄存器用来存放外设、I/O 接口的控制命令 (如 I/O 接口启动、中断使能等), 
	状态寄存器则用来记录外设、I/O 接口的状态信息 (如中断状态、系统故障等)。

逻辑控制功能模块主要接收 CPU 传输过来的地址, 根据**地址进行外设选择**。

外设连接功能模块是 I/O 接口与外设的交互接口。**同一个 I/O 控制器可与多个外设连接, 因此需要设置一个或多个外设连接逻辑功能来实现外设与 CPU 的连接**。
### I/O 接口的数据传输
“**I/O 接口的数据传输方式一般包括程序查询方式、中断方式和直接访存方式3种**。”

“程序查询方式是CPU负责查询外设的**状态寄存器**，判断外设的数据是否就绪”

“中断方式是当外设数据就绪时，外设向CPU发出中断服务请求信号”

“直接访存方式DMA在外设与内存之间建立直接的数据传输通道，数据传输过程中不 需要CPU的参与”
## gpio
“I/O 接口的功能一般固定，但是 GPK）可通过寄存器配置和编程实现功能复用。”
![[Pasted image 20240925185232.png]]
### GPIO 引脚复用
GPIOx. AFRL 寄存器用来选择 AF 0〜AF 7 功能模块, GPIOx. AFRH 用来选择 AF 8-AF 15功能模块
![[Pasted image 20240925185401.png]]
### GPIO 配置
“GPIO 引脚的配置需完成输入配置、输出配置和复用配置等。”
![[Pasted image 20241014152451.png]]
	![[Pasted image 20241014152458.png]]
	![[Pasted image 20241014152508.png]]
## 外部中断/事件
外部中断/事件是实现**外设与微处理器之间通信的主要方式**之一。**微处理器中包含一个外部中断/事件控制器** (External Interrupt/Event Controller, EXTI), 用于管理微处理器中的外部中断/事件线。
每个中断/事件线都对应一个边沿检测器, 实现输入信号的上升沿或下降沿检测。EXTI 可以对每个中断/事件线进行单独配置, 配置中断或者事件类别以及触发时间的属性。EXTI 还包含一个挂起寄存器, 用于记录每个中断/事件线的状态。
### EXTI 结构和外部中断/事件响应过程
![[Pasted image 20240925185709.png]]
向NVIC发出一个中断请求

从外部激励信号来看，中断和事件的产生源可以一样。**中断和事件的区别在于**，中断需要CPU参与后续的中断响应，包括上下文切换、执行中断服务程序、恢复现场并返回等；但是事件仅触发脉冲发生器产生一个单脉冲，进而由硬件自动完成事件的响应，比如DMA操作、AD转换等。
### ==外部中断/事件的配置==
首先, 选择中断/事件的边沿触发信号, 设置两个触发选择寄存器, 
同时在中断/事件屏蔽寄存器相应位写1, 允许中断/事件请求。
当外部中断/事件线发生了配置的边沿触发信号时, 将产生一个中断/事件请求, 对应挂起位将被置为1。
在挂起寄存器对应位写1将清除该中断。通过在软件中断/事件寄存器写1, 也可以由软件产生中断/事件请求。

![[Pasted image 20241014153200.png]]
基于以上配置过程, 使用外部中断/事件的程序编写步骤如下。
(1) 初始化GPIO口为输入。
(2) 开启GPIO口复用时钟SYSCFG, 设置GPIO口与中断线的映射关系。
(3) 初始化线上中断, 设置触发条件等。
(4) 配置NVIC中断分组, 并使能中断。
(5) 编写中断服务函数。
## 通讯接口
### USART
典型应用中, PC通过USART与嵌入式系统进行数据交换和通信
![[Pasted image 20241014153423.png]]
![[Pasted image 20241014153436.png]]


![[Pasted image 20240925191925.png]]
**USART的数据发送和接收过程**
....
![[Pasted image 20240925191959.png]]
### spi
主要用于微处理器与外设之间的串行通信, 比如Fla^h、RAM、A/D  转换器、数字信号处理器和网络控制器等外设

**一个SPI总线可连接多个主机和多个从机，但是在同一个时刻只允许由一个主机来操作总线**

SPI 在不同时钟相位下的数据传输时序如图 5.16 所示。
![[Pasted image 20240925192058.png]]

其中可通过SPI_CR1寄存器的配置来设置SPI的工作模式
![[Pasted image 20240925192117.png]]
**SPI的从模式配置与传输**
...
SPI主模式配置与传输
### i2c
I2C总线通过硬件设置的方法给总线中的所有设备设置了一个唯一的地址, 并通过软件寻址完全避免了设备的片选线寻址方法

只能有一个控制设备获得总线控制权而成为主机
![[Pasted image 20241014154230.png]]

如图5.22所示为主机发送数据到从机的通信过程。如图5.23所示为主机读取从机数据的通信过程。如图5.24所示为主机分别向从机发送和读取数据的复合通信过程。
这个应该是没有控制器的时序
# 嵌入式最小系统构建
### 时钟系统
HSI (High Speed Internal Clock, 高速内部时钟)主要为驱动系统时钟
LSI (Low Speed Internal Clock>低速内部时钟)可作为低功耗时钟源
HSE (High Speed External Clock, 高速外部时钟)主要为驱动系统时钟 
LSE (Low Speed External Clock, 低速外部时钟)用于驱动 RTC 时钟

在使用部分**特殊外设 (如以太网、USB 等**)时, STM 32 微控制器**需要外部 PHY (Physical, O-SI 模型中的物理层)提供时钟来保证合适的时钟频率**, 如图 7.4 所示的标注 3 处为外部 PHY 提供的以太网 MAC 时钟与 USB 时钟。
### 复位系统
### 外部接口
常用外部接口包括通用输入/输出接口、串行通信接口、定时器接口、模数转换器接口、数模转换器接口和JTAG调试接口

定时器接口：“通用定时器在基本定时器的基础上扩展而来，增加了输入捕获与输出比较等功能”“高级定时器是在通用定时器的基础上扩展而来，增加了可编程死区互补输出、重复计数器、带刹车（断路）等功能”
# 实时操作
## 进程的通信方式
### 共享内存
![[Pasted image 20240925191141.png]]

若多进程要进行写操作, 则应注意互斥保护。
如图 8.8 所示的基于共享内存的通信, CPU 与 I/O 设备之间通过总线, 使用共享内存实现通信。若 CPU 想要向 I/O 设备发送数据, 它就向共享内存中写入数, 然后 I/O 设备从该内存中读取数据。

**在共享内存中, 通常需要设置一个标志位, 标志数据是否准备好。在一方需要写数据、另一方需要读数据, 这样是没问题的。但若是双方都是写数据, 则可能出现数据竞争的情况。**
**为了避免这种情况的出现**,CPU总线通过支持测试置位 (test-and-set) **原子操作**来实现。测试置位操作首先对数值进行检测, **如果该值通过了检测则执行设置**。在Cortex-M4  中使用SWP指令实现测试置位原子操作。
### 消息传递
消息邮箱是系统预留一块公共内存区域, 作为“邮箱”, 由统一的调度器来控制各个任务的数据和信息的传递
多个任务可以访问邮箱, 但是只有一个任务能从邮箱中读取出数据或控制信息。数据或控制信息一旦从邮箱读取出来, 邮箱就置为空闲状态。同时邮箱还会设置超时控制来防止死锁。
### 进程同步与互斥 
