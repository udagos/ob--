---
same:
  - "[[../note/c现代编程总结|c现代编程总结]]"
prev:
  - "[[../inbox/bookcotex|bookcotex]]"
  - "[[book对象及设计模式]]"
obsidian-note-status:
  - colorful:5
next:
  - "[[c现代编程续 设计模式]]"
---
没有接口抽象
# c 语言与面向对象

### c 与模块化
> **c 99 标准**

![[Pasted image 20240625140204.png]]
> ==可见一个函数占用一个栈的数据==


像这样在头文件中声明对外公开的函数和变量，对其他无需对外公开的变量和函数指定 static 修饰符，是 C 开发中常用的模块化方法这样可以提高栈实现的独立性, 使其可以与其他程序结合运行
![[Pasted image 20240625135125.png]]
![[Pasted image 20240625135134.png]]

### 使用结构体将数据结构与代码块分离 
上一节的代码清单 3-3 中还有其他问题该实现方法中仅有 1 个栈，如果有**多个栈的时候怎么做才好呢**？
**一种方法是编写两个名字不同，功能相同的函数**
![[Pasted image 20240625111201.png]]
**会出现多段处理完全相同的代码**


将栈的==**实现中所需的数据整合**==在一起, 并==**允许内部持有多个栈**==
![[Pasted image 20240625111320.png]]
![[Pasted image 20240625111327.png]]

![[Pasted image 20240625111358.png]]
![[Pasted image 20240625111408.png]]

还可以**使用名为 newstack 的宏方便地将结构体初始化**
![[Pasted image 20240625111437.png]]

> **extern "C**

> **C 99 标准中的可变长数组**
	![[Pasted image 20240625134726.png]]
	实际可以
	![[Pasted image 20240625134825.png]]


代码清单 3-5、代码清单 3-6 中通过将栈的数据作为结构体分离出来, 然后作为参数传递, 可以方便地构造出多个栈 
![[Pasted image 20240625135548.png]]



> ==得出结论使用结构体的作用==
### 使用 c 进行面向对象编程
#### 带检查功能的栈 
请各位试着考虑一下如果**要将栈中保存的值限定在一定范围内应该怎么做呢**

**一个办法是再编写一个函数代替 push 函数**
![[Pasted image 20240625112201.png]]
但是**这种实现方法传递的参数太多，而且每次 push 的时候都要传递允许的范围**，非常麻烦

**改进一下，在生成栈的时候将允许范围传递过去**
![[Pasted image 20240625112304.png]]
![[Pasted image 20240625112311.png]]
![[Pasted image 20240625112411.png]]
代码清单 3-7 中的结构体里保存了是否需要进行范围检查 (needRangeCheck)的信息以及值的有效范围 (min、max), 并且定义了新的宏，用来生成带有范围检查功能的栈
代码清单 3-8 在 push 函数中增加了范围检查的逻辑
#### 带有范围检查功能的栈的问题点
上面的代码中仍然有以下**==问题==**
- 要生成的是不带范围检查功能的栈. 栈内也需要保存 needRangeCheck、min. max 等**多余的结构体成员**，浪费了内存
- 在栈内增加其他校验功能. 就必须在结**构体内再增加其他成员。push 函数也会因这些功能变得臃肿**

这次增加的结构体成员 needRangeCheck、min, max 真的必须和结构体的其他成
员放在相同的位置吗？这些成员只在 isRangeOk 函数中被使用，将它们作为结构体成员让所有函数都可以看到是不合适的，也就是说存在**==作用域污染问题==**

**首先我们要把这些成员分离出去** (代码清单 3-9、代码清单 3-10)
![[Pasted image 20240625113709.png]]
![[Pasted image 20240625113718.png]]
以上代码==将**范围检查所必需的数据移动到**结构体 Range== 中，并在 isRangeOk 函数中使用 Range 进行判断。
这样就可以将保存栈数据的 Stack 与保存范围检查数据的 Range 分离开来了

>  ==**拓展 push 的功能为了不污染，将成员独立成型的结构体，形成结构体的嵌套**==
#### 检查功能的通用化
例如, 如果**要求每次 push 到栈中的值都必须比上次的值 (最开始是 0 以上)大** (虽然一般不会行这种要求，这里只是以此为例讨论增加其他校验时的情况), 目前的设计还无法实现该需求，但是只要将==**输入值检查变得更通用化**==就能解决这个问题

首先**==将检查输入值的通用职责转移到 Validator 结构体中==**
![[Pasted image 20240625113902.png]]
> ==这个是类似接口==

1.该函数的参数为指向 Validator 结构体的指针和需要校验的值
2.第二个成员为校验时所需用到的数据由于校验类型不同, 所以被校验数据的类型也不尽相同, 为了能够保存任意类型数据, 这里使用了 void 指针
3.如果是范围检查，则 2中的 void 指针接受的数据为 Range 结构体
4.如果是 push 值递增的校验,则 2中的 void 指针接受的数据为保存上次 push 的值的结构体
6.**使得结构体的生成变得更加简单的宏**

经过以上修改后, 可以使用如下代码生成进行**范围检查的 Validator** 
![[Pasted image 20240625132300.png]]
push 值**递增校验的 Validator** 则则 以像下面这样生成.
![[Pasted image 20240625132332.png]]

**接下来继续修改代码，以便==在 Stack 中使用 Validator==**
![[Pasted image 20240625132430.png]]
> 仍是把函数指针包装进结构体

==接着修改 stack, c 中的 push 函数==
![[Pasted image 20240625132455.png]]
验证功能被转移至了通用函数 validate 中

实际上, **这就是使用 C 进行面向对象开发的一种方式**、




图 3-1 为没有校验器时的代码行为
![[Pasted image 20240625132634.png]]
使用范围检查校验器时的代码行为如图 3-2 所示
![[Pasted image 20240625132649.png]]
图 3-3 为使用 push 值递增检查校验器时的代码行为
![[Pasted image 20240625132706.png]]

### 面向对象与多态性
面向对象的基本思考方式是将数据和处理数据的行为放到一起. 降低耦合性
要点就是不要将数据和处理数据的行为分开
上一节中的带石检杳功能的栈的示例代码就将上限数据、下限数据和栈自身的数据都放在栈结构体中. 结果增加了耦合性。也就是说，仅将数据整合在一起. 是绝对无法降低耦合性的
==**仅将数据整合在一起. 是绝对无法降低耦合性的**==
**之后==我们通过将校验处理和校验处理中所需数据从 Stack 结构体中分离出来, 将它们移至 Validator 结构体中==，使得校验处理被解耦, 从而可以方便地为栈中添加各种各样的校验功能，同时这些功能也 I 以很容易地在其他功能中被复用**

所谓**多态性**就是指**从调用者的角度看对象，会发现它们非常相似，难以区分，但是这些被调用对象的内部处理实际上各不相同**
前面的例子中. 在校验 push 函数的值时, 调用者只会调用 Validator 的 validate 函数，从外部并不知道其中到底进行了怎样的校验处理。
![[Pasted image 20240625133850.png]]
**内部到底进行了怎样的校验处理，取决于 Validator 自身**

像这样，**通过将数据和处理成对分离，并使用结构体和函数指针实现多态性**，就**将校验职责 (赋予对象的任务)分离至 Validator 中了，而被分离出来的部分也可以作为组件被重复利用**
### 继承
**首先有一个校验器，之后扩展出了范围检查校验器和 push 值递增校验器这两个校验器**。我们称第一个校验器为父类，扩展出的两个校验器为子类、

这样在使用父类的地方，都可以直接直接使用子类
**虽然上面的例子中父类的存在比较模糊, 但是如果将没有设定 pData 的 Validator 看作父类，那么就可以将设定了 Range. PreviousValue 的 Validator 看作子类**


这个方法适用于简单情况, **==但情况稍微复杂一些==, 就显得很麻烦了。范围检查校验器中, 这个 pData 指向的是 Range 结构体。假设现在需要扩展范围检查校验器的功能, 使得栈中只能接受奇数或者偶数, 那么仅是简单地扩展 Range 的话, Range 在程序中的角色就会变得很模糊。**
![[Pasted image 20240625144120.png]]

首先我们试着**将目前的两个校验器按照下面的方法重新编写,**
![[Pasted image 20240625151338.png]]
1Validator 结构体的定义中删除 void 指针
2 范围检查校验器的定义请注意在其中**增加了父 Validator 成员** (2-1)。
4 用于生成校验器的宏。


下面再看看实际进行校验的函数
![[Pasted image 20240625151512.png]]
![[Pasted image 20240625151524.png]]
1 **validateRange 对父校验器进行了类型转换, 转换为子校验器 RangeValidator 后访问其中的内部成员**


其他
![[Pasted image 20240625151856.png]]
/
![[Pasted image 20240625151906.png]]
按照下面这样生成使用校验器的栈
![[Pasted image 20240625151934.png]]
1 newStackWithValidator 的第 2 个参数是指向 Validator 的指针, 请注意这里传递的是 RangeValidator 结构体中保存的指向 base 成员的指针。这里利用了结构体中起始内存地址与第一个成员的内存地址相同的特性 


![[Pasted image 20240625152045.png]]
![[Pasted image 20240625152107.png]]

**使用此方法就可以通过继承解决本节中最开始的提出的扩展 Range 的问题。具体而言，就是只'需要按照以下方法将 RangeValidator 作为 base 使用即可扩展 RangeValidator**
![[Pasted image 20240625152122.png]]
**这样，c 中就也可以使用继承了**
### 封装
几乎所有的面向对象语言都提供了访问控制功能以帮助程序员进行封装, 这样就可以让外部无法看见 Rangevalidator 中的内容 (数据隐藏，试图访问内部成员时会出错)。但是遗憾的是 C 中没有提供访问控制功能，无法实现数据隐藏

实际上，我们使用了 const 修饰符，这样就不必担心会从外部不小心修改内部数据了
还可以规定成员命名规则来回避这个问题,例如不允许直接访问的成员在命名时以下划线_开头。
### 虚函数表
各对象都持有函数指针，这样可能会导致内存浪费
![[Pasted image 20240625144412.png]]
根据对象定义生成几个对象吧。
![[Pasted image 20240625144421.png]]
当然，**生成这些内容完全相同的对象是非常浪费的**

当其中含有函数指针以外的数据时情况会如何呢？
![[Pasted image 20240625144507.png]]
现在持有不同的数据就不算浪费内存了吗？函数指针部分依然相同，依然是浪费的

如果出现以下情况 
•有多个对象都具有相同的行为 (即函数指针集相同)
•其中持有较多的函数指针
•需要生成较多数量的对象
持有函数指针的部分就很可能会浪费内存。
此时，**如果引入虚函数表就可以避免回避内存浪费问题**

虚函数表的示例代码
![[Pasted image 20240625150022.png]]
![[Pasted image 20240625152631.png]]
这样的代码结构仅需要在对象中持有指向虚函数表的指针即可，而无需持有函数指针，因此可以节约内
但另一方面，由于函数调用必须经过虚函数表, 所以需要使用如下的调用方法，程序的结构也变得复杂了。
![[Pasted image 20240625150103.png]]
###  非虚函数
通过在对象内持有函数指针，可以让对象的行为根据对象不同而发生变化但是某些函数在不同的对象中处理也可能是相同的，这时就无需在对象内持有函数指针了
![[Pasted image 20240625152745.png]]


假设这里需要增加一个将 count 复位为 0 的函数，这时增加一个虚函数倒也可以
![[Pasted image 20240625152802.png]]

但如果**只是将 count 恢复为 0. 就没有必要动态地根据对象不同改变函数行为: 这种情况下，也可以单独使用一个函数来实现**
![[Pasted image 20240625152906.png]]
这里由于没有使用函数指针，所以能够节省内存，但另一方面，也就意味着无法根据对象来动态地改变行为了


> 以面向对象方式编写代码时，由于不得不在对象内持有函数指针，因此内存使用量会增加，
此外，编译器很难针对函数指针的调用进行内联优化, 这又会让人觉得程序在运行效率上有问题。
首先如果内存使用量过多，请考虑使用虚函数表并将其保存在 ROM 空间中。由于虚函数表不会改变，所以存储在 ROM 上也没有问题
关于运行效率问题，当函数指针的调用成为性能瓶颈的时候，就应该考虑使用非虚函数或是使用宏


## 总结 
•多态可以以相同的方式处理行为不同的对象
•继承可以方便地将不同代码中的共通部分提取出来
•封装可以将对象的行为和内部状态集中到一起，方便进行抽象化

在结构体内持有函数指针的方法来实现多态