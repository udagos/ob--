SystemInit

# gpio
对 GPIO_InitTypeDef 初始化
/*开启控制蜂鸣器的 GPIO 的端口时钟*/
    RCC_AHB1PeriphClockCmd ( RCC_AHB1Periph_GPIOI, ENABLE);

GPIO_Init(GPIOI,GPIO_InitTypeDef);


# 外部中断 （按键）
RCC_AHB1PeriphClockCmd？
// **使能 SYSCFG 时钟** ，使用GPIO外部中断时必须使能SYSCFG时钟
RCC_APB2PeriphClockCmd (RCC_APB2Periph_SYSCFG)
**NVIC_Configuration**
	NVIC_PriorityGroupConfig
		AIRCR
	NVIC_InitTypeDef 结构体配置
		中断源 NVIC_IRQChannel = **EXTI0_IRQn**
	NVIC_Init
GPIO_Init 
// **连接EXTI 中断源到 key1引脚**
SYSCFG_EXTILineConfig（EXTI_PortSourceGPIOA，EXTI_PinSource0）
	**EXTICR**
EXTI_InitTypeDef 结构体配置
EXTI_Init


**KEY1_IRQHandler**

\#define KEY1_IRQHandler                   EXTI0_IRQHandler
//DCD     EXTI 0_IRQHandler                  ; EXTI Line 0    
# 系统定时器
//为什么系统定时器不配置 NVIC_Configuration
SysTick_Config 
	SysTick->LOAD
	//设置中断优先级
	NVIC_SetPriority (**SysTick_IRQn**
	SysTick->VAL = 0 UL;//数值寄存器
	// 设置系统定时器的时钟源为 AHBCLK=180 M  18 
	// 使能系统定时器中断  19 
	// 使能定时器
	SysTick->CTRL = SysTick_CTRL_CLKSOURCE_Msk |  
			SysTick_CTRL_TICKINT_Msk |  
			SysTick_CTRL_ENABLE_Msk;


SysTick_Handler
	TimingDelay --

//EXPORT  SysTick_Handler


# uart 中断


NVIC_Configuration
	NVIC_IRQChannel = USART1_IRQn
//使能串口接收中断
USART_ITConfig (USART1,USART_IT_RXNE)
//使能串口空闲中断
USART_ITConfig (USART1,USART_IT_IDLE)


DEBUG_USART_IRQHandler
# dma 直接模式存储器到外设
用 DMA 把内存数据传输到串口。

USART_DMA_Config
	//开启 dma 时钟
	RCC_AHB 1 PeriphClockCmd（RCC_AHB 1 Periph_DMA 2）
	DMA_DeInit（DMA 2_Stream 7）
	DMA_InitTypeDef 结构体配置
		//usart1 tx对应dma2，通道4，数据流7
		DMA_Channel = DMA_Channel_4
		DMA_PeripheralBaseAddr = DEBUG_USART_DR_BASE
		DMA_Memory0BaseAddr = **SendBuff**
		DMA_DIR = DMA_DIR_MemoryToPeripheral
		DMA_BufferSize = 5000
		DMA_FIFOMode = DMA_FIFOMode_Disable
		//外设数据单位
		DMA_PeripheralDataSize
		//内存数据单位
		DMA_MemoryDataSize
		//不断循环
		DMA_Mode = DMA_Mode_Circular
	DMA_Init (DMA 2_Stream 7, USART_DMA_Config)
	//使能 DMA
	DMA_Cmd (DMA 2_Stream 7)

//**USART1 向 DMA发出TX请求**
USART_DMACmd (USART1,USART_DMAReq_Tx)
# dma 外设到存储器
USART_DMA_Config
	DMA_InitStructure
		//usart 1 rx 对应 dma 2，通道 4，数据流2
		DMA_Channel = DMA_Channel_4
		//设置 DMA 源：串口数据寄存器地址
		DMA_PeripheralBaseAddr = DEBUG_USART_DR_BASE
		DMA_Memory 0 BaseAddr = ReceiveBuff
		DMA_DIR = DMA_DIR_PeripheralToMemory
	DMA_Init (DMA2_Stream2, USART_DMA_Config)
	DMA_Cmd (DMA2_Stream2)

USART_DMACmd (USART1,USART_DMAReq_Rx)


# dcmi
## OV 2640 功能框图
![[Pasted image 20240927110042.png]]
1 控制寄存器
	SIO_C 与 SIO_D 使用的通讯协议 SCCB 跟 I 2 C 十分类似，在 STM 32 中我们完全可以直接用 I 2 C 硬件外设来控制。
2 通信、控制信号及时钟
	XCLK 是用于驱动整个传感器芯片的时钟信号，是外部输入到 OV 2640 的信号；而 PCLK 是 OV 2640 输出数据时的同步信号，它是由 OV 2640输出的信号
3 感光矩阵
4 数据输出信号

### SCCB 时序
外部控制器对 OV 2640 **寄存器的配置参数是通过 SCCB 总线传输**过去的 
SCCB 与标准的 I2C 协议的区别是它每次传输只能写入或读取一个字节的数据
## STM 32 的 DCMI 接口简介
DCMI 数字摄像头接口，支持使用上述类似 VGA 的时序获取图像数据流
接收数据时, 主要使用 HSYNC 及 VSYNC 信号来同步
### “DCMI 整体框图”
![[Pasted image 20240927110600.png]]
![[Pasted image 20241015090014.png]]
### DCMI 接口内部结构
![[Pasted image 20240927110634.png]]
## 代码
使用 DCMI 驱动 OV 2640 摄像头，拍摄的图像直接显示在液晶屏上。
(1) 初始化 DCMI 时钟, I2C 时钟;  
(2) 使用 I2C 接口向 OV2640 写入寄存器配置; 
(3) 初始化 DCMI 工作模式;
(4) 初始化 DMA, 用于搬运 DCMI 的数据到显存空间进行显示; 
(5) 编写测试程序, 控制采集图像数据并显示到液晶屏。
###   初始化 DCMI 的 GPIO 及 I2C
OV2640_HW_Init 
	/*\*\*DCMI 引脚配置 *\**\/
	//使能 DCMI 时钟
	RCC_AHB 1 PeriphClockCmd
	/*控制/同步信号线和*数字信号/
	VSYNC、HSYNC、PIXCLK、PWDN、数据信号线的 gpio 配置
	/\*\*配置 I 2 C, 使用 I2C 与摄像头的 SCCB 接口通讯\*\*/
	//使能I2C时钟
	RCC_APB 1 PeriphClockCmd
	//使能I2C使用的GPIO时钟
	RCC_AHB 1 PeriphClockCmd
	//配置引脚源
	GPIO_PinAFConfig (GPIOB, GPIO_PinSource 6, GPIO_AF_I 2 C 1)
	GPIO_PinAFConfig (GPIOB,GPIO_PinSource7,GPIO_AF_I2C1)
	//初始化 I2C 模式

###   配置 DCMI 的模式
OV 2640_Init ();
	/\*\*\* 配置 DCMI 接口 \*\*\*/
	//使能DCMI时钟
	RCC_AHB 2 PeriphClockCmd
	DCMI_Init
	/
	//开始传输, **从后面开始一行行扫描上来, 实现数据翻转**
	OV **2640_DMA_Config**(FSMC_LCD_ADDRESS+(lcd_height-1)*(lcd_width)*2, img_ width*2/4);
		DMA_FIFOMode = DMA_FIFOMode_Enable
		DMA_FIFOThreshold = DMA_FIFOThreshold_Full
	  /* 配置中断 */
	NVIC_PriorityGroupConfig
	NVIC_InitTypeDef 结构体配置 
		NVIC_IRQChannel = **DMA2_Stream1_IRQn**//配置 dma 中断
	NVIC_Init
	DMA_ITConfig（DMA 2_Stream 1，DMA_IT_TC）
	/****配置帧中断**/
	NVIC_InitTypeDef 结构体再次配置 
		NVIC_IRQChannel = **DCMI_IRQn** //配置帧中断
	NVIC_Init 
	DCMI_ITConfig（DCMI_IT_FRAME）



### 配置 DMA 数据传输
OV2640_DMA_Config
	/* 配置DMA从DCMI中获取数据*/
	DMA_InitStructure.DMA_PeripheralBaseAddr = DCMI_DR_ADDRESS; //DCMI数据寄存器地址
	DMA_InitStructure.DMA_Memory0BaseAddr = DMA_Memory0BaseAddr; //DMA传输的目的地址(传入的参数)
	DMA_InitStructure.DMA_FIFOMode = DMA_FIFOMode_Enable;
![[Pasted image 20241015093850.png]]
### DMA 传输完成中断及帧中断
//DMA 传输完成中断服务函数
DMA2_Stream1_IRQHandler
	if ( DMA_GetITStatus (DMA2_Stream1, DMA_IT_TCIF1) == SET )  
		 line_num++;
		 **OV2640_DMA_Config**(FSMC_LCD_ADDRESS+(lcd_width*2*(lcd_height-line_ num-1)), img_width*2/4)
//帧中断服务函数
DCMI_IRQHandler
	if ( DCMI_GetITStatus (DCMI_IT_FRAME) == SET )
		line_num=0;
### 使能 DCMI 采集
DCMI_Cmd (ENABLE); 
DCMI_CaptureCmd (ENABLE);
### 读取 OV2640 芯片 ID
OV2640_ReadID
### 向 OV2640 写入寄存器配置

OV 2640_UXGAConfig ();



# sdio
控制器对 SD 卡进行读写通信操作一般有两种通信接口可选，一种是 SPI 接口，另外一种就是 SDIO 接口
## SDIO 总线
### 总线协议
SD **总线通信是基于命令和数据传输的**
![[Pasted image 20241015103738.png]]
SD 总线的基本交互是命令与响应交互,
### 命令
## STM 32 的 SDIO 功能框图
stm 控制器有一个 SDIO, 由两部分组成: SDIO 适配器和 APB 2 接口
SDIO 适配器提供 SDIO 主机功能, 可以提供 SD 时钟、发送命令和进行数据传输。APB 2 接口用于控制器访问 SDIO 适配器寄存器并且可以产生中断和 DMA 请求信号。
![[Pasted image 20240927134538.png]]
![[Pasted image 20240927134558.png]]
## 代码 
使用 SDIO 接口对 SD 卡进行基本读写 (不含文件系统)
### gpio初始化
SD_LowLevel_Init 
### DMA 传输配置
SD_LowLevel_DMA_TxConfig  
	**DMA_ITConfig(SD_SDIO_DMA_STREAM, DMA_IT_TC, ENABLE);**
SD_LowLevel_DMA_RxConfig
	DMA_ITConfig(SD_SDIO_DMA_STREAM, DMA_IT_TC, ENABLE);
###  SD卡初始化
SD 卡初始化过程主要是卡识别和相关 SD 卡状态获取
SD_Init
	NVIC_PriorityGroupConfig
	NVIC_InitTypeDef 
	NVIC_Init
		NVIC_IRQChannel = **SDIO_IRQn** 
	NVIC_Init
		NVIC_IRQChannel = **SD_SDIO_DMA_IRQn**
	SD_LowLevel_Init
	SDIO_DeInit 
	、
	SD_PowerON
		

#### SD_PowerON
SD_PowerON
	**SDIO_SendCommand**//给sd卡发送指令 




 CMD0 命令无需响应, 所以调用 CmdError  函数检测错误即可
 CMD8 的响应类型是 R7,  使用 CmdResp7Error 函数可获取得到 R7 响应结果
### SD 卡数据操作
数据读取和写入都可以分为单块操作和多块操作。

SD_WriteBlock
	SDIO_ITConfig
	**SD_LowLevel_DMA_TxConfig**
	SDIO_DataConfig（SDIO_DataInitStructure）
执行完以上代码后, SDIO 外设会自动生成 DMA 发送请求, 将指定数据使用 DMA 传输写入到  SD 卡内。

SD_ReadBlock
	SDIO_ITConfig
	**SD_LowLevel_DMA_RxConfig**
	SDIO_DataConfig（SDIO_DataInitStructure）
最后控制器向 SD 卡发送单块读数据命令 CMD17, SD 卡在接收到命令后就会通过数据线把数据传输到控制器数据 FIFO 内, 并自动生成 DMA 传输请求
### SDIO 中断服务函数
SDIO_IRQHandler
	SDIO_GetITStatus(SDIO_IT_DATAEND)

# eth
## phy:LAN8720A
在物理层, 由 IEEE 802.3 标准规定了以太网使用的传输介质、传输速度、数据编码方式和冲突检测机制, **物理层一般是通过一个 PHY 芯片实现其功能的。**

![[Pasted image 20241015105511.png]]
## MAC 子层
MAC 子层是属于数据链路层的下半部分, 它主要负责与物理层进行数据交接, 如是否可以发送数据, 发送的数据是否正确, 对数据流进行控制等。它自动对来自上层的数据包加上一些控制信号, 交给物理层。接收方得到正常数据时, 自动去除 MAC 控制信号, 把该数据包交给上层
## TCP/IP 协议栈
### 为什么需要协议栈
物理层主要定义物理介质性质, MAC子层负责与物理层进行数据交接。**就嵌入式控制芯片来说, 很多都内部集成了MAC控制器, 完成MAC子层功能, 所以依靠这部分功能是可以实现两个设备数据交换**

**使用以太网接口的目的就是为了方便与其它设备互联, 如果所有设备都约定使用一种互联方式,  在软件上加一些层次来封装, 这样不同系统、不同的设备通讯就变得相对容易了**
## 以太网外设 (ETH)
**STM32F42x 系列控制器内部集成了一个以太网外设, 它实际是一个通过 DMA 控制器进行介质访问控制 (MAC), 它的功能就是实现 MAC 层的任务**

借助以太网外设, STM 32 F 42 x 控制器可以通过 ETH 外设发送和接收 MAC 数据包。
MII 和 RMII 接口用于 MAC 数据包传输, ETH 还集成了站管理接口  (SMI) 接口专门用于与外部 PHY 通信, 用于访问 PHY 芯片寄存器。

P**HY 芯片是物理层功能实现的实体**

**ETH 有专用的 DMA 控制器,** 它通过 AHB 主从接口与内核和存储器相连, AHB 主接口用于控制数据传输, 而 AHB 从接口用于访问控制与状态寄存器 (CSR)空间。
**在进行数据发送是, 先将数据有存储器以 DMA 传输到发送 TXFIFO 进行缓冲, 然后由 MAC 内核发送;** 
**接收数据时, RXFIFO 先接收以太网数据帧,再由 DMA 传输至存储器**
ETH 系统功能框图见图 ETH 功能框图
![[Pasted image 20240927141020.png]]
### SMI 接口
SMI 是 MAC 内核访问 PHY 寄存器标志接口

#### SMI 帧格式
SMI 是通过数据帧方式与 PHY 通信的
#### SMI 读写操作
### MII 和 RMII 接口
介质独立接口 (MII) 用于连接 MAC 控制器和 PHY 芯片, 提供数据传输路径。RMII 接口是 MII  接口的简化版本
### mac 数据包发送和接收
**ETH 外设负责 MAC 数据包发送和接收**。
**利用 DMA 从系统寄存器得到数据包数据内容, ETH 外设自动填充完成 MAC 数据包封装，然后通过 PHY 发送出去**
## 代码
使用 TCP 协议与电脑端通讯, 用作 TCP client

ETH_BSP_Config
	ETH_GPIO_Config 
	ETH_MACDMA_Config
		RCC_AHB 1 PeriphClockCmd

 LwIP_Init ();
	netif_set_link_callback (**ETH_link_callback**)

LwIP_Periodic_Handle 
	ETH_CheckLinkStatus//获取 PHY 状态



### 0 
更多涉及到 ETH 硬件底层驱动.**一些是 PHY 芯片驱动函数、一些是 ETH  外设与 LwIP 连接函数**
### ETH_BSP_Config
ETH_BSP_Config
	ETH_GPIO_Config
	ETH_MACDMA_Config
#### ETH_GPIO_Config 
**STM 32 f 42** x 控制器**支持** MII 和 RMII 接口, **通过程序控制使用 RMII 接口**, 同时需要使能 SYSYCFG  时钟, 函数后部分就是接口 GPIO 初始化实现, 这里我们还连接了 LAN 8720 A 的复位引脚, 通过拉低一段时间让芯片硬件复位
#### ETH_MACDMA_Config
首先是使能 ETH 时钟, 复位 ETH 配置。ETH_StructInit 函数用于初始化 ETH_InitTypeDef 结构体变量, 会给每个成员赋予缺省值。接下来就是根据需要配置 ETH_InitTypeDef 结构体变量, 关于结构体各个成员意义已在“ETH 初始化结构体详解”作了分析。最后调用 ETH_Init 函数完成配置, ETH_Init 函数有两个形参, 一个是 ETH_InitTypeDef 结构体变量指针, 第二个是 PHY 地址,  函数还有一个返回值, 用于指示初始化配置是否成功。

### ETH_link_callback 
**被 LwIP 调用, 当链路状态发送改变时该函数就被调用, 用于状态改变后处理相关事务**
### 1 LwIP 应用相关
### LwIP_Init 
LwIP_Init 函数用于**初始化 LwIP 协议栈**
首先是内存相关初始化,  mem_init 函数是动态内存堆初始化, memp_init 函数是存储池初始化
接下来为 ipaddr、netmask 和 gw 结构体变量赋值, 设置本地 IP 地址、子网掩码和网关, 如果使用  DHCP 功能直接赋值为 0 即可
netif_set_default 函数用于设置指定网卡为默认的网络通信设备
最后, 根据需要调用 netif_set_link_callback 函数实现当链路状态发生改变时需要调用的回调函数配置。

