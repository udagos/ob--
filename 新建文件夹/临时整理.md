


类没有复用，就没有作为类的必要，没有动作也就结构体内没有函数指针



spi 是板级的传输，就是因为长了不行
时序都是测入的，线的长度和时钟频率，导致建立保持时间不满足，协议可能不适用

无效电平（低电平）是高电平的百分之多少




某一光源的明输出是发光使能的后一个名输出
名输出顺序：白 ir  白 uv  白 tx1   白 tx 2



模拟信号上升沿是坡度的中间

数字通道会干扰模拟通道


产生干涉的原因是有问题的表笔，其他表笔就算手碰也会有干涉

**模拟通道一直有，数字通道动一动，verf 接地好一点，换个测量点**



# 镜像与容器
镜像是类容器时对象




工程中结构体是类
类的接口时直接传递该类
在其他没模块要先获得该实例




能在模块内写的函数就在模块内定义 
	比如获取循环箱状态机 
	
速度控制状态机和appgatecommon中的退出超时检测冲突么

事件是一对多
