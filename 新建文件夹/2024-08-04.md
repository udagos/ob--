
名校正
	rgbdata:
暗校正
	darkdata:
偏移校正
	imagemin:
cisdeug 在校正后重启后输出的是自动校正的暗校正，但加载的仍是校正的原始暗校正

cisdebug 中的 CisCorrectDataOut
	暗校正和名校正，通过 usb 发送




扫描幅面用工具直接读出，图像
自动校正的白基准区域在日志中及基准计算值
扫描绝对速度
最大最小扫描介质，不同分辨率，不同模式
	虽然和上面扫描幅面差不多，但是没有分辨率



si 日志在 BvCisSiWidthControl
10 2 4



## 记录 
typedef struct stack_type \*Stack;
可以看成先 struct stack_type \*Stack 再 typedef  \*Stack

同理 
typedef struct tagBNImagePara
{
	POINT 2 i	LeftUpCorner;				//前景左上顶点坐标
	POINT 2 i	RightUpCorner;				//前景右上顶点坐标
}BNImagePara;


### 2
**ThickAlgorithm.h**
typedef struct
{
	...
}THICK_CORRECT_LIST;

typedef struct
{
	THICK_CORRECT_LIST mCorrect;
}**THICK_SORT_LIST**;
因为要用 THICK_CORRECT_LIST，所以即使 THICK_CORRECT_LIST 直接的外部引用也放到. h 中，这样直接放到. h 中，可被修改和访问

extern THICK_SORT_LIST          gThickSortList;
<span style="background:#ff4d4f">为什么 gThickSortList 要在别的模块. c 中定义？或直接在别的模块定义该变量没必要在这定义</span>


**ThickAlgorithm. c**
THICK_SORT_LIST  gThickSortList=
{
	{
		{
			THICK_CHANNEL_GAP\*0+THICK_MODULE_LEFTMARGIN			//第 0 通道左边缘据起始点距离
		},
		17,							//邻近两个通道的间距
		20,							//允许偏移距离
		{0,0}						//保留
	},



循环箱的 typedef struct 都在. c 中，**防止外部使用该类型**


看一个. h 文件哪些结构体是直接暴露的


### 3
typedef struct
{
    const CHAR \*mName;                      
	PIDPARA mPidPara;                          
	
}DC_MANAGER;

extern DC_MANAGER \***MidGetDcMotorInMainManagerInstance**(DCMOTOR_INMAIN_INDEX MotorId);

DC_MANAGER *MidGetDcMotorInMainManagerInstance(DCMOTOR_INMAIN_INDEX MotorId)
{
    return &sDcMotorInMainManager[MotorId];
}
sDcMotorInMainManager 在. c 初始化

这样和前面一个在模块外定义的区别应该是，模块外要修改。
模块内定义需要实例访问，也能改