---
up:
  - "[[../moc/work|work]]"
---
#kan


#ed 
[moc 日志](../moc/moc%20日志.md)



## 入钞出钞停和入的发出
//以下重要
状态机中 
出钞
	//**在回调函数中进行空间状态检测**，此处只对空间状态进行检查
	if (mGateSpaceState == GATE_SPACE_STATE_EMPTY)
		GateWorkFsmSet（GATE_HANDLE_FSM_STOP）
入钞
	RcbWorkInFlowHandle
	%% GateWorkFsmSet (GATE_HANDLE_FSM_EXITREADY); %%


**入钞中的工作流程**
RcbWorkInFlowHandleInWorking
	OsMsgWait (**ptFlowManager->mptCtrl->mptGateHandleManager->mptCtrlMsg**, &ptFlowManager->mTaskMsg） 
	Task = mTaskMsg.mTask
	mTapeState == RCB_TAPE_STATE_FULL
		RcbWorkInFlowChangeToStop
	if Task == GATE_HANDLE_TASK_STARTWORKIN
		RcbWorkInFlowStartHandle 入钞流程启动
	else if (Task == GATE_HANDLE_TASK_STOP_NORMAL)
		RcbWorkInFlowChangeToStop
	else if (Task == GATE_HANDLE_TASK_STOP_ABNORMAL)
		RcbWorkInFlowChangeToStop（RCB_WORKIN_FLOW_RES_STOPABNORMAL）
	 else if (Task == GATE_HANDLE_TASK_EXITREADY)
		RcbWorkInFlowChangeToStop（RCB_WORKIN_FLOW_RES_STOPNORMAL）
//出钞空间检测空就停了，入超超口控制比较麻烦


ptFlowManager->mptCtrl->mptGateHandleManager->**mptCtrlMsg 谁发的**
**GateWorkHandle**（GateCtrl）
	mTask = GateCtrl
	OsMsgSend (sGateHandleManager[Id]->mptCtrlMsg, &GateHandleMsg）


//GateWorkHandle 被引用的地方
**钞口管理线程**
GATE_FSM_WORKING
	GateWorkHandle（GATE_HANDLE_TASK_STARTWORKOUT）
	if (OsMsgWait (&**sOsMsgGateManagerTask**, &GateManagerMsg))
	else 
		if (GateManagerMsg. mCtrlTask == GATE_CTRL_STOP)
			GateWorkHandle (GATE_HANDLE_TASK_STOP_NORMAL)
		else if (GateManagerMsg. mCtrlTask == GATE_CTRL_ERRSTOP)
GATE_FSM_READY
	GateModuleReadyHandle (STD_ENABLE);
		...
		GateWorkHandle makready exitready
GATE_FSM_INIT
	if(GateManagerMsg.mGateNo == GATE_RCB)
		GateRcbInitHandle ();
			GateWorkHandle (init)
**（还是 GateManagerCtrl 发送的 sOsMsgGateManagerTask）**
这个好像是负责出钞的控制


**GateManagerCtrl**
	GateWorkHandle

GateManagerCtrl 在 flow 中
GateManagerCtrl  （stop&out）在 GateManagerCheckInFlow 流程中钞口管理 **在** MainAndDestBranPaperManage 主通道和目的分支通道纸币管理
GateManagerCtrl（in） 在 InformDestGate 将纸币信息发到目标钞口 **在** 目标钞口纸币管理
GateManagerCtrl（stop）在 DestGatePaperManage 目标钞口纸币管理 **在** MainAndDestBranPaperManage







## 超口管理线程
mCurrentFsm = GATE_FSM_POWERON
for
	switch（**sGateManager**.mCurrentFsm）
	GATE_FSM_POWERON
		SetGateManagerFsm (GATE_FSM_IDLE);
	GATE_FSM_IDLE
		OsMsgWait (&sOsMsgGateManagerTask, &GateManagerMsg)
		switch (GateManagerMsg. mCtrlTask)
			GATE_CTRL_MAKERDY
				setGateManagerFsm (GATE_FSM_READY);
				GateManagerStatusEvtHandle
				(GATEMANAGER_EVT_MAKEREADY, 0);
			GATE_CTRL_INIT
				SetGateManagerFsm (GATE_FSM_INIT)
				GateManagerStatusEvtHandle
				(GATEMANAGER_EVT_INITSTART, 0)
			GATE_CTRL_STOP  
				SetGateManagerFsm (GATE_FSM_STOP)
				GateManagerStatusEvtHandle
				(GATEMANAGER_EVT_EXITREADY, 0);
	GATE_FSM_READY
		mErrCode = CheckGateManagerPara () != 0
			SetGateManagerFsm (GATE_FSM_ERROR)
			GateManagerStatusEvtHandle (GATEMANAGER_EVT_ERR, 
			GATEMANAGER_EVT_MAKEREADY 
			| GATEMANAGER_EVT_READY)
	GATE_FSM_MANAGER:
	GATE_FSM_WAIT
		GateManagerStatusEvtHandle (0, GATEMANAGER_EVT_WORK)
		if mErrCode && IsGateEmptyErrCode
		(GATE_NULL,  sGateManager.**mErrCode**) == STD_FALSE//**除了空的错误码**，钞口停止
			GateManagerStatusEvtHandle (**GATEMANAGER_EVT_ERR**, 
			GATEMANAGER_EVT_WORK)
			SetGateManagerFsm(GATE_FSM_STOP)
	GATE_FSM_WORKING:
		..
	GATE_FSM_STOP
		GateHandleErrFlg = GateHandleWaitOver
		(GATE_HANDLE_WAITOVER_MODE_EXITREADY)
		if mErrCode && ! IsGateEmptyErrCode (GATE_NULL, mErrCode)
			SetGateManagerFsm (GATE_FSM_ERROR)
				mErrCode = GateHandleErrFlg
				SetGateManagerFsm(GATE_FSM_ERROR);
	GATE_FSM_ERROR:
		SetGateManagerFsm(GATE_FSM_IDLE);
	GATE_FSM_INIT:


sGateManager.mErrCode 是如何被赋值的？ #疑问 
发现

###    纸币的流程实现日志
paic: FLOW_STATE_ERROR, GateFSMStat: IDLE  //sFlowCurState, GateFsmGetCurStateString

## 流程
case  FLOW_STATE_IDLE
	if (**BvErrInfoGet** (MODULE_GET_ERR_CLASS)!=0)
	||超口||流程
		FlowNextState = FLOW_STATE_ERROR 
	if OsMailWait (&sOsMailFlowCtrl, &FlowCtrl, 0) == RT_EOK
		if (FlowCtrl == FLOW_CTRL_START)
			FlowNextState = FLOW_STATE_START;
			“”**FlowControlStartWorkProc**
case FLOW_STATE_START:
	if(OsMailWait(&sOsMailFlowConfig, (U32 \*)&FlowConfig, 0) == RT_EOK)
		SystemStateSet (SYSTEM_STATE_WORKING);
	memcpy(&gFlowConfig, FlowConfig）
	DEBUG_LOG_TRACE FlowControlStart
	**Flow_FromPointToPoint（gFlowConfig）**
	DEBUG_LOG_TRACE FlowControlStop
	FlowNextState = FLOW_STATE_USER;
case FLOW_STATE_ERROR:
	SystemStateSet(SYSTEM_STATE_ERR);
		“”SystemStateIsError

## 流程日志
FlowControlStartWorkProc
“”**FlwFSMStat**: FLOW_STATE_START, **GateFSMStat:IDLE**
SystemStateIsWork
FlowControlStart
... Flow_FromPointToPoint
FlowControlStop
FlwFSMStat: FLOW_STATE_USER, GateFSMStat:IDLE
FlwFSMStat: FLOW_STATE_ERROR, GateFSMStat:IDLE
SystemStateIsError


### 在 Flow_FromPointToPoint 中日志
FlwFSMStat:FLOW_TRANS_STATE_INIT
**GateManagerCtrl:MAKERDY**
GateManagerFsm:READY


## 如果有错如何退出流程
==有错不光要退出流程状态机，其他线程状态机也要重置==
在 Flow_FromPointToPoint 
FLOW_TRANS_STATE_WORKING 
	for 
		MainAndDestBranPaperManage  
			塞纸检测
		if ==OneOrMoreModuleGoWrong==//如果有错
			if ==mFlow_AutoClrErr== == 2|| mFlow_AutoClrErr == 0//如不是自动排错
				 NextState = **==FLOW_TRANS_STATE_BRAKE==**;//从这里退出流程
			else 
				自动排错
FLOW_TRANS_STATE_BRAKE 
	NextState = FLOW_TRANS_STATE_SRC_GATE_STOP;
	''**FlowStopMotorAtOnce**
FLOW_TRANS_STATE_END
	GateManagerCtrl（GATE_CTRL_STOP）

**FlwFSMStat**: FLOW_TRANS_STATE_SRC_GATE_STOP 
GateManagerCtrl:ERRSTOP
FlwFSMStat:FLOW_TRANS_STATE_END


OneOrMoreModuleGoWrong
	if (FlowErrInfoGet (MODULE_GET_ERR_ALL)&&CashBoxModuleGetErrCode)


%% update 和 errcode 区别
update 
	merrcode = errcode %%

GateManagerCtrl(GATE_CTRL_STOP, GATE_NULL, 0);
	不会有 GateWorkHandle




%% 按照循环钞箱堵转类似处理退出，置状态 
或是流程状态机
	FLOW_TRANS_STATE_SRC_GATE_STOP
	GateManagerCtrl(GATE_CTRL_ERRSTOP, GATE_NULL, 0);
		if (OsMsgSend (&sOsMsgGateManagerTask
但最好用第一种循环钞箱自己处理 %%

%% RCB_MOTOR_MOVE_RES_MOTORJAM
RCB_WORKIN_FLOW_RES_MOTORJAM, %%



%% RcbMotorMoveHandle 
	if (IsGateMotorErrWhenWaitSem(ptHandleManager) != ERR_GATE_NOERR)
		RCB_MOTOR_MOVE_RES_MOTORJAM

RcbWorkInFlowHandleInWorking 
	if (AppIsMotorRunning 
		RcbWorkInFlowChangeToStop (ptFlowManager, 
		RCB_WORKIN_FLOW_RES_MOTORJAM); %%
## 超时
//IsGateMotorErrWhenWaitSem 会在初始化和出钞中调用，

IsGateMotorErrWhenWaitSem 
	如果在电机在加速和运行（AppIsMotorRunning）
		return MakeErrCodeGate ERR_GATE_MOTORERR
	if 没有等到电机中断信号
		if 次数>10
			return MakeErrCodeGate ERR_GATE_MOTORERR

%% AppIsMotorRunning  
	mSpeedCtrlFsm == DCMOTOR_SPEEDCTRL_FSM_START 
	||DCMOTOR_SPEEDCTRL_FSM_RUN
		return 1 %%

DcMotorInMainFsmUpdate (state)
	mSpeedCtrlFsm = State;

DcMotorInMainSpeedCtrl 
	switch  mSpeedCtrlFsm
### 电机线程的超时
DcMotorInMainCaptureErrCheck (MotorStopFlg)
	StepState = **MidDcMotorCaptureStateGet**
	StepState == **DCMOTOR_STEPSTATE_TIMEOUT**
		if mSpeedCtrlFsm == DCMOTOR_SPEEDCTRL_FSM_STOPPING
		||mSpeedCtrlFsm == DCMOTOR_SPEEDCTRL_FSM_STOP
			errcode = ERR_MOTOR_NO_ERROR;
		else 
			errcode = **ERR_MOTOR_JAM_STOP**//在电机运转过程中检测到电机停止
		if (MotorStopFlg)
			MidMotorBrake
			**DcMotorInMainFsmUpdate** (errcode，DCMOTOR_SPEEDCTRL_FSM_STOP)
MidDcMotorCaptureStateGet





# 电机控制任务 
OsEventWaitOr（sOsEvtMotorCtrlTask）
TrigerEvt = OsEventGet(&sOsEvtMotorCtrlTask);
**MotorCtrlMsgCheck**
if TrigerEvt & MOTOR_CTRL_EVT_TIMER//如果存在定时器触发
	MotorSpeedCtrlTaskHandle
		**DcMotorInMainSpeedCtrl**


定时器中断回调中
	MotorCtrlTimerTriger
		**OsEventSend** (&sOsEvtMotorCtrlTask
		
**AppMotorCtrl**
	if needpid
		if (OsMsgSend (&**sOsMsgMotorCtrl**
			MotorCtrlTaskTriger
				**OsEventSend** (&sOsEvtMotorCtrlTask
			return 0


DcMotorInMainSpeedCtrl
switch  mSpeedCtrlFsm 
	DCMOTOR_SPEEDCTRL_FSM_START 
		MidMotorSpeedCtrl//电机速度控制
		**step** = **DcMotorInMainCaptureErrCheck (STD_FALSE)**//为 0 时不更新状态，也不停电机
		if mCurrentStateCnt++ > BLDC_START_TIMEOUT//1.5 s
			DcMotorInMainFsmUpdate (**DCMOTOR_SPEEDCTRL_FSM_RUN**)
		if step == **DCMOTOR_STEPSTATE_TIMEOUT** 
			**break**//
		if errcode = DcMotorInMainSpeedCheck == 0
			if 连续 20 步速度稳定（和目标速度差小于 10%）
				DcMotorInMainFsmUpdate (**DCMOTOR_SPEEDCTRL_FSM_RUN**)
	DCMOTOR_SPEEDCTRL_FSM_RUN 
		MidMotorSpeedCtrl
		StepState = DcMotorInMainCaptureErrCheck (**1**)
		StepState == DCMOTOR_STEPSTATE_TIMEOUT //未检测到脉冲超时
			break
	DCMOTOR_SPEEDCTRL_FSM_STOPPING 
		MidMotorSpeedCtrl
		DcMotorInMainCaptureErrCheck
	DCMOTOR_SPEEDCTRL_FSM_STOP 
		DcMotorInMainCaptureErrCheck
		StepState == DCMOTOR_STEPSTATE_TIMEOUT 
			MidMotorStop
	DCMOTOR_SPEEDCTRL_FSM_WAIT 
	DCMOTOR_SPEEDCTRL_FSM_IDLE



AppMotorSpeedCtrlMsgStartHandle (ptmsg)
	DcMotorInMainFsmUpdate **DCMOTOR_SPEEDCTRL_FSM_START** 

AppMotorSpeedCtrlMsgStopHandle
	DcMotorInMainFsmUpdate **DCMOTOR_SPEEDCTRL_FSM_STOP**

sMotorSpeedCtrlMsgHandleFun\[]\[] (ptMsg)
	[MOTOR_CTRL_START]=
		[DCMOTOR_SPEEDCTRL_FSM_IDLE]=
		AppMotorSpeedCtrlMsgStartHandle,


MotorCtrlMsgCheck
	OsMsgWaits **OsMsgMotorCtrl** MsgMotorCtrl
	MotorCtrlMsgHandle (MsgMotorCtrl)
		sMotorSpeedCtrlMsgHandleFun\[ptMsg->mCtrlMode]\[mSpeedCtrlFsm]


进入速度状态机外部进就是 stat 和 stop，AppMotorCtrl 配置表
循环钞箱电机也是用这个么：是
电机捕获异常检查在电机状态机中，如果超时未捕获停流程
既然有超时未捕获停，为什么循环钞箱还有 IsGateMotorErrWhenWaitSem，这个循环钞箱电机中断回调中发送的
有刷无刷的测速原理相同，MidDcMotorInMainSpeedCalc，**捕获有刷电机使用码盘，无刷电机使用内部电感**
电机没有捕获计数累计多少时间后，如果不是电机正常停状态，报 ERR_MOTOR_JAM_STOP，if MotorStopFlg 就停止流程
循环钞箱是进一次捕获中断调用一次发送，如果多久没接收


MidDcMotorCaptureStateGet 
	 if (MidDcMotorStepRunCheck
		 mStepCheckTimeRecord = TimeStart();
		 return  DCMOTOR_STEPSTATE_NORMAL
	else 
		mStepNoMoveTimeCnt = TimeElapse（mStepCheckTimeRecord）
		mStepNoMoveTimeCnt > mStepNoMoveOutTime
		return DCMOTOR_STEPSTATE_TIMEOUT

MidDcMotorStepRunCheck 
	StepValue = DrvGetDcMotorInMainRecordValue
	if mStepNumRecord != StepValue//发生了运动 
		mStepNumRecord = StepValue
		return 1


DrvGetDcMotorInMainRecordValue  //直流电机计数值获取
	if mPulseData == FPGA_ADDR_OFFSET_NULL 
		 return 0;
	else 
		return ReadFpgaU32Data (**mPulseData**)


**MidDcMotorInMainSpeedCalc**
	CaptureSumValue = DrvGetDcMotorInMainCapValue (MotorId);
		 return ReadFpgaU32Data（**mCapSumData**）
	StepValue = DrvGetDcMotorInMainRecordValue(MotorId);
	StepValue < mSumMode
		CaptureValue = CaptureSumValue / StepValue;
	else  
		CaptureValue = CaptureSumValue / mSumMode;
	mCurrentSpeed = mLenPerStep/CaptureValue


mLenPerStep 
	# define BLDC_STEP_LEN(len,poles)    ((len) * 1000 / (3 * (poles)))
	# define BDC_STEP_LEN (len, poles)     ((len) * PI \*1000 / (poles))
	BDC_STEP_LEN (17.0, 20) //17 是速度滚的直径，即每一个码道对应的卷带长度
	 umbm 1 BLDC_STEP_LEN (UMBM 1_LEN_PER_CIRCLE, 6)  3相 6级：大的
	 LABM 1  BLDC_STEP_LEN (LABM 1_LEN_PER_CIRCLE, 4),   3 相 4 级：小的



循环钞箱 
OsTaskRecycleCashBox
	for
		case GATE_HANDLE_FSM_WORKOUT 
			AppMotorStartParaInit
			RcbMotorStart
			while（1）
				ErrCode = **IsGateMotorErrWhenWaitSem** 
				ErrCode != ERR_GATE_NOERR 
					break
			break//重新回到出钞，重启电机？

RcbMotorMoveHandle 
	RcbMotorStart
	for 
		 if **IsGateMotorErrWhenWaitSem**
			 MoveRes = RCB_MOTOR_MOVE_RES_MOTORJAM;
			 break 
	**RcbMotorStop** 
	return MoveRes



ready 中的第一阶段 
	MotorMoveRes = RcbMotorMoveHandle 
	switch (MotorMoveRes) 
	case RCB_MOTOR_MOVE_RES_MOTORJAM:
		return MakeErrCodeGate

 ErrCode = RcbInitHandle
if (ErrCode != 0)   //  发生异常
{
	GateWorkErrHandle(ErrCode);
}

检查到没捕获多久之后才判断为堵转停流程，为了防止时间太长导致卷带异常
如果刚检查到没捕获就检测卷带，如果卷带状态变化肯定是电机转但捕获不到了，但如果是空出钞也没有卷带状态的变化，但这种在流程上也不怎么出现


在循环中调用一个函数，函数中 switch break 退出（函数中无循环）, 会退出外面函数的循环么？
continue 语句只用于循环语句，不用于 switch 语句。在某个循环包含的 switch 语句中的 continue 语句，将导致进入下一次循环。
使用 break 将跳出本 switch 但仍将执行 for 循环的剩余语句；使用 continue 则直接跳至 i++进行下一次 for 循环执行






OneOrMoreDestGateAreFull   检测目标钞箱是否满，用于停止流程
	RcbTapeStateCheck GateNo == GATE_SPACE_STATE_FULL 目前并没有退出流程

每一个捕获更新一次时间
电机线程没有运动时间超过目标速度跑一圈的时间

电机线程超时不停也不切换因为在加速怕有不满足的，所以加速阶段也不能用来发送事件。
入钞阶段因为卷带位置确定了，所以不担心加速 1.5s 会断
初始化阶段，如果是加速无捕获无法停电机，如果 1s 没有信号量停电机，停电机，不会超时到运行状态，因为切换到停止状态了
出钞，无需担心
**所以只有初始化停止电机，其他只是做好堵转和码盘异常区分就好**
150 cm/s/20=

rt_sem_take （10）是多久, time 单位为系统滴答
OsSemWait（10）检验一次可以么

加速阶段 1.5s 没超时停机怎么办？

堵转虽然电机流程停，但循环钞箱流程没停，
循环钞箱流程停的条件
RcbMotorMoveHandle
	for
		if IsGateMotorErrWhenWaitSem
			MoveRes = RCB_MOTOR_MOVE_RES_MOTORJAM
	电机停
	return MoveRes


前提：堵转只停电机，各个流程仍是正常的
入朝停
	工作
		如果不是运动和run
			钞箱停，切换到停止流程
	停止状态
		看消息
		GATE_HANDLE_TASK_STOP_NORMAL
		GATE_HANDLE_TASK_STOP_ABNORMAL
	
出钞停



if FlowGetSysErrCode
	GateManagerCtrl (GATE_CTRL_ERRSTOP
