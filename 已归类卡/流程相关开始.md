---
up:
  - "[[../moc/work]]"
---
#kan
bv 没有退出，updata
bv 的类错误码



SYS_ERR_CLASS_JAM
if((ErrCodeUnion.mGroup == ERR_GROUP_NOTE_TRANFER) && (ErrCodeUnion.mError == ERR_TRANS_JAM_NOTE_REMAIN))



GateManagerCtrl 钞口管理
	GateWorkHandle 发送控制信息



![[Pasted image 20240712135326.png]]
AppMotorCtrl 电机控制 
MotorCtrlMsgCheck 电机线程中的电机控制速度检查
	MotorCtrlMsgHandle





之前 bv 是个单独的模块，抬盖就不用单列了，机器的抬盖就是 bv 的抬盖

4 个字节，第三个启动、流程中做不做区分，如果少直接放到第三字节预留第四字节；如果多在第三字节做一下区分并占用第四字节

测厚的错误码并没有按 makeerrcode 的格式，需要重新写么





thickalgorithm. c
mErrorCode 流通钞错误码
mATMErrorCode  atm 钞错误码

几种生成错误码的方式
MakeErrCodeTrackMotor
BvMakeErrCode
BvMakeAndSaveErrCode
MakeErrCodeFlow
MakeErrCodeGate
![[Pasted image 20240712164015.png]]


bv
上传里面有判断 class 的


在第三字节测厚流通钞，和测厚 atm 钞票
第四字节用具体什么错
用 bv 提交错误码 （需不需要保存）保存为了查询上次的错误码没别的用，错误码没法传递

因为 bv 没有单独的电源和抬盖，所有没有 class




ThickStateQuery（mDataFile）
**mThicknessData.mErrorCode = mDataFile.mErrorCode**


测厚的错误码用处
	SendToLinuxPaperImage 向 linux 发送介质信息
	ThickDataHandle 在测厚处理任务
		![[Pasted image 20240712183305.png]]
		return result
		if result
			mRejectFlag =  即PaperPtr->mPaperData.mRejectBnData.mRejectFlag |= DOUBLEPAPER_ERR;
		if（mRejectFlag)
			RejectCode = 


搜索mRejectBnData.mRejectFlag
测厚结果 生成拒钞码 mRejectFlag，拒钞码会停机么
图像识别清分也有一个 ImageRecognitionSort  return 的是判断来的-1 与 0
SerialNoHandle 冠字号处理。


mMagneData. mErrorCode
**RejectCode = PaperDataPtr->mMagneData.mErrorCode;**
mMagneData. mErrorCode 没有赋值没有开发只有测厚


测厚的错误码自动排错会处理么，还是拒钞会处理 
**不会，只是把钞排到出钞口；和 bv 排错是不一样的**


MainAndDestBranPaperManage
	PaperJamJudge
	return
检测到塞纸进入到自动排错，目前没有自动排错所有错误应该都是停机处理，之后的化会地不同的错误码做分别的处理


