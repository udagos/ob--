---
up:
  - "[[mocWorkCode]]"
---
 [moc 日志](../moc/moc%20日志.md)
 #ed 
 
 
 #kan
MainAndDestBranPaperManage//主通道和目的分支通道纸币管理
	UpdateSensorState//更新通道传感器
	GateManagerCheckInFlow//流程中钞口管理
	**PaperPosUpdate**//更新纸币位置
	PaperEnterMainTrackCheck//纸币进入主通道检测
	OutCashControl//根据斜度和钞间距进行拒钞
	FlowSelectorCtrl//拨叉控制
	BvModuleManageInFLow//流程中 bv 模块处理
	AllDestGatePaperManage
		FOR_LOOP_ALL_GATE
			**BranPaperPosUpdate**
	if ((Cnt++) % 2)
		**PaperEnterDestBranHandle**//处理纸币从主通道进入分支通道
	else//均衡奇偶步的执行时间,以便降低单步的最大执行时间

FLOW_TRANS_STATE_WORKING
	for
		MainAndDestBranPaperManage

PaperEnterDestBranHandle //**先有了主通道，切换了拨叉，才有的分支通道**
	WALK_MAIN_LIST_FROM_FIRST
		PaperSlopAndLenCheckBeforeInsertExit//在插入目的钞口链表之前对纸币进行斜度过大和长度过大检测
		BranNodeList = FlowGetBranNodeList (Paper->mDestGateNo);
		DestBranEntryNodeSN = TrackNodeGetFlowSN (Paper->mSrcGateNo, **BranNodeList->mOverlapNodeId**);//目的钞口入口节点顺序号
		\*EntryNodeInfo = &Paper->**mInfoOfNode**[DestBranEntryNodeSN]
		//mInfoOfNode **纸币通过传感器节点的记录**
		 if ((EntryNodeInfo->mHeadPosMax >= PAPER_ENTER_DEST_BRAN_MIN) 
		 && Paper->mPaperBVFlag)
			 **PaperEnterDestBran**(Paper)//插入出口链表
//**主和分支通道都需要纸币通过传感器的节点记录，然后插入链表**



%% FlowSelectorCtrl
	主链表空
		return
	**WALK_MAIN_LIST_FROM_FIRST** 
		for[0，4） %%



FlowGetBranNodeList//获取当前流程钞口分支通道节点列表
	sBranTrackNodeList



## 拨叉数据 
FlowSelectorCtrl
	const SEL_CONFIG  \***SelConfig** = Paper->mInfoOfNode[NodeSnOfHead]. **mTrackNode->mSelConfig**[SelConfigIndex];
	AppSelectorCtrl (**SelConfig**)

==MountSelectorToNode== (**SelConfig**, FlowNodeList)
	for (j: 0,mTrackNodeNum)
		TrackNodeId = FlowNodeList->**mNodeArray**[j]
		**if mTriggerNodeId == TrackNodeId**
			for (k: 0,NODE_SEL_CONFIG_MAX)
				T**rackNode->==mSelConfig==**[k] = **SelConfig**; ^9zjdn5

DestGateSelMount (FlowNodeList)
	FOR_LOOP_ALL_GATE (GateNo)
		SelConfig = **FlowGetGateSelConfig** (GateNo)
		MountSelectorToNode (SelConfig，FlowNodeList)

AppMainAndBranTrackNodeListInit
	FOR_LOOP_ALL_GATE (SrcGateNo)
		**FlowNodeListInitConn**
		DestGateSelMount (&**gFlowTrackNodeListTable**[SrcGateNo])



Flow_FromPointToPoint 纸币流程
	case FLOW_TRANS_STATE_START:
		AppMainAndBranTrackNodeListInit


### 搜索 gFlowTrackNodeListTable
初始化在：
FlowNodeListInitConn//初始化主通道传感器节点列表.根据流程编号获取相应传感器节点列表，并计算节点数量
	 MainFlowNodeList = &gFlowTrackNodeListTable[GateNo];

### SelConfig 的初始化
FlowGetGateSelConfig（GateNo）
	return **gFlowModuleInfo**[FlowID]. mGateSelConfigArray[GateNo];
	
.mGateSelConfigArray  = sFlowDestSelConfigTable[path],

## 拨叉控制总结
![[分支通道#^9zjdn5]]
应该是经过每个节点都有固定的拨叉动作（因为拨叉初始化每个节点对应一套拨叉参数）如下解释
	SelConfig 对应 gSelConfigSEL_LBCM1 对应的初始化只有 SEL_CONFIG_STATIC 
不同的流程****不一样导致拨叉的