---
up:
  - "[[../moc/MocRtos]]"
---
# 1
获取实例
经理模块内编写函数

# 回调 
**初始化中断的时候就是中断发生时的处理函数**
## 1
​AppSensorCallBackSet（void (\*CallBack)）
	mCallBack = CallBack


## 2
InterruptIdInstall(GPIO_INTERRUPT_ID,(Xil_ExceptionHandler)XGpioPs_IntrHandler,&Gpio);
XGpioPs_SetCallbackHandler(&Gpio, (void \*)&Gpio, FuncPtr);
# 中断
# 1
InterruptIdInstall(MOTOTIMER_INT_IRQ_ID,(Xil_ExceptionHandler)MotoTimerIrqHandle,NULL);
# 2
InterruptIdInstall(TIMER_INT_IRQ_ID,(Xil_ExceptionHandler)FuncPtr,&TimerInstance);
1ms PIT定时器超时中断回调函数用的

# 3
SET_FUNC_POINTER(sDrvEncoderConfig.mptCallBack, CallBack);

DrvFpgaInterruptInit(sDrvEncoderConfig.mFpgaIntId, DrvEncoderInterruptCallBack);
	InterruptIdInstall(sDrvFpgaIntId2GlobalId[IntId], FuncPtr, (void *)IntId);
# 事件信号

事件接受时可以选择清事件，而信号一定会清
事件可以多对一

## 信号量的实现 
### rt_sem_init
### rt_sem_take（rt_sem_t sem, rt_int32_t time）
关中断
if (sem->value > 0)
	==sem->value--==
	开中断
else 
	if time == 0
		开中断 
	else
		当前上下文检查
		信号量不可用，推送到 Suspend 列表
		获取当前线程 
		挂起线程
		if time>0
			rt_timer_start
		开中断
		rt_schedule
### rt_sem_release
关中断
if 挂起列表不为空
	恢复挂起线程
	need_schedule= 1
else
	sem->value ++
开中断 
if need_schedule
	rt_schedule
## 事件的实现
### rt_event_send（event, set）
关中断
==event->set |= set;==
if 挂起列表不为空
	/\*搜索线程列表以恢复线程\*/
	n = event->parent.suspend_thread.next
	while (n != &(event->parent.suspend_thread))
		thread = rt_list_entry(n, struct rt_thread, tlist);
		if (thread->**event_info** & RT_EVENT_FLAG_AND)
		else if (thread->event_info & RT_EVENT_FLAG_OR)
		n = n->next;
		/\*满足条件恢复线程\*/
		if (status == RT_EOK)
			clear event
			恢复线程和线程列表分组
			need_schedule =1
开中断
if (need_schedule == RT_TRUE)
        rt_schedule


### rt_event_recv (event, set,option, timeout,\*recved)
## 邮箱
OsMsgInit(&sOsMsgTaskCtrlRCB[RcbId], sRCBMsgName[RcbId], &sOsPoolMsgTaskCtrlRCB[RcbId], sizeof(GATE_HANDLE_MSG), sizeof(sOsPoolMsgTaskCtrlRCB) / RCB_NUM, OS_IPC_FLAG_FIFO);
### rt_mq_init (mq,\*name,\*msgpool,msg_size,pool_size, flag)
mq->parent.parent.flag = flag;
mq->msg_pool = msgpool;
mq->msg_size = RT_ALIGN(**msg_size**, RT_ALIGN_SIZE);
mq->max_msgs = **pool_size** / (mq->msg_size + sizeof(struct rt_mq_message));

for (temp 0,mq->max_msgs)
	head =
	head->next = 
	mq->msg_queue_free = head;
### rt_mq_send （rt_mq_t mq，\*buffer, size）
关中断 
msg = (struct rt_mq_message*)mq->msg_queue_free;
if (msg == RT_NULL)
	开中断
mq->msg_queue_free = msg->next;
开中断
msg->next = RT_NULL
rt_memcpy(msg + 1, buffer, size)//复制缓存
关中断
/\*link msg to message queue\*/
.


**其实信号量事件就是类似全局变量或是叫标志**，用来同步
# 缓存 fifo
# 2
宏展开，结构体展开