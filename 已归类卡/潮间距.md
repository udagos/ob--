---
up:
  - "[[moc循环箱]]"
---
#ed
[moc 日志](../moc/moc%20日志.md)
## 主通道
gap: mPaperSpacing * mMainMotorResolution
len: mPaperLen * mMainMotorResolution


mPaperSpacing = PaperSpacingCalc (Paper, PrevPaper)
### 计算潮间距
Paper != STD_NULL
	纸头位置传感器节点顺序号
	上一张纸尾位置传感器节点顺序号
	while NodeSn < PrevTailNodeSn
		Distance += mInfoOfNode[NodeSn++]. mDis 2 NextNode
	Distance += pre. mInfoOfNode[PrevTailNodeSn]. mTailPosMin 
	Distance -= mInfoOfNode[HeadNodeSn]. mHeadPosMax
else
	while NodeSn < PrevTailNodeSn
		Distance += mInfoOfNode[NodeSn++]. mDis 2 NextNode
	Distance += pre. mInfoOfNode[PrevTailNodeSn]. mTailPosMin 
	 Distance -= (S 16) PaperOffsetCalcBySlope (PrevPaper->mInfoOfNode[PrevTailNodeSn]. mTrackNode, PrevPaper->mSlopeLast, Resolution);


### 计算钞长
for 1，mSensorNum
	mSensorTable[i]. mHeadPos > mSensorTable[i]. mTailPos 
		mSensorTable[i]. mPaperLen = mHeadPos - mTailPos 
	mPaperLen < mSensorTable[i]. mPaperLen
		mPaperLen = mSensorTable[i]. mPaperLen]





# 纸币流程状态机-working
## MainAndDestBranPaperManage
### PaperPosUpdate//更新纸币位置 
//还有一个分支通道纸币更新在 pagemanagein 和 out
循环遍历链表
	PaperPosUpdate_Head//纸头计数
	CurNodeInfo->mPaperSpacing = **PaperSpacingCalc**(Paper, PrevPaper);
	PaperPosUpdate_Tail
### OutCashControl ();//根据斜度和钞间距进行拒钞
#### PaperSpacingJudge (Paper); /\*间距只需在纸头传感器位置检测一次*/
##### BetweenNoteSpacingCheck (Paper, PrevPaper); 钞间距判断