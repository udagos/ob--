---
up:
  - "[[../moc/work]]"
---
[moc 日志](../moc/moc%20日志.md)
#ed 

#kan
 ErrCode = MakeErrCodeGate (ptCtrl->mptGateHandleManager->mGateId, ERR_GATE_EMPTY);


FlowErrInfoUpdate 只有 update 的才能停流程




出钞
	空
		GateWorkFsmSet(ptHandleManager, GATE_HANDLE_FSM_STOP);


MakeErrCodeGate


bv
gFlowConfig.mErrCode






GateWorkErrHandle 不会停循环钞箱，会上传错误码，
是不是将来 GATE_HANDLE_FSM_ERR 有停的

吧 motorid 传进函数做ptHandleManager转换
void GateWorkFsmSet(GATE_HANDLE_MANAGER *ptHandleManager, GATE_HANDLE_FSM AimFsm)





ThickModule
测厚模块自检 
	Average = MagneThickCalculateAverage (1, (U 32) DataAddr, AverageLength);
	if(Average < (THICKDATA_DEST_AVERAGE - THICKDAT_DEST_OFFSET))
		mThickCorrectionFlag = 1
		**mThickAverage** = Average
		if (ChangeCnt < THICKSENSOR_CHANGENUM)
			“”**Thick correct**，Register BaseValue，**mThickDacValue**[Channel]，Average“”//该 da 的值是 pwm 校验得到的？
			Average = THICKDATA_DEST_AVERAGE - Average;
			Average = Average / THICK_ADCVALUE_OFFSET;
			Average += 1;
			if (Average < mThickDacValue）
				**mThickDacValue**[Channel] = 
				mThickDacValue[Channel] - Average;
			用该值配置 DAC
		else
			“”Thick error
	if (Average > (THICKDATA_DEST_AVERAGE + THICKDAT_DEST_OFFSET))
	else
		“”**Thick OK**


pwm 校验 
while（1）
	Value =
	Average = MagneThickCalculateAverage(1,(U32)DataAddr,AverageLength);
	“”**Thick correct** Register  BaseValue  “”Value  Average
	if(Average < (THICKDATA_DEST_AVERAGE - THICKDAT_DEST_OFFSET))
	else if(Average > (THICKDATA_DEST_AVERAGE + THICKDAT_DEST_OFFSET))
	else   
		**mThickAverage**[Channel] = Average;
		break;
**mThickDacValue**[Channel] = ValueBak;
AppEepMultiWrite（mThickDacValue）





