---
up:
  - "[[moc模块]]"
---
[moc 日志](../moc/moc%20日志.md)
#ed 


## 传感器防光
if mPowerState//发光管状态 
	mAdvalOn = mAdValueGet
	mPowerCtrl（STD_DISABLE）
	mPowerState = STD_FALSE、
	return
else
	mAdvalOff = mAdValueGet 
	mPowerCtrl（STD_ENABLE）
	mPowerState = STD_TRUE;
Adval = STD_ABS（mAdvalOn ，mAdvalOff）
if Adval >= mJudgeHighVal//off 状态判断阈值 
	mCurState = OPTICAL_SENSOR_STATE_ON
else if Adval <= mJudgeLowVal
	mCurState = OPTICAL_SENSOR_STATE_OFF
## 传感器去抖
if mCurState == mLastState
	mStateCnt ++
else 
	mStateCnt  =1
	mLastState  =mCurState 
if mStateCnt >= mStateConfirmCnt
	if mStateConfirm != mCurState 
		mStateConfirm = mCurState 
		mCallBack


Sensor->mCallBack         = SensorConfig->mCallBack;

SensorConfig->mCallBack         = SensorChangeCallback;

SensorChangeCallback
	""**SnsChg** mName  mS
	
	tateConfirm mAdValueGet

## 传感器 pwm 校验 
![[Pasted image 20240724085956.png|650]]
![[Pasted image 20240724090029.png|641]]
![[Pasted image 20240724090053.png|675]]
![[Pasted image 20240724091311.png|775]]
为多路选通器
![[Pasted image 20240724091401.png|675]]
==pwm 让发射 UCS 03_D 稳定，UCS 03_C 即 UCS 03_AD 给多选器再给 adc 转换==
==UCS 03_C  接受==
==UCS 03_D 发射==

pwm 校验应该是让发光管固定电压

传感器 pwm 校验（Mode）
	InitSensorCorrEnableCurr（Mode）//初始化本次传感器是否校正
	MakeSensorCorrTable//生成本次传感器校正表
	PwmCorrTar = AppEepRead (EEP_ADDR_SNS_PWM_TAR);//读取校正目标值
	SensorPwmVerify（mode, PwmCorrTar）

InitSensorCorrEnableCurr Mode
	所有传感器 
		mVerifyEnableCurr = mVerifyEnable
	if mode == SENSOR_CORRMODE_CMD
		InitSensorCorrEnableCurrByGate 1
	else
		InitSensorCorrEnableCurrByGate 0 xff


MakeSensorCorrTable
	所有传感器 SnsCnt
		if mVerifyEnableCurr
			mptSnsCorrTable[mSnsCorrTableNum++] = &sSensorManager[SnsCnt]


static SENSOR_MANAGER sSensorManager[SENSOR_NUM] = {
SENSOR_MANAGER_INIT_EEP (    NFS 03,  MAIN,   UFPGA,  SNS_PATH_UPPER,     SENSOR_PWM_DEF_ALL,         SENSOR_THRE_DEF_ALL),
SENSOR_MANAGER_INIT_NOEEP (),
...}


没有从 ee 加载的传感器怎么办 
ee 映射到内存的方法
开启缓存
指令



# 传感器集成测试
Sensor_C_Pwm: 传感器==发光管Pwm控制信号==
Sensor_C_IO: 传感器==发光管IO控制信号==
Sensor_R: 传感器==接收管信号==


==pwm不检验ras3 s4==

==io关闭打开指的是什么, 发射管的关闭打开==


## 电路
### nfu02
==nfu02_IO  输出到7010==


![](../fujian/Pasted%20image%2020250304212339.png)
![](../fujian/Pasted%20image%2020250304212422.png)
![](../fujian/Pasted%20image%2020250304212455.png)
![](../fujian/Pasted%20image%2020250304212515.png)


### lau1
## ==代码== 
AppSensorVerify
	AppSensorPwmVerify
		InitSensorCorrEnableCurr
			 sSensorManager[SnsCnt].mCorrPara.mPwmCorr.**mVerifyEnableCurr**
			  = sSensorManager[SnsCnt].mCorrPara.mPwmCorr.**mVerifyEnable**;
		**MakeSensorCorrTable**//生成本次传感器校正表
			if (sSensorManager[SnsCnt]. mCorrPara. mPwmCorr. **mVerifyEnableCurr**) {
				 gSysSensorInfo. **mptSnsCorrTable**[gSysSensorInfo. mSnsCorrTableNum++] = &sSensorManager[SnsCnt];
			}
		SensorPwmVerify
	TrackSensorThresholdVerify




### pwm

SensorPwmCorrParaInit//pwm校正参数初始化



SensorPwmVerify（**==PwmCorrTar==**）
	SnsNum = gSysSensorInfo.mSnsCorrTableNum;//校验表大小
	while
		SensorPwmCorrPwmSet（SnsNum）//传感器PWM校正PWM值设置
		SensorPwmCorrGetCurrentAdData//**当前pwm下的ad值**
		for（0，SnsNum）
			**==CurrentAD==** = SensorCorrGetMaxADProbability//获得最大概率ad值
			SensorPwmCorrPwmChangeCalc（==CurrentAD, PwmTarVal==）//==传感器PWM校正PWM更改==




SensorPwmCorrGetCurrentAdData（SnsNum）
	for (0,1000)
		for (0, SnsNum)
			mSensorLevalNum[AppSensorGetAd
			(ptSnsCorrManagerTbl[SnsIdCnt]->mIdManager.mSensorId)]++;
			//mSensorLevalNum即为计数


SensorPwmCorrPwmChangeCalc
	==即是当前ad与目标ad比较，来改变pwm==

### 阈值

SENSOR_THRESHOLD_VALUE 39//0.5v固定阈值
SENSOR_THRESHOLD_LOW_PRISM//导光条默认低阈值为1V
SENSOR_THRESHOLD_HIGH_MIN//模块传感器默认低阈值

TrackSensorThresholdVerify
	SensorAd = AppSensorGetAd(SensorId);
	if==（ras3 ras4）==
		SensorAd = 获取十次平均值
		AdValueH = SensorAd+==SENSOR_THRESHOLD_VALUE==;
		AdValueL = SensorAd+SENSOR_THRESHOLD_VALUE;
	else
		if(SensorAd + ==SENSOR_THRESHOLD_LOW_PRISM== > SENSOR_THRESHOLD_HIGH_LIMIT)
		else if(SensorAd + SENSOR_THRESHOLD_LOW_PRISM > SENSOR_THRESHOLD_HIGH_PRISM)
		 else if(SensorAd + SENSOR_THRESHOLD_LOW_PRISM < SENSOR_THRESHOLD_HIGH_MIN)
		 else


## 图
![](../fujian/Pasted%20image%2020250304213518.png)
![](../fujian/Pasted%20image%2020250304213529.png)