---
up:
  - "[[../moc/代码]]"
---
#kan 
OsSemPost(ptCtrl->mptGateHandleManager->mptMotorSem);

ptCtrl = &sRcbCtrl[RcbId];

OS_SEM \*mptMotorSem;

在结构体里定义，所有没有定义为数组，每个钞箱都持有一个该信号量



不然就是这种
static OS_MSG sOsMsgTaskCtrlRCB[RCB_NUM];
![[Pasted image 20240708181431.png]]



事件要用数组就不大对了, 那么多钞箱分开也麻烦，


**定义的数据包括结构体数组基本都是 static，除非用获取实例这种函数方式**




static OS_MAIL sOsMailErrCode; **是邮箱，但不是数组**
OsMailInit(&sOsMailErrCode, "OsMailErrcode", &sOsPoolMailErrCode[0], sizeof(sOsPoolMailErrCode) / 4, OS_IPC_FLAG_FIFO);


gCisAutoCorrectState
gCisLightSwitch
gSysConfigInfo   **全局的**

DC_MANAGER \*MidGetDcMotorInMainManagerInstance (DCMOTOR_INMAIN_INDEX MotorId)
{
return &sDcMotorInMainManager[MotorId];
}
**static 的，但通过函数访问**


void PidCorrect(MOTOR_INDEX MotorId, U32 Speed, BOOL Dir)
	DCMotorId = GetDcMotorInMainByMotorId (MotorId);
	RcbId = Change2RcbId (MotorId);
	ptManager = MidGetDcMotorInMainManagerInstance (DCMotorId);
从这里可以看出，电机无法获取实时的哪个。所以传感器线程判断哪个钞箱传递定义无意义，MidGetDcMotorInMainManagerInstance 传递仍需要id


**rtt 中信号量可以阻塞线程，可以主动调度**
**挂起状态下不参与调度** 



多个钞箱
OsEventSend(&gOsEvtInitFinish, **sRcbThreadInitFlg**[RcbId]);
if((OsEventGet(&**gOsEvtInitFinish**) & **EVENT_COMBINE_THREAD_INIT_FINISH**) == EVENT_COMBINE_THREAD_INIT_FINISH)



两个线程不同时写可以用全局变量
6 个钞箱要有 6 个全局变量，而事件只需要一个就可以了

事件、邮件可以阻塞，需要函数栈
事件可以设置不对标志减，信号不可以 
事件定义基本没有数组，邮件定义要给一个数组（给一个指针也不能是野指针）


CAN_MESSAGE_FORMAT
按位定义

(OsEventGet(&gOsEvtSensorAvailable) & STD_BITX(Group)
检查每一位



**最大长度持续有钞是如何停下来的**
FlowErrInfoUpdate   
	SYS_ERR_CLASS; 严重错误类别
FlowErrInfoGet
	如果是严重错误类别
	返回 
gatemanagerctrl (errstop)

好像严重不严重都停止钞口

**严重类别判断**
SysErrInfoUpdate（**errclass**）

if ErrClass  == CriticalErrArray
	IsCriticalErr = 1



ErrCodeUnion.mErrCode = MainAndDestBranPaperManage(TransferConfig);




FlowErrInfoUpdate(ErrClass, ErrCode) 一层层向上传递



FlowErrInfoUpdate(SYS_ERR_CLASS_JAM, ErrCodeUnion.mErrCode);
BvErrInfoUpdate(BvErrCodeClassify(ErrorCode), ErrorCode);
	这个从 errcode 中提取，但流程中没有，应该需要改