---
up:
  - "[[../moc/work]]"
---
#kan



**gateinlet. c**
for
	switch mGateWorkFsm
	case  GATE_HANDLE_FSM_IDLE:
		 OsMsgWaitForever (mptCtrlMsg, &TaskMsg)
		 TaskMsg.mTask == GATE_HANDLE_TASK_MAKEREADY
			 **GateWorkFsmSet** GATE_HANDLE_FSM_READYING
		mTask == GATE_HANDLE_TASK_INIT
		mTask == GATE_HANDLE_TASK_CLEARERR


GateWorkHandle (GateCtrl)
	GateHandleMsg.mTask = (U8)GateCtrl
	OsMsgSend (mptCtrlMsg, &GateHandleMsg,)

GateRcbInitHandle 或 GateModuleInitHandle
	**GateWorkHandle** (GATE_HANDLE_TASK_INIT)//更改入钞口状态也就也循环钞箱

GateModuleReadyHandle
	GateWorkHandle (GATE_HANDLE_TASK_MAKEREADY)

在超口管理线程的 
GATE_FSM_READY 中，
	**GateModuleReadyHandle**
GATE_FSM_INIT
	mGateNo == GATE_RCB
		GateRcbInitHandle 
	else
		GateModuleInitHandle
		
超口管理状态切换是 
**GateManagerCtrl**
	OsMsgSend (&sOsMsgGateManagerTask


纸币流程实现
FLOW_TRANS_STATE_INIT//在流程控制 start，这个状态控制自己里面控制
	GateManagerCtrl GATE_CTRL_MAKERDY

流程控制任务 
FLOW_STATE_INIT
	Flow_SystemInit
	GateManagerCtrl  GATE_CTRL_INIT, GATE_RCB


Flow_SystemInit
	FlowStateErr = ERROR_FLOW_STATE_INIT
	for
		switch  FlowStateErr 
		ERROR_FLOW_STATE_INIT
			if (IsInitCmd)
				GateManagerCtrl (GATE_CTRL_INIT, GATE_NULL
			else
				GateManagerCtrl (GATE_CTRL_INIT, GATE_NULL







