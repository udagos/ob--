---
up:
  - "[[mocWorkCode]]"
---
#ed
# 指令校正 CisCorrectMain
CisOffsetCorrect
	for（Cnt 0，3）
		CisOffsetCorrectSingle
		if **sCorrectOffsetValue**.mSet. mCurValue <= **sCorrectOffsetValueStd**.mSet. mCurValue\*103/100
		&&mSet. mCurValue >= mSet. mCurValue\*97/100
			IsFinish[CisId] = STD_TRUE;
		if (Cnt >= 1)
			for (0, cnt)
				mSet. mCurValue <= mSet. mCurValue\*102/100
				&&mSet. mCurValue >= mSet. mCurValue\*98/100
					sCorrectOffsetValueStd. mSet. mCurValue = 
					sCorrectOffsetValue.mSet.mCurValue
	""%% CisOffsetStdRefLast %% ""sCorrectOffsetValueStd. mSet. mCurValue
	sCorrectOffsetValue.mSet.mCurValue
CisCorretRealTimeDark
CisLightCorrectRGBBalance
CisLightCorrect



## CisOffsetCorrectSingle
CisOffsetCorrectSingle 
	CisLightClose
	CisScanXLength//扫描一段时间
	CalcDataForOffset (pStartAddr,sCorrectOffsetValue.**mGray**)//统计偏移量数据
	CisCorrectOffsetCalcResult（sCorrectOffsetValue）
	%% Cis1OffsetStaticValue   mGray.mMax %%
	%% Cis 1 OffsetCalc   mSet. mCurValue %%
	CisCorrectOffsetCorrectStepByStep//CisCorrectOffsetCalcResult 更新给 8233 偏差值后再次扫描看
	%% Cis1OffsetEnd    mSet.mCurValue %%



sGrayHist[256]
### CalcDataForOffset （pData，pValue）
CalcDataForOffset（pStartAddr，sCorrectOffsetValue.mGray）
	CalcHistogramFromRectMemEx (pData，**sGrayHist**)//计算单输出图像灰度直方图, 即找到图像中每个灰度对应的数量
	 SearchMaxFromHist   （CORRECT_ROWS_NEED_OFFSET\*2， &pValue->mMax）
		IsMaxErr = 1//**如果找不到进入**
		pValue->mMax = 0//最大值的索引，也就是灰度
	 pValue->mMin
		IsMinErr = 1
		pValue->mMix = 0
	最大最小概率值都存在
		return ERR_NO_ERROR
SearchMaxCountFromHist（&MaxGrayValue）//寻找极大概率值，返回最大的 sGrayHist 索引也就是灰度值
if (IsMaxErr)
	pValue->mMax = MaxGrayValue;//极大概率值替代极大值
极大概率值替代极小值
return ERR_NO_ERROR;


CalcHistogramFromRectMemEx
	得到 sGrayHist 数组

### SearchMaxFromHist (max_ref,\*pmax_index)
SearchMaxFromHist
	for (gray_index 255,2)
		if [gray_index] >max_ref && [gray_index -1] > max_ref/2
		&& [gray_index -2] > max_ref/2
			索引赋值
			return
	for 
		if [gray_index] > 250
			索引赋值
			return
	for 
		if [gray_index] > max_ref && [gray_index -1] > max_ref/2
			索引赋值
			return
	return ERR_FAIL
### CisCorrectOffsetCalcResult  （pValue）
CisCorrectOffsetCalcResult（sCorrectOffsetValue）
	pValue->**mGray. mMin** > pValue->mSet. mLowRef//如果上一步计算出最小值大于设定
		pValue->**mSet. mCurValue** = pValue->mStdSet - 过高的部分
	mOffset = mSet. mCurValue
	DevCisOffsetSet（mSet. mCurValue）//**发送给 8233**


### CisCorrectOffsetCorrectStepByStep 
while (1)
	**DevCisOffsetSet** (mSet.mCurValue)
	CisScanXLength
	CalcDataForOffset
	%% Cis1OffsetStaticValueStep   mGray.mMax %%
	if (!**UpdateAndCalcOffsetValue**)
		bFinish
	if bFinish 
		break
#### UpdateAndCalcOffsetValue 
mGray.mMin > 0
	mGray. mMin <= 0 x 03 
		return ERR_NO_ERROR
		...
mSet.mPreValue = mSet.mCurValue