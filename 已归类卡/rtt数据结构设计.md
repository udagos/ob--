---
up:
  - "[[../moc/MocRtos]]"
---
[moc  uml](../moc/moc%20%20uml.md)
#ed 


## pin 设备
注册实际上就也实例化类
### drive_pin  驱动层
![[Pasted image 20240817104624.png]]
**为什么在设备基类的基础上还要早加 gpio 和 pin 因为不够或是需要拆解**
比如上层只关注 led1，但驱动层要制定是哪个引脚对应，device 相当于父类
![[Pasted image 20240817111655.png]]
对设备实例化
![[Pasted image 20240817092512.png]]
![[Pasted image 20240817092521.png]]


eio_pin_data_t \*driver_data = (eio_pin_data_t \*) me->user_data; 为了从设备里获取驱动的数据，即上层获取下层的数据。
\_get_status 从硬件获取数据 HAL_GPIO_ReadPin，应用层调用该设备层接口 eio_pin_get_status 即驱动层的实现返回给应用层


上层是设备层，下层是驱动层。下层的驱动层使用上层的接口 register

这些_init 接口函数指针传递给上层，为了上层使用

### eio_pin . h 设备层 
![[Pasted image 20240817092631.png]]
**user_data 用来获取驱动曾数据。mode 不是驱动曾来的。status 是通过 ops 从驱动曾获取的**

eio_pin.c
用实例化函数，外界模块调用函数进行实例化，而不是我们工程里一样外部模块获取实例，然后对该指针进行实例化
![[Pasted image 20240817092656.png]]

### 总结

**这样完成硬件与设备的剥离, 把硬件相关的程序放到驱动层，而抽象层即设备层的接口不会变。**
**设备层的上层接口给应用层调用，获取修改硬件数据；下层接口用于驱动层对器实例化**

 HAL_GPIO_ReadPin 这这里对应驱动层跟下面一层了，



驱动层中的 INIT_EXPORT(eio_pin_dirver_init, 1); 不太理解？


这一节和上一节应用层唯一的不同就是设备的获取，之前直接是一个全局变量，对他进行操作的 
![[Pasted image 20240817102821.png]]

一个和不足时 find 实现是用的链表实现的，如果设备太多可以用哈希表实现


设备层仅仅定义了设备的数据接口以及接口，接口的具体定义也没有

怎么样建立一个 pin 设备，通过对象的接口即 ops 把驱动层和设备层剥离
## pwm 设备
### 设备
pwm 设备的数据结构
![[Pasted image 20240817110327.png]]
数据结构
对象接口
数据
名字（用来寻找该设备）
占空比（比 pin 设备多出来的）

对象的接口 
![[Pasted image 20240817110508.png]]


如何操作硬件通过对象的接口（这里操作硬件是设置 pwm），可以看到驱动程序



### 驱动
![[Pasted image 20240817110756.png]]
核心还是数据结构
对应哪一个定时器
对应哪一个通道
==驱动的数据结构由硬件决定==

之前 pin 设备的驱动数据结构
![[Pasted image 20240817110849.png]]
驱动数据对应的那个设备
名字
真实的物理口
物理口上的哪个引脚


![[Pasted image 20240817111105.png]]
上面两个是设备中的约束
下面两个是私有的函数 


数据和行为进行剥离
![[Pasted image 20240817135643.png]]


## 设备基类
观察 pwm 和 pin 设备发现很多相似之处

接口由 open 和 close 对于 linux 由这个概念，对于嵌入式没有，但要跨平台所以也留着
案例还应该有 ctrl，但往往增加复杂度不要

![[Pasted image 20240817143502.png]]
![[Pasted image 20240817143451.png]]
多了一个是否冲突和打开次数的定义
对于 spi 设备多从所以没有冲突 

这里的 user_data 设备给驱动用，所以时驱动、硬件的数据，他们用什么数据结构设备层不知道所以用 c 语言的泛型 void


断言是每个微小的错误强制性的变为崩溃性的错误，让微小错误能被发现就是防御式编程的意义
## 设备子类
在基类的基础上继承得到 pin 设备和 pwm 设备

![[Pasted image 20240817144816.png]]
**把基类放在收地址，这样通过强转既可以访问子类又可以访问基类**
除了基类还有
子类专属的接口
私有的数据


![[Pasted image 20240817145854.png]]
pin 都不用
虽然 pin 设备的 set get 可以用 read 和 write 但并不好用，也没必要抽象层级那么高