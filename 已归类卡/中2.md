---
up:
  - "[[../moc/mocwork]]"
---
# 1
FLOW_TRANS_STATE_WORKING
	for
		MainAndDestBranPaperManage
			UpdateSensorState
			GateManagerCheckInFlow//与主电机码盘同步
			PaperPosUpdate
			PaperEnterMainTrackCheck
			FlowSelectorCtrl
			BvModuleManageInFLow//bv触发
				BvDataTrigHandle
			AllDestGatePaperManage
			PaperEnterDestBranHandle
		FlowTransIsFinished//==通道里没有纸张==
			break     


## PaperPosUpdate
从最先进入遍历（paper）
	for（nodesn从最大开始）
		CurNodeInfo（**paper，nodesn**）
		如果前一张未经过该传感器//从第二章开始，第二章节点遍历到第一张还未经过的点是contine
			contine
		如果 （NodeSn + 2) < FlowGetTrackNodeNum && **纸尾通过（Paper，nodesn+2）**
			break//比如node为ums01，纸尾刚过bvs03
		PaperPosUpdate_Head（**CurNodeInfo**）
		if((CurNodeInfo->mHeadPosMin >= HoleLenMax + 1) && !CurNodeInfo->mSpacingAndSlopeIsValid)
			if(PrevPaper != STD_NULL)
				PaperSpacingCalc (Paper, PrevPaper)
		PaperPosUpdate_Tail（**CurNodeInfo**）
		 if((CurNodeInfo->mTailPosMin >= lTailPosMin)
			 && (! CurNodeInfo->mTrackNode->mHasCash)
			  && (CurNodeInfo->mPaperLen == 0))
			PaperConnectLengthCalc

### PaperPosUpdate_Head (**InfoOfNode**) 
for (i: 0,)
	SensorState = AppSensorStateGet（InfoOfNode->mTrackNode->mConst->mSensorIdTable[i]）
	if(SensorState == SENSOR_HAS_CASH)
				**InfoOfNode**->mSensorTable[i].mHeadPos++;

### ==PaperPosUpdate_Tail==
...
for (节点使用的传感器数量)
	if((SensorState == SENSOR_HAS_CASH)
		InfoOfNode->mSensorTable[i].mTailPos = 0;
	 else
		 InfoOfNode->mSensorTable[i]. **mTailPos**++;
PaperPosMaxMinUpdate_Tail
	**mTailPosMax** = mTailPos
	mTailPosMin = mTailPos
## AllDestGatePaperManage
BranPaperPosUpdate（GateNo）
	WALK_BRAN_LIST_FROM_FIRST
		for (NodeSn)
			PaperPosUpdate_Head
			if
				prepaper！=null
					潮间距计算
			PaperPosUpdate_Tail
			注释：**钞尾触发的潮间距计算在纸尾计数后**
			 if((InfoOfNode->mTailPosMin >= lTailPosMin)
			 && (!InfoOfNode->mTrackNode->mHasCash)
			 && (InfoOfNode->mPaperLen == 0))
				 **BranPaper->mNodeSnOfTail = NodeSn;**