---
up:
  - "[[../moc/work]]"
---


潮间距计算是上涨纸尾到这张的纸头
潮间距果断巨潮
	判断这张和上一张的潮间距
	巨潮上一张
		把上一张目的超口改为据超口




# 计算钞间距
## 直接两张纸头纸尾相减DisSpace
DisSpace=PrevPaper->mInfoOfNode\[==Paper->mNodeSnOfHead==]. mTailPosMin
-Paper->mInfoOfNode\[Paper->mNodeSnOfHead]. mHeadPosMax;
## 加上两张重点节点的距离，减小走步误差Distance
if(Paper != STD_NULL)//当前纸张已插入主链表
	HeadNodeSn = Paper->mNodeSnOfHead
	PrevTailNodeSn = ==PrevPaper->mNodeSnOfTail==;
	Distance += (S16)Paper->mInfoOfNode[NodeSn++].mDis2NextNode;
	Distance += (S16)PrevPaper->mInfoOfNode\[==PrevTailNodeSn==].mTailPosMin;
	Distance -= (S16)Paper->mInfoOfNode\[HeadNodeSn].mHeadPosMax;