---
up:
  - "[[../moc/work|work]]"
---
[moc 日志](../moc/moc%20日志.md)
#ed 

#kan 
现在校正用什么纸张
所有样张
两两组合样张
没有校正
最厚单张和双张是用什么纸张


4cm

## 测厚集成
pwm 校验
无纸 ad：Thick correct
	对应 da

重张阈值
6 路阈值怎么判断逻辑


## 测厚阈值 ThickSensorCorrectionFun
mNoCashAverage//无纸张均值
Data =有钞票均值
if Data >= mNoCashAverage + CASH_THRESHOLD_MIN_OFFSET//有钞无钞插值最小值 320mv
	mCashAverage = Data
else
	if  data >= mNoCashAverage
		Data = Data - mNoCashAverage
	else 
		data = 0
	""Paper's amplitude is smaller than CASH_THRESHOLD_MIN_OFFSET, data
if mCashAverage >= mNoCashAverage + CASH_THRESHOLD_MIN_OFFSET
	**mAmplitudesThreshold** = ((mCashAverage - mNoCashAverage)\*100)/CORRECT_PAPER_OFFSET//mAmplitudesThreshold 有纸无纸赋值
	""Paper's Correct Is Ok, mAmplitudesThreshold
	“”NopaperAve  PaperAve  ，mNoCashAverage  mCashAverage

GenerateCorrectRatio//计算校正系数
	if mAmplitudesThreshold != 0
		mCurrRatio = （gThickSortList. mCorrect. mTarget * gThickSortList. mCorrect. mTimes）/mAmplitudesThreshold
	else
		mCurrRatio[Channel] = gThickSortList.mCorrect.mTimes;
	gThickSort. mCorrect. mAdjustRatio[Channel] = 
	. mCurrRatio[Channel] * gThickSortList. mCorrect. mTimes) / 
	gThickSortList. mCorrect. mRatio[Channel];


## 确定纸张有无
CashResultStateQuery
	mCurrRatio
	mResult.mAllAverage= (AllSum / AllDot)


ThickDataHandle
	ThickStateQuery//测厚结果查询
		CashResultStateQuery//确定纸张有无
		确定纸币与字典库均值差异
		修正差值
		厚度补偿
		确定差值是否有效
		算法结果转换
		确定厚度异常结果
		确定测厚识别结果
	if **mResult** & THICK_RESULT_DOUBLE
		""Thickness Result Is Double
		Result = RESULT_CODE_THICK_DOUBLE
### 测厚识别结果
RecognitionDoubleResult(pThick) != 0
	pThick->**mResult** |= THICK_RESULT_DOUBL
	mATMErrorCode |= THICK_ERRORCODE_DOUBLE
if mResult.mAllAverage < mFeature.mAllAverage\*PAPERAVER_MINOFFSET/100
//如果幅值偏低，则以重张退朝



**RecognitionDoubleResult**  
	统计纵向长度是否满足条件
	Result = RecognitionTapeLengthResult(
	DoubleResult |= 0x02;
	统计面积是否满足条件
	Result = RecognitionTapeAreaResult
	DoubleResult |= 0 x 04;
	Result = 0;
	if(   ((DoubleResult & 0x01) != 0)
	   && ((DoubleResult & 0x02) != 0)
	   && ((DoubleResult & 0x04) != 0))
	{
		**Result** |= 0x01;
	}
	mDoubleIndex != 0//重张补偿系数不为 0
		//整体赋值偏大
		mResult.mAllAverage > (gThickDicIndex. mAllAverage \* mLevel. mGradLevel/256 +mAllAverage) \*mDoubleIndex/1000
			Result |= 0 x 01;
	return result
## 测厚线程
mThickCollectMode == THICK_COLLECT_REF//测厚阈值校正
	ThickSensorCorrectionFun
else
	ThickDataHandle
	