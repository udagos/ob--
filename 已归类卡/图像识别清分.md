---
up:
  - "[[mocbv]]"
---


# 1  ImageRecognitionSort 图像识别清分




GetImageBandMutipyValue//每张纸币白基准图像亮度系数计算
GeneratePaperImageAngle//获取图像倾斜角度

BanknoteClassify（SortResule[0]）//api 面值面向判断

CheckBanknoteGenuine (SortResule[0])//api真伪鉴别
if(!SortResule[0]->Genuine)
else
	SortRefGetBaseValue
		gSortRefValue.**mImageRealValue**\[CisId]\[Color] = GetBlockGray
		SortRefValueCalcSingle
			gSortRefValue.**mResultSuccess**[CisId] = 0;
			if(0 == get_soil_base//清分基准获取
				gSortRefValue.mResultSuccess[CisId] = 1;
//ATM清分判断
get_sort_paras//依据清分等级计算清分参数
sort_banknote计算纸币清分结果

# 2 SortRefValueSet 清分基准参数设置指令

Result =SortRefValueSetandSave
	if（STD_TRUE != gSortRefValue.mResultSuccess）
		“SortRefSetStatusErr”
		return(-1);
	BnSoilBaseUpdateParam//更新图像识别基准
		SortBnRefSetUpdate,  gSortRefValue.mImageRealValue
if (Result != 0)
	SortRefValueSetErr



# 3 flow
case FLOW_STATE_START
	Flow_FromPointToPoint
	SortRefBack
		BnSoilRefDataReturnGenerate//格式化并生成图像基准返回值
			SortRefValueCalcAll//计算图像基准平均值
			


## SortRefValueCalcAll
if(TRUE == gSortRefValueAll[Index].mResultSuccess[DevId])
	ColorSumValue += gSortRefValueAll[Index].mImageRealValue
	TotalCount[DevId]++;
            

if(TotalCount[DevId] > 0)
	gSortRefValue.mImageRealValue = ColorSumValue/TotalCount
else
	gSortRefValue.**mResultSuccess**[DevId] = FALSE;


## gSortRefValueAll
SortRefValueSaveToMap (pValue)
	memcpy(&gSortRefValueAll[gSortRefValueIndex],pValue,sizeof(SORT_REF_VALUE_T));


OsTaskImageHandle
	SortRefValueSaveToMap(&gSortRefValue);////每张纸币处理完毕后,将正确的纸币基准值保存 (基本在最后了)



# OsTaskImageHandle
ImageRecognitionSort
CheckTrueClassify//面值面向综合判断
SortRefValueSaveToMap(&gSortRefValue);   //每张纸币处理完毕后,将正确的纸币基准值保存
StartMagneHandle//发送磁处理邮件