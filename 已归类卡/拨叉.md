---
up:
  - "[[../moc/work|work]]"
---
[moc 日志](../moc/moc%20日志.md)
#ed 


#kan 
TrackClearSelectorCtrl (**TC_PATH_INDEX** PathIndex, Dynamic) //通道拨叉动作控制
	if (Dynamic)
		SelConfigList = sPathDynamicSelConfigTable[PathIndex]
	else
		SelConfigList = sPathStaticSelConfigTable[PathIndex];
	for (0, SEL_NUM)
		AppSelectorCtrl (SelConfigList. mSelId, mSelDir)





sPathStaticSelConfigTable\[TC_PATH_NUM]\[SEL_NUM] =
{
	[TC_PATH_ALL_GATE] ={STD_NULL}//不动作任何拨叉
	[TC_PATH_UMS 04_UMS 02_UMS 01] =
		&gSelConfigSEL_UMCM1[SEL_UMCM1_DIR_DEPOSIT],
}



%% gSelConfigSEL_UMCM1 是如何被赋值的 %%
sSelConfigArray\[PATH_NUM]\[SEL_NUM]
	[PATH_DEPOSIT] =
	{
		&gSelConfigSEL_UMCM1[SEL_UMCM 1_DIR_DEPOSIT],
		...
	} ^us14he

 \# define FLOW_MODULE_INFO_INIT (flow, path)
	 [flow] =
	 {
		 .mSelConfigArray = sSelConfigArray[path],
	 } ^uiq6vq

gFlowModuleInfo[FLOW_NUM]
	FLOW_MODULE_INFO_INIT (FLOW_DEPOSIT, PATH_DEPOSIT), ^mw6rf9



TrackClear_DealWith_MotorJam





## 主通道链表如何初始化的
搜索 **sMainPaperListHead**
NewMainPaper
	PaperIndex = MallocPaperManageUnit
	PaperUnitInit (PaperIndex)//介质单元初始化
	OsListInsertAfter (&sMainPaperListHead,&sPaperManage[PaperIndex]. mList);

MallocPaperManageUnit
//申请介质管理单元 sPaperManage
	  if (sPaperManage[PaperIndex]. mUsedFlag == 0)
		   return(PaperIndex);


PaperEnterMainTrackHandle//介质插入处理
	NewMainPaper


**PaperEnterMainTrackCheck**//纸币进入主通道检测
	if (ListIsEmpty (MainListHead))
		PaperEnterMainTrackHandle
		AppGateCashCountAdd
		return 0
	Paper = MainListGetLatestPaper();//获取当前主链表最新进入的纸币
	 if (**PAPER_PASS_FIRST_NODE** (Paper))//纸币纸尾已经通过第一个节点
		 **PaperEnterMainTrackHandle**
		 AppGateCashCountAdd
		 return 0


PaperEnterMainTrackCheck 在主通道和目的分支通道纸币管理



PAPER_PASS_FIRST_NODE（Paper）
	PAPER_PASS_NODE (Paper, 0)
		Paper->**mInfoOfNode**[NodeSN]. mPaperLen != 0
		//mInfoOfNode 纸币通过各个通道传感器节点相关信息记录

### **传感器信息记录如何更新的**
PaperUnitInit //**在介质进入通道 NewMainPaper 中引用**
	for (0, mTrackNodeNum)
		TrackNode = TrackNodeGetInstance
		mInfoOfNode. mTrackNode = TrackNode

TrackNodeGetInstance 
	return **sTrackNodeTable**




## 拨叉动作

搜索 gSelConfigSEL_LBCM1
### 1
FlowSrcGateSelChange (**mGateId**)
	for (SelCnt 0,SEL_NUM)
		\*SelConfig =sFlowSrcGateChangeSelConfigTable\[**mGateId**]\[SelCnt]
		{
			\[GATE_RCB_A1]=
				&gSelConfigSEL_LBCM1\[SEL_LCM_DIR_CASHBOX],
		}
		**AppSelectorCtrl**（**SelConfig**）


FlowSrcGateSelChange 的引用##
纸币流程
 case FLOW_TRANS_STATE_WORKING:
	 TransferConfig->mptCurrFlowCashBoxConfig = &TransferConfig->mCashBoxConfig\[0];
	 FlowSrcGateSelChange (TransferConfig->mptCurrFlowCashBoxConfig->mGateId))
### 2
流程中的钞口管理 GateManagerCheckInFlow
case GATE_MANAGER_INFLOW_FSM_WAITGAP
	TransferConfig->mptCurrFlowCashBoxConfig = &TransferConfig->mCashBoxConfig\[sGateManagerInFlow. mGateNumCurrent];
	FlowSrcGateSelChange(TransferConfig->mptCurrFlowCashBoxConfig->mGateId);
case GATE_MANAGER_INFLOW_FSM_WAITTRACKREADY
	FlowSrcGateSelChange (TransferConfig->mptCurrFlowCashBoxConfig->mGateId);##


sFlowDestSelConfigTable={
[PATH_DEPOSIT] =
	[GATE_RCB_A1]   =   &gSelConfigSEL_LBCM1[SEL_LCM_DIR_CASHBOX],
}
 \# define FLOW_MODULE_INFO_INIT(flow, path)
	 [flow] = 
	 .mGateSelConfigArray  = sFlowDestSelConfigTable[path],  \
		
gFlowModuleInfo
	FLOW_MODULE_INFO_INIT(FLOW_DEPOSIT,         PATH_DEPOSIT),
### 3
DeviceNodeConfigRefresh//在系统变量初始化中初始化
	SelTrigConfigRefreshByNodeUse
		gSelConfigSEL_LBCM1[SEL_LCM_DIR_CASHBOX]. mTriggerNodeId = 
		NODE_ID_MULTI (SEL_LBCM 1_TRIG_NODE_LAS 4, 0);
//只有 gSelConfigSEL_LBCM1 和 2 的赋值？

### 4
![[拨叉#^us14he]]
![[拨叉#^uiq6vq]]
![[拨叉#^mw6rf9]]
### 5  真正赋值
**SEL_CONFIG_STATIC** 
 \# define SEL_CONFIG_STATIC(SelId,Dir0,SelDir0,Dir1,SelDir1)  \
const SEL_CONFIG gSelConfig##SelId[SelId##_DIR_NUM] =       \

**SEL_CONFIG_LCM**


