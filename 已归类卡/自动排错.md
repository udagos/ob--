---
up:
  - "[[../moc/work|work]]"
---
#kan

[moc 日志](../moc/moc%20日志.md)
#ed 


Flow_ErrClearAndRestart_CheckPatch
switch (sErrState)
	FLOW_ERR_STATE_IDLE
		**sErrClearType** = AllModuleErrClearTypeGet != ERR_CLEAR_NOT_DONE
			sErrState = FLOW_ERR_STATE_SRCGATE
	FLOW_ERR_STATE_SRCGATE
		if (sErrClearType == ERR_CLEAR_STOP_CLEAR_RESTART)
		if(sErrClearType != ERR_CLEAR_NOTELONG_PROC)
		sErrState = FLOW_ERR_STATE_TARGATE;
	FLOW_ERR_STATE_TARGATE
		sErrState = FLOW_ERR_STATE_WAIT;
	FLOW_ERR_STATE_WAIT
		if((RestartEn == 0)
		|| (sErrClearType == ERR_CLEAR_STOP_QUIT)
		|| (sErrClearType == ERR_CLEAR_STOP_CLEAR_RESTART)
		|| (sErrClearType == ERR_CLEAR_STOP_CLEAR_SRCBRAN_RETURN)) 
		else
			sphErrState = FLOW_ERR_STATE_RESTAR;
	FLOW_ERR_STATE_TRACK
	FLOW_ERR_STATE_END
	FLOW_ERR_STATE_RESTAR


AllModuleErrClearTypeGet 
	**ERR_CLEAR_TYPE**  ErrClearType FlowModuleErrClearTypeGet MODULE_GET_ERR_CODE
	BvModuleErrClearTypeGet MODULE_GET_ERR_CODE
	GateModuleErrClearTypeGet MODULE_GET_ERR_CODE
	//优先检测完所有模块的错误码,然后才检测各个模块的警告码
	BvModuleErrClearTypeGet MODULE_GET_WARNING_CODE
	FlowModuleErrClearTypeGet
	GateModuleErrClearTypeGet
	returrn ErrClearType


FlowModuleErrClearTypeGet
	 else if (ErrClass == SYS_ERR_CLASS_JAM)
		 ErrCodeUnion. mGroup == ERR_GROUP_NOTE_TRANFER
			  lJamSensorId = ErrCodeUnion. mDetail;
			  if ((lJamSensorId == SENSOR_SCS 1)||(lJamSensorId == SENSOR_SCS 2))
			  else
				  if IsDestGateEntrySensor//检测是否指定目的钞口的计数传感器
					  ErrGateNo = GateNo
			if mError == ERR_TRANS_REACH_TIMEOU && mDetail == CASHBOX_SENSOR_ID
				lErrClearType = ERR_CLEAR_STOP_QUIT;
			else if (GateIsValid (ErrGateNo) && ! IsFlowRejectGate (ErrGateNo)
				lErrClearType = ERR_CLEAR_STOP_QUIT;
			else //非ATM钞口入口塞纸,按正常流程处理
				mError == ERR_TRANS_REACH_TIMEOUT || mError == ERR_TRANS_LEAVE_TIMEOUT
					lErrClearType = ERR_CLEAR_STOP_CLEAR_RESTART
				else if


## 初始化排错

for
	switch (FlowStateErr)
		ERROR_FLOW_STATE_INIT
		ERROR_FLOW_STATE_PHASE2
			TrackClear_ByFlow
		ERROR_FLOW_STATE_BVCHECK
		ERROR_FLOW_STATE_FINISH

TrackClear_ByFlow
for
	PathConfig.mPathIndex == TC_ERR_BREAK
	else if mPathIndex == TC_OK_BREAK 
	else






PathConfig = &sTrackClearCtrl.mTrackClearFlowCfg->mPathList[i]


sTrackClearConfigTable
	TC_FLOW_CFG_INIT(FLOW_DEPOSIT, GATE_OUTLET),
    TC_FLOW_CFG_INIT(FLOW_DISPENSE_BV, GATE_SCB),