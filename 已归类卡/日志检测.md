---
up:
  - "[[../moc/work]]"
---
[moc 日志](../moc/moc%20日志.md)
#ed 
# 拒钞
RJReason
rejectcode
# 测厚
thickness





GateWorkHandle



# 0
## FlwFSMStat
流程控制任务中
 ("FlwFSMStat:%s, GateFSMStat:%s\n", sFlowFsmString[sFlowCurState], GateFsmGetCurStateString ()));

## GateManagerCtrl
流程控制任务和介质管理（上级被流程调用）
("GateManagerCtrl:%s Task:%s, Para:%08 x\n", GetGateCtrlName (GateCtrl), GetGateName (GateNo), Para));
### GateWorkHandle 
在 GateManagerCtrl 下
("GateWorkHandle: Start %s work %s, para:%x, Speed:%d\n", 
            GetGateName (Id), GetGateHandleTaskName (GateCtrl), Para, Speed));


## 在钞口管理线程状态机
("GateManager:MakeGateReady,%08 x\n", GateManagerMsg. mptReadyTbl));

("GateManagerFsm:%s\n", GetGateManagerFsmName (FsmState)));
	下发到其他状态机

## GateHandle 
在各个钞口
## RcbInit:
## NodeChange
## AppMotorCtrl
```
if (OsMsgSend(&sOsMsgMotorCtrl, &MsgMotorCtrl, sizeof(MOTOR_CTRL_MSG)) == OS_EOK)
        {
            DEBUG_LOG_TRACE(DEBUG_LOG_MOTO, ("%s:MsgSendSucc,%s,Mode:%s,Dir:%d,Speed:%d\n", 
                        __FUNCTION__, GetMotorName(MotorId), 
                        GetMotorCtrlModeName(CtrlMode), Dir, TarSpeed));

```
### MidDcMotorInMainStart
("%s:Motor:%s,Dir:%d,Tar:%d\n", __FUNCTION__, ptManager->mName, Dir, TarSpeed));
#### DrvDcMotorInMainStart
DEBUG_LOG_INFO(DEBUG_LOG_MOTO, ("%s:%d,%d,%d,%d\n", 
            __FUNCTION__, 
            ptCfg->mCtrl. mDataTemp. mComponent. mEn, 
            ptCfg->mCtrl. mDataTemp. mComponent. mBrake, 
            ptCfg->mCtrl.mDataTemp.mComponent.mDir, 
            ptCfg->mCtrl.mDataTemp.mComponent.mPwm));
##### DcMotorInMainFsmUpdate
("%s:%s, Err:%08 x, State:%s->%s\n",
            __FUNCTION__, GetMotorName (MotorId), ErrCode, GetMotorSpeedCtrlFsmName (ptManager->mSpeedCtrlFsm), GetMotorSpeedCtrlFsmName (State)));

## 动作停止状态检查（只在初始化中有）
DEBUG_LOG_INFO(DEBUG_LOG_GATEMODULE, ("RcbInit:InRdy check: Sns:%s,%d\n", 
                    SensorGetName(ptCtrl->mSensorManager.mTrack), AppSensorStateGet(ptCtrl->mSensorManager.mTrack), AppSensorGetAd(ptCtrl->mSensorManager.mTrack)));

## FlowGateCtrl
介质管理中和流程管理
流程管理汝
```
GateManagerCtrl(GATE_CTRL_STARTWORKOUT, 
                                    TransferConfig->mptCurrFlowCashBoxConfig->mGateId, 
                                    TransferConfig->mptCurrFlowCashBoxConfig->mNoteNumLimit);   /* 启动源钞口 */

DEBUG_LOG_INFO(DEBUG_LOG_GATEMODULE, 
			("FlowGateCtrl:Start SrcGate %d Name:%s,NoteNum:%d\n", 
			0,
			GetGateName(TransferConfig->mptCurrFlowCashBoxConfig->mGateId),
			TransferConfig->mptCurrFlowCashBoxConfig->mNoteNumLimit));
```

## FlowTrackIsEmpty