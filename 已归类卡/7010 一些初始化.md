---
up:
  - "[[mocWorkCode|mocWorkCode]]"
---
#kan
## 日志模块初始化 
DebugLogInit（DEBUGLOG_START）
	RingBuffModuleInit
## 通信缓冲区初始化
ComBuffInit
	ReceiveBuffInit
		SysDataRecvBuffInit//系统数据
			mBuffStart = SYSDATA_RECVBUFF_HEAD
			mWritePonit = SYSDATA_RECVBUFF_HEAD
			mWritePointAddr = SYSDATARECV_WRITEPOINT_ADDR
			\*mReadPointAddr = mReadPonit
		RtDataRecvBuffInit//实时数据
	DataSendBuffInit



SYSDATA_RECVBUFF_HEAD   (PSCOMMBUFFSTART + 1 * 1024)
SYSDATARECV_WRITEPOINT_ADDR   (PSCOMMBUFFSTART + 0)

//ddr 中
PSCOMMBUFFSTART      (OUTSIDEL_BANLIST_END)     //PS间通信缓冲区开始地址      0x3f700000
## gpio 初始化  
GpioAppInit
	GpioDeviceInit
		XGpioPs_CfgInitialize


Xil_Out 32
[Zynq Reset - element14 Community](https://community.element14.com/products/devtools/avnetboardscommunity/avnetboard-forums/f/microzed-hardware-design/33753/zynq-reset)
## qspiflash 初始化  
QspiFlashInit
	QspiDeviceInit (Prescale)
		XQspiPs_CfgInitialize //设置QSPI为主模式、FLASH接口模式、片选无效、FIFO宽度为32bit
		XQspiPs_SetClkPrescaler (Prescale)//频率
	QspiModeSet (QSPI_MODE_MANUAL, QSPI_CLK_PH_0_POL_0);//手动模式，手动开始，手动片选
		XQspiPs_SetOptions (Options)

首先默认配置初始化 XQspiPs_CfgInitialize，然后再修改配置
看看该 api 接口的写法

## ee 初始化 


## PL控制的外设初始化
HardwareInitPl
	DrvMagentInit
		case DCMOTOR_INMAIN_PL
			DrvMagentStop
	DrvDcMotorInMainInit（DCMOTOR_INMAIN_PL）
		case DCMOTOR_INMAIN_PL
			**DrvDcMotorInMainStop**
	FpgaAdc088Cfg


## 上下部 fpga 初始化
SlaveBoardPowerOnInit
	LFpgaPowerOnInit
		HardwareInitLFpga
	UFpgaPowerOnInit
		HardwareInitUFpga




流程中
	case FLOW_STATE_SELFTEST 
		for（0，3）
			SlaveBoardPowerOnInit
	case FLOW_STATE_WAIT_INIT_CMD 
		if 有异常
			SlaveBoardResetHandle
	case FLOW_STATE_INIT 
		Flow_SystemInit
			if (MidFpgaInitStateGet (DEVICE_UFPGA) == STD_FALSE)
				SlaveBoardPowerOnInit (DEVICE_UFPGA)


UFpgaPowerOnInit
	SlaveBoardGetDataFileFromLinux//从Linux获取从板需要加载的数据
	MidFpgaConf//加载 UFPGA
	HardwareInitUFpga//上部 FPGA 控制的外设初始化
	MidFpgaInitStateSet//FPGA 初始化标志设置

SlaveBoardGetDataFileFromLinux 
	SlaveBoardDataSrcCheck
		从板数据文件信息

HardwareInitUFpga
	DrvDcMotorInMainInit
	DrvStepMotorIoInit
	..




## 串口指令解释线程
OsTaskUartInstrExp
	U8 TestBuf[256];
	UartCommInit
	while
		OsSemWaitForever (&OsSemUartInstrExp);
		while 
			if GetUsedDataSize (gUartRecvBufMq) == 0//所有指令处理完毕
				break 
			GetUartNChar (TestBuf, STD_MINVAR (256,GetUsedDataSize (&gUartRecvBufMq)),POINTMOVE);//临时测试用DataSendToUart(TestBuf,STD_MINVAR(256,GetUsedDataSize(&gUartRecvBufMq)));//临时测试用
			break;//临时测试用

UartCommInit
	UartRecvBufInit
	UartSendBufInit
	UartInit
		UartDeviceInit
		UartInterruptInit（UartIntrCallBack,48）

UartIntrCallBack (**CallBackRef**)
	if Event == XUARTPS_EVENT_RECV_DATA)
	 || (Event == XUARTPS_EVENT_RECV_TOUT)
		 UartRecvDataIntr (CallBackRef)
	 if (Event == XUARTPS_EVENT_SENT_DATA)
		 UartSendDataIntr (CallBackRef)

UartInterruptInit （FuncPtr, TriggerLevel）
	InterruptIdInstall (IntId,**UartIntrHandler**,Param)
	//IntId：中断 ID，对应 XSCUGIC_MAX_NUM_INTR_INPUTS 个设备中断	
	//IntrHandler：中断处理函数
	//Param：中断处理函数参数
		rt_hw_interrupt_install  
			isr_table[vector].handler = handler;
			 isr_table[vector]. param = param;
	XUartPs_SetHandler (FuncPtr)
	XUartPs_SetFifoThreshold (TriggerLevel)
	//接收触发级别指定接收 FIFO 中导致生成接收数据事件（中断）的字节数。
	...